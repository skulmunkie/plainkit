{
  "tag": "pk-tabs",
  "title": "Tabs",
  "group": "Navigation",
  "summary": "A tab strip with optional panels. Children: pk-tab and pk-tab-panel paired by value. Arrow keys, Home and End move between tabs (roving tabindex). It can show no tab as selected, slide sideways with edge fades, and hold trailing content after the last tab.",
  "props": [
    { "name": "value", "type": "string", "default": "", "reflect": true, "commit": "pk-tab-change", "description": "The value of the selected tab. Empty selects the first enabled tab." },
    { "name": "activation", "type": "enum", "default": "auto", "values": ["auto", "manual"], "reflect": true, "description": "auto selects a tab when arrow keys focus it; manual moves focus and waits for Enter or Space." },
    { "name": "noneActive", "type": "boolean", "default": false, "reflect": true, "description": "No tab is shown as selected, and choosing any tab (even the last one chosen) raises pk-tab-change. For a page whose cross-cutting view sits above its tabs." },
    { "name": "scroll", "type": "boolean", "default": false, "reflect": true, "description": "One row that slides (touch swipe, mouse wheel, trackpad, drag) when the tabs outgrow the width, instead of wrapping onto more rows. A fade marks the edge that has more tabs (it follows the scroll, the tab list and the width, and mirrors in right-to-left); moving to a tab with the arrow keys, Home, End or a click scrolls it fully into view, clear of the fade, smoothly unless the reader prefers reduced motion." }
  ],
  "slots": [
    { "name": "tab", "description": "pk-tab children (they assign themselves to this slot).", "selfAssigned": true },
    { "name": "panel", "description": "pk-tab-panel children (they assign themselves to this slot).", "selfAssigned": true },
    { "name": "trailing", "description": "Content inside the strip after the last tab: a hint, links or buttons." }
  ],
  "events": [
    { "name": "pk-tab-change", "detail": "{ value, previous, fallback? }", "detailProps": { "value": "string", "previous": "string", "fallback": "bool" }, "description": "A tab was chosen. Cancelable: preventDefault keeps the previous tab. Also raised, not cancelable, when the value falls back to the first enabled tab (empty or no matching tab), so the host learns the value the element settled on; that one carries fallback: true, so a host that only reacts to a choice the user made can skip it." }
  ],
  "parts": [
    { "name": "list", "description": "The tab strip." },
    { "name": "panels", "description": "The panel area." }
  ],
  "cssProperties": [],
  "methods": [],
  "writes": [
    { "target": "pk-tab children", "attributes": ["selected","tabindex","id","aria-controls"], "why": "selected and tabindex (roving) are set on every sync from the value; id is set only when the tab has none; aria-controls names the paired panel. selected is a reflected prop of pk-tab. Kept when the element disconnects." },
    { "target": "pk-tab-panel children", "attributes": ["selected","tabindex","id","aria-labelledby"], "why": "selected and tabindex follow the value; id is set only when the panel has none; aria-labelledby names the paired tab. Both live in the same tree, so the ids resolve. Kept when the element disconnects." }
  ],
  "a11y": "The strip has role=tablist; tabs and panels are related with aria-controls and aria-labelledby set on the light-DOM children (both live in the same tree, so the ids resolve). Roving tabindex: only the selected tab is in the Tab order (every tab when none is active); arrow keys move focus and skip hidden and disabled tabs.",
  "examples": [
    { "title": "Tabs with panels", "html": "<pk-tabs value=\"details\">\n  <pk-tab value=\"details\">Details</pk-tab>\n  <pk-tab value=\"history\">History</pk-tab>\n  <pk-tab-panel value=\"details\">Item details.</pk-tab-panel>\n  <pk-tab-panel value=\"history\">Item history.</pk-tab-panel>\n</pk-tabs>" },
    { "title": "Scrolling strip with counts and trailing content", "html": "<pk-tabs scroll value=\"a\">\n  <pk-tab value=\"a\" count=\"12\">Pending</pk-tab>\n  <pk-tab value=\"b\" count=\"3\">Failed</pk-tab>\n  <pk-tab value=\"c\">Done</pk-tab>\n  <span slot=\"trailing\">Updated a minute ago</span>\n</pk-tabs>" }
  ]
}
