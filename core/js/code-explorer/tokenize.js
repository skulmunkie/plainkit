// Moved to modules/code-explorer/tokenize.js (the code explorer is a tool and lives with its module). This file only keeps the old import path working.
import * as m from '../../modules/code-explorer/tokenize.js';
export const languageOf = m.languageOf;
export const tokenize = m.tokenize;
export const buildSegments = m.buildSegments;
export const wordAt = m.wordAt;
