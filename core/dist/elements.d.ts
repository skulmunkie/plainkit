// GENERATED by tools/build.mjs from elements/*/*.meta.json: do not edit.
// Include in tsconfig ("include" or a /// <reference>) for tag, property, event and JSX autocomplete.

export interface PkAccordionElement extends HTMLElement {
    /** Opening one item closes the others. */
    exclusive: boolean;
    /** No gaps: one shared frame. */
    flush: boolean;
}

export interface PkAccordionItemElement extends HTMLElement {
    /** Header text. */
    heading: string;
    /** Expanded. */
    open: boolean;
}

export interface PkAlertElement extends HTMLElement {
    /** The kind setting */
    kind: "info" | "success" | "warning" | "danger";
    /** The heading setting */
    heading: string;
    /** The dismissible setting */
    dismissible: boolean;
    /** Page-level strip. */
    banner: boolean;
    /** Coloured text with no box, icon or panel: the quiet form for a message beside a field or under a button. */
    plain: boolean;
    /** Flows in a line of text (like a span) instead of a block; use with plain. */
    inline: boolean;
    /** Smaller text and no margin, for an actions strip. */
    compact: boolean;
    /** Dismisses it */
    dismiss()(...args: unknown[]): unknown;
}

export interface PkAppShellElement extends HTMLElement {
    /** Drawer state, mirrors the side nav. */
    navOpen: boolean;
}

export interface PkAvatarElement extends HTMLElement {
    /** The accessible name and the source of the initials. */
    name: string;
    /** An image URL; initials show until it loads or if it fails. */
    src: string;
    /** Size. */
    size: "md" | "sm" | "lg";
    /** Shape. */
    shape: "circle" | "square";
    /** Status dot. */
    status: "none" | "online" | "away" | "busy";
    /** A colour slot 1 to 5, or muted; by default a stable slot chosen from the name. */
    colour: string;
    /** Initials for a name. */
    initials(name, max)(...args: unknown[]): unknown;
    /** Stable colour slot. */
    colourSlot(name, count)(...args: unknown[]): unknown;
}

export interface PkAvatarGroupElement extends HTMLElement {
    /** Avatars shown before the +N tile. */
    max: number;
    /** Accessible name of the group. */
    label: string;
    /** Shown people and the remainder. */
    groupOverflow(people, max)(...args: unknown[]): unknown;
}

export interface PkBackToTopElement extends HTMLElement {
    /** Scroll container (default: the window). */
    for: string;
    /** The threshold setting */
    threshold: number;
    /** Accessible name */
    label: string;
    /** Set by the element. */
    visible: boolean;
}

export interface PkBadgeElement extends HTMLElement {
    /** Colour. */
    variant: "accent" | "muted" | "ok" | "warn" | "danger" | "outline";
    /** Value to show when the slot is empty, capped by max. */
    count: string;
    /** Counts above read as max+; 0 disables capping. */
    max: number;
    /** A status dot with the text as its label instead of a pill. */
    dot: boolean;
    /** Text such as 99+. */
    formatCount(value, max)(...args: unknown[]): unknown;
}

export interface PkBreadcrumbElement extends HTMLElement {
    /** Landmark name. */
    label: string;
    /** Crumbs shown before the middle folds (3 on a phone). */
    max: number;
}

export interface PkButtonElement extends HTMLElement {
    /** Visual weight. */
    variant: "primary" | "ghost" | "warn" | "secondary" | "plain";
    /** md is the reading-size control; mini is the compact desktop-density one; lg is the large one. */
    size: "md" | "mini" | "lg";
    /** What a click does inside a form: nothing, submit it, or reset it. */
    type: "button" | "submit" | "reset";
    /** Blocks interaction and dims the control. Also set by a disabled fieldset. */
    disabled: boolean;
    /** An accessible name when the visible content is only an icon. */
    label: string;
    /** Shows a spinner and ignores clicks without disabling the button, so focus stays put. */
    busy: boolean;
    /** Replaces the label while busy. */
    busyText: string;
    /** Fills the width of its container. */
    block: boolean;
    /** A square icon-only button (give it a label). */
    icon: boolean;
    /** A button that stays pressed: a click flips pressed. */
    toggle: boolean;
    /** The pressed state of a toggle button. */
    pressed: boolean;
    /** A value reported by pk-toggle. */
    value: string;
    /** Focuses the inner button. */
    focus()(...args: unknown[]): unknown;
}

export interface PkButtonGroupElement extends HTMLElement {
    /** Direction of the bar. */
    orientation: "horizontal" | "vertical";
    /** Fills the container width and shares it between the buttons. */
    fill: boolean;
    /** Accessible name of the group (aria-label). */
    label: string;
    /** single: the toggle buttons in the group release each other (one is always pressed); multi and none leave each toggle alone. */
    mode: "none" | "single" | "multi";
}

export interface PkCalendarElement extends HTMLElement {
    /** The selected day, an ISO date such as 2026-09-19. */
    value: string;
    /** An ISO date whose month is shown; defaults to the value or today. */
    month: string;
    /** Earliest selectable ISO date. */
    min: string;
    /** Latest selectable ISO date. */
    max: string;
    /** First weekday: 0 Sunday, 1 Monday. */
    weekStart: number;
    /** Language of month and weekday names; defaults to the page language. */
    locale: string;
    /** ISO dates that get a dot. */
    marks: json;
    /** Show only; no selection. */
    readonly: boolean;
    /** Inert. */
    disabled: boolean;
    /** Weeks of a month. */
    monthGrid(year, month, weekStart)(...args: unknown[]): unknown;
    /** Keyboard movement. */
    dateForKey(iso, key)(...args: unknown[]): unknown;
    /** Clamped month step. */
    addMonths(iso, n)(...args: unknown[]): unknown;
}

export interface PkCardElement extends HTMLElement {
    /** The card title. Without it (and without actions) the header is not shown. */
    heading: string;
    /** The heading level the title exposes to assistive technology (aria-level). */
    level: number;
    /** error draws the border in the error colour. */
    tone: "default" | "error";
    /** Horizontal puts the media beside the content; it stacks when the card is narrower than 30rem (a container query, not the viewport). */
    orientation: "vertical" | "horizontal";
    /** No padding around the content, for a table or an edge-to-edge body. */
    flush: boolean;
    /** Makes the whole card one link (a single tab stop named by the heading). */
    href: string;
    /** Fills a flex parent (a workspace pane): the card takes the remaining height, has no padding, and its body is the scroll region. */
    fill: boolean;
}

export interface PkChartElement extends HTMLElement {
    /** Chart type. */
    kind: "bar" | "line" | "donut" | "stack" | "spark";
    /** Title (figcaption). */
    caption: string;
    /** viewBox height of bar and line charts. */
    height: number;
    /** Data as a property instead of a table: { labels: string[], series: { name, values: number[] }[] }. */
    data: json;
    /** Summary text of the data details. */
    dataLabel: string;
    /** Bar geometry. */
    barRects(series)(...args: unknown[]): unknown;
    /** Line points. */
    linePoints(series)(...args: unknown[]): unknown;
    /** Donut dash arrays. */
    donutSegments(values)(...args: unknown[]): unknown;
    /** Sparkline points. */
    sparkPoints(values)(...args: unknown[]): unknown;
}

export interface PkCheckboxElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The value submitted while checked. */
    value: string;
    /** Whether the box is checked. */
    checked: boolean;
    /** The mixed state (some of a group are checked). */
    indeterminate: boolean;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Puts this checkbox in a select-all group of that name. */
    group: string;
    /** Makes this the select-all checkbox of the group of that name; it shows checked, unchecked or indeterminate as its members change. */
    master: string;
    /** sm is the compact checkbox of a toolbar: smaller label text. */
    size: "md" | "sm";
    /** Toggles as a click does. */
    toggle()(...args: unknown[]): unknown;
    /** Focuses the box. */
    focus()(...args: unknown[]): unknown;
}

export interface PkClusterElement extends HTMLElement {
    /** Row (wraps at the edge) or column. */
    direction: "horizontal" | "vertical";
    /** Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default sm (the control gap). */
    gap: "none" | "xs" | "sm" | "md" | "lg" | "xl";
    /** Cross-axis alignment of the items. */
    align: "start" | "center" | "end" | "stretch" | "baseline";
    /** Main-axis distribution of the items. */
    justify: "start" | "center" | "end" | "between";
    /** Keep a horizontal cluster on one line instead of wrapping (wrapping is the default). */
    nowrap: boolean;
}

export interface PkCodeBlockElement extends HTMLElement {
    /** File or language shown in the bar; also the region's name. */
    label: string;
    /** A gutter of line numbers. */
    lineNumbers: boolean;
    /** Wrap long lines. */
    wrap: boolean;
    /** Hide the copy button. */
    noCopy: boolean;
    /** Cap the height, any CSS length. */
    maxHeight: string;
    /** Lines without blank ends. */
    splitLines(text)(...args: unknown[]): unknown;
    /** Remove the shared indent. */
    dedent(text)(...args: unknown[]): unknown;
    /** Async clipboard copy. */
    copyText(text)(...args: unknown[]): unknown;
}

export interface PkColourInputElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The colour as #rrggbb. */
    value: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Focuses the hex field. */
    focus()(...args: unknown[]): unknown;
}

export interface PkComboboxElement extends HTMLElement {
    /** autocomplete filters as you type; select is a button that opens the list (type a letter to jump). Prefer the native pk-select on a phone. */
    mode: "autocomplete" | "select";
    /** The form field name. */
    name: string;
    /** The current value; what the form submits. */
    value: string;
    /** Hint shown while empty. */
    placeholder: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Autocomplete: keep typed text that is not an option, and use it as the value. */
    free: boolean;
    /** client also filters the options here; off leaves filtering to the host, which listens for pk-combo-query and replaces the options. */
    filtering: "client" | "off";
    /** Whether the list is open. */
    open: boolean;
    /** Opens the list. */
    show()(...args: unknown[]): unknown;
    /** Closes the list. */
    hide()(...args: unknown[]): unknown;
    /** Focuses the control. */
    focus()(...args: unknown[]): unknown;
}

export interface PkCommandPaletteElement extends HTMLElement {
    /** Whether it is shown */
    open: boolean;
    /** The placeholder setting */
    placeholder: string;
    /** localStorage key; empty disables recents. */
    recentsKey: string;
    /** Do not bind Ctrl/Cmd+K on the document. */
    noShortcut: boolean;
    /** Accessible name of the dialog. */
    label: string;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
    /** { id, label, group?, hint?, keywords?, href?, shortcut? }[] (property only). */
    items (property)(...args: unknown[]): unknown;
}

export interface PkContextMenuElement extends HTMLElement {
    /** Whether the menu is shown. */
    open: boolean;
    /** Leave the browser menu alone. */
    disabled: boolean;
    /** See the description */
    showAt(x, y)(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
}

export interface PkDialogElement extends HTMLElement {
    /** Whether it is shown */
    open: boolean;
    /** The heading setting */
    heading: string;
    /** The size setting */
    size: "sm" | "md" | "lg" | "xl" | "fullscreen";
    /** The body scrolls inside a bounded dialog. */
    scrollable: boolean;
    /** Hide the header close button (the dialog offers its own Cancel in the footer). */
    hideClose: boolean;
    /** Card width in pixels before the viewport clamp; 0 keeps the size preset. */
    maxWidth: number;
    /** A tinted card: product (green wash) or archived (red wash). */
    tint: "none" | "product" | "archived";
    /** The host owns open: close requests (Escape, close button, backdrop, swipe) only raise pk-close and never close the element; the host closes it by clearing open. Used by Blazor wrappers. */
    controlled: boolean;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
    /** See the description */
    PkDialog.confirm(options) -> Promise<boolean>(...args: unknown[]): unknown;
    /** See the description */
    PkDialog.alert(options) -> Promise<void>(...args: unknown[]): unknown;
    /** See the description */
    PkDialog.prompt(options) -> Promise<string|null>(...args: unknown[]): unknown;
}

export interface PkDividerElement extends HTMLElement {
    /** A vertical rule; the parent must be a flex row. */
    vertical: boolean;
    /** Where the label sits. */
    labelPosition: "center" | "start";
    /** Remove the separator role. */
    decorative: boolean;
}

export interface PkDrawerElement extends HTMLElement {
    /** Whether it is shown */
    open: boolean;
    /** Edge it slides from. */
    side: "left" | "right" | "bottom";
    /** Title; also the accessible name. */
    heading: string;
    /** The backdrop does not close it. */
    persistent: boolean;
    /** A wider panel (up to 70% of the viewport). */
    wide: boolean;
    /** The host owns open: close requests (Escape, close button, backdrop, swipe) only raise pk-close and never close the element; the host closes it by clearing open. Used by Blazor wrappers. */
    controlled: boolean;
    /** A non-modal inspector: it sits inside the nearest positioned ancestor instead of the viewport, has no backdrop and no focus trap, and the page behind stays usable. Escape still asks to close. */
    docked: boolean;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
}

export interface PkDropdownElement extends HTMLElement {
    /** Whether the menu is shown. */
    open: boolean;
    /** Preferred side. */
    placement: string;
    /** Stay open after an item is chosen (multi-select filters). */
    keepOpen: boolean;
    /** A wide panel whose items flow into columns (navbar mega menu). */
    mega: boolean;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
}

export interface PkDropzoneElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** Accepted types: extensions, MIME types or type/* (like the accept attribute). */
    accept: string;
    /** Allows several files. */
    multiple: boolean;
    /** Largest file: 500KB, 5MB, 1GB or bytes. */
    maxSize: string;
    /** Most files (multiple); 0 means no limit. */
    maxFiles: number;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Removes every file. */
    clear()(...args: unknown[]): unknown;
    /** Focuses the file input. */
    focus()(...args: unknown[]): unknown;
}

export interface PkEmptyStateElement extends HTMLElement {
    /** The title. */
    heading: string;
    /** Heading level exposed to assistive technology. */
    level: number;
    /** The explanation. */
    description: string;
    /** Density and frame. */
    tone: "default" | "compact" | "boxed" | "error";
    /** Announce the element politely when it appears (role status), for results that replace a search. */
    announce: boolean;
}

export interface PkFieldElement extends HTMLElement {
    /** The label text (or use the label slot). */
    label: string;
    /** Help text under the control. */
    help: string;
    /** An error message; puts the control into the invalid state. */
    error: string;
    /** A warning message; puts the control into the warning state. */
    warning: string;
    /** Marks the field required (a star, and required on the control). */
    required: boolean;
    /** Counter maximum: shows length / max and turns warm from 90% and red past the max; 0 hides the counter. */
    max: number;
    /** stack puts the label above the control; row puts it in a fixed column beside it (stacks on a phone); inline puts label and control on one line at their natural width. */
    layout: "stack" | "row" | "inline";
    /** sm is the compact field of a toolbar or filter row: smaller label, tighter gap. */
    size: "md" | "sm";
}

export interface PkFieldListElement extends HTMLElement {
    /** Section title. */
    heading: string;
    /** Heading level exposed to assistive technology. */
    level: number;
    /** Term beside value, or above it. */
    layout: "grid" | "stacked";
    /** A rule under each value. */
    dividers: boolean;
    /** Tighter rhythm and smaller text. */
    dense: boolean;
}

export interface PkFieldRowElement extends HTMLElement {
    /** Vertical alignment of the fields in the row. */
    align: "stretch" | "end" | "center";
}

export interface PkFormElement extends HTMLElement {
    /** When to check: blur (after leaving a field, and on submit), input (while typing) or submit (only on submit). A field already showing an error re-checks as you type. */
    validate: "blur" | "input" | "submit";
    /** Also shows the valid state on fields that hold a value. */
    showValid: boolean;
    /** Shows a summary of the problems with links to the fields. */
    summary: boolean;
    /** Checks every control now; returns true when all are valid. */
    validateAll()(...args: unknown[]): unknown;
}

export interface PkFormActionsElement extends HTMLElement {
    /** Sticks to the bottom of the scrolling area with a top edge, clearing the phone home indicator. */
    sticky: boolean;
    /** Where the buttons sit. */
    align: "start" | "end" | "between";
}

export interface PkFormSectionElement extends HTMLElement {
    /** The section heading (or use the heading slot). */
    heading: string;
    /** A sentence under the heading. */
    description: string;
}

export interface PkGalleryElement extends HTMLElement {
    /** What to show: foundations, controls, elements, layouts or templates. Empty shows the controls when a group or control is named, otherwise everything. */
    kind: string;
    /** One control group by name ("Forms & inputs") or slug (forms-inputs); with kind layouts or templates, that samples group. */
    group: string;
    /** One control, element or sample id, or a comma separated list ("button,input"); elements also match by tag (pk-tabs). */
    control: string;
    /** The theme of the gallery and its samples. auto keeps the gallery's own default (dark). */
    theme: "auto" | "dark" | "light";
    /** Render the samples at desktop width or in a 375px phone frame. */
    width: "desktop" | "phone";
    /** Search text: only controls whose name (or group) contains it are shown. */
    filter: string;
    /** none shows only the content, sized to fit it; full keeps the contents nav, the viewport and text size toolbar and the element inspector (Details: an element's API and live markup). */
    chrome: "none" | "full";
    /** A fixed height in pixels. 0 fits the content when chrome is none and uses the default height when chrome is full. */
    height: number;
    /** Address of the gallery's embed.html when it is not next to the element module, for example on a CDN prefix. */
    src: string;
}

export interface PkGridElement extends HTMLElement {
    /** Minimum column width as a CSS length (for example 12rem or 200px). Columns are equal width and never narrower than this, so the grid drops to one column when the container is narrower than two. */
    min: string;
    /** The most columns to use, 0 for no limit. The grid still drops columns when they would be narrower than min. */
    columns: number;
    /** Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default md (the card gap). */
    gap: "none" | "xs" | "sm" | "md" | "lg" | "xl";
}

export interface PkHintElement extends HTMLElement {
    /** The toggle text. */
    label: string;
    /** Expanded. */
    open: boolean;
    /** Accent colour. */
    tone: "info" | "warn";
}

export interface PkIconElement extends HTMLElement {
    /** The symbol id in the sprite (search, settings, orders, ...). */
    name: string;
    /** md is 1.25em and follows the text; sm, lg and xl are fixed steps of the space scale. */
    size: "sm" | "md" | "lg" | "xl";
    /** The accessible name. Without it the icon is decorative and hidden from assistive technology. */
    label: string;
}

export interface PkImageGalleryElement extends HTMLElement {
    /** The images: [{ src, alt, primary?, status? }]. status is a short warning badge such as Staged. Property or a JSON attribute. Only same-site paths, http(s) and raster data URLs are shown. */
    images: json;
    /** The index of the primary image; -1 uses the first image flagged primary in images, or none. */
    primary: number;
    /** A fixed number of equal columns (1 to 12). 0 fits as many as min allows. */
    columns: number;
    /** The narrowest a thumbnail may get, as a css length in px, rem or em. Used when columns is 0. */
    min: string;
    /** Show make-primary and remove buttons under each image and the add tile. */
    editable: boolean;
    /** Text on the add tile. */
    addLabel: string;
    /** The file types the add tile's picker offers. */
    accept: string;
    /** Opens the lightbox at an image. */
    view(index)(...args: unknown[]): unknown;
    /** Marks an image primary, as the button does (raises pk-primary-change first). */
    makePrimary(index)(...args: unknown[]): unknown;
    /** Removes an image, as the button does (raises pk-remove first). */
    removeImage(index)(...args: unknown[]): unknown;
}

export interface PkInputElement extends HTMLElement {
    /** The kind of value. Date and time types open the platform picker. */
    type: "text" | "email" | "url" | "tel" | "search" | "password" | "number" | "date" | "time" | "datetime-local" | "month" | "week";
    /** The form field name. */
    name: string;
    /** The current value; what the form submits. */
    value: string;
    /** Hint shown while empty (ignored by a floating label). */
    placeholder: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The value can be read and selected but not edited. */
    readonly: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Shows the warning state (a value the user may want to check). */
    warning: boolean;
    /** Shows the valid state. */
    valid: boolean;
    /** Minimum value (number and date types). */
    min: string;
    /** Maximum value (number and date types). */
    max: string;
    /** Step for number and date types; also the stepper increment. */
    step: string;
    /** Minimum text length. */
    minlength: string;
    /** Maximum text length. */
    maxlength: string;
    /** A regular expression the whole value must match. */
    pattern: string;
    /** The autofill token, for example current-password, new-password or one-time-code. */
    autocomplete: string;
    /** The virtual keyboard to request (numeric, decimal, email, ...). */
    inputmode: string;
    /** Control height. */
    size: "sm" | "md" | "lg";
    /** Shows a clear button while there is a value. */
    clearable: boolean;
    /** Password only: a show and hide button. */
    reveal: boolean;
    /** Number only: minus and plus buttons that respect min, max and step. */
    stepper: boolean;
    /** money formats the value as an amount (1,234.50) when the field is left and shows the plain number while editing; value stays the plain number. */
    format: "none" | "money";
    /** Decimal places for the money format. */
    decimals: number;
    /** ISO currency code whose symbol leads a money field. */
    currency: string;
    /** Locale for money formatting. */
    locale: string;
    /** Shows label inside the field until it has focus or a value, then floats it to the top edge. */
    floating: boolean;
    /** Password reveal: seconds until the value is masked again; 0 keeps it revealed. */
    autohide: number;
    /** Search only: milliseconds to wait after typing before pk-search fires; 0 fires on every change. */
    debounce: number;
    /** Moves focus to the inner input. */
    focus()(...args: unknown[]): unknown;
}

export interface PkLightboxElement extends HTMLElement {
    /** The index setting */
    index: number;
    /** Accessible name */
    label: string;
    /** Whether it is shown */
    open: boolean;
    /** See the description */
    show(index)(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
    /** { src, alt, caption }[] (property only). */
    items (property)(...args: unknown[]): unknown;
}

export interface PkListGroupElement extends HTMLElement {
    /** Row behaviour and frame. */
    variant: "plain" | "action" | "flush";
    /** Rows are labels with a checkbox. */
    checklist: boolean;
    /** Accessible name of the list. */
    label: string;
}

export interface PkLoadingOverlayElement extends HTMLElement {
    /** The busy setting */
    busy: boolean;
    /** Announced and shown. */
    label: string;
    /** Cover the viewport. */
    fullscreen: boolean;
}

export interface PkLocalTimeElement extends HTMLElement {
    /** The instant: an ISO 8601 string (2026-09-19T14:30:00Z). A date-only value is a calendar day and is never shifted by the time zone. */
    datetime: string;
    /** What to show. relative reads 3 hours ago and refreshes every 30 seconds while connected; its title holds the full date. */
    format: "datetime" | "date" | "time" | "relative";
    /** How much of the date to spell out (9/19/26, Sep 19, 2026, September 19, 2026). */
    length: "short" | "medium" | "long";
    /** A BCP 47 tag such as de-DE; empty uses the reader's browser locale. */
    locale: string;
    /** An IANA zone such as Europe/Berlin; empty uses the reader's own zone. */
    timeZone: string;
    /** The pure formatter the element uses: format, length, locale, timeZone and now. */
    formatLocal(value, options)(...args: unknown[]): unknown;
}

export interface PkMediaElement extends HTMLElement {
    /** Aspect ratio as width/height: 16/9, 4/3, 1/1, 3/4. */
    ratio: string;
    /** How media fills the box. */
    fit: "cover" | "contain";
    /** Figure caption. */
    caption: string;
    /** The box becomes a button that fires pk-open. */
    lightbox: boolean;
    /** Square corners. */
    square: boolean;
}

export interface PkMenuItemElement extends HTMLElement {
    /** What the row is. */
    type: "item" | "checkbox" | "radio" | "header" | "divider";
    /** Checkbox and radio state. */
    checked: boolean;
    /** Skipped by keyboard, not activatable. */
    disabled: boolean;
    /** Destructive styling. */
    danger: boolean;
    /** Reported in pk-select; defaults to the label. */
    value: string;
    /** Renders as a link. */
    href: string;
    /** A submenu is open. */
    open: boolean;
}

export interface PkNavItemElement extends HTMLElement {
    /** Makes it a link; omit for a branch. */
    href: string;
    /** The current setting */
    current: boolean;
    /** Prevents interaction */
    disabled: boolean;
    /** Branch open. */
    expanded: boolean;
    /** Set by the nav in icon-rail mode. */
    rail: boolean;
    /** In the icon rail, the branch is open as a flyout beside the rail. */
    flyout: boolean;
}

export interface PkNavbarElement extends HTMLElement {
    /** Accessible name */
    label: string;
    /** Links unfolded (small screens). */
    open: boolean;
    /** The sticky setting */
    sticky: boolean;
}

export interface PkOtpInputElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The code typed so far. */
    value: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Number of cells. */
    length: number;
    /** The alphabet: digits, letters and digits, or anything. */
    type: "numeric" | "alnum" | "any";
    /** Puts a dash after this many cells (0 for none). */
    separatorAt: number;
    /** Focuses the first empty cell. */
    focus()(...args: unknown[]): unknown;
    /** Empties the code. */
    clear()(...args: unknown[]): unknown;
}

export interface PkPageHeaderElement extends HTMLElement {
    /** The title. Without it (and without default or actions content) the title bar is not shown. */
    heading: string;
    /** The heading level the title exposes to assistive technology (aria-level). */
    level: number;
    /** page is the standard page top; section is a titled section inside a page (title, actions, note); record is the compact strip of a single-record page (chips on the left, actions right). */
    variant: "page" | "section" | "record";
}

export interface PkPagerElement extends HTMLElement {
    /** Accessible name */
    label: string;
}

export interface PkPaginationElement extends HTMLElement {
    /** Current page, 1-based. */
    page: number;
    /** Page count; 0 derives it from total and pageSize. */
    pages: number;
    /** Item count; enables the range summary. */
    total: number;
    /** Items per page. */
    pageSize: number;
    /** Options for the page-size select, for example [10, 25, 50, 100]; empty hides the select. */
    sizes: json;
    /** Pages either side of the current one. */
    siblings: number;
    /** Pages always shown at each end. */
    boundary: number;
    /** Layout. */
    mode: "numbered" | "simple" | "load-more";
    /** Accessible name of the nav. */
    label: string;
    /** Load-more busy state. */
    loading: boolean;
    /** Show First and Last buttons beside Previous and Next. */
    edges: boolean;
    /** The numbers and gaps to show. */
    pageWindow(current, total, options)(...args: unknown[]): unknown;
    /** First and last item on a page. */
    pageRange(page, size, total)(...args: unknown[]): unknown;
}

export interface PkPopoverElement extends HTMLElement {
    /** Whether the panel is shown. */
    open: boolean;
    /** Preferred side. */
    placement: string;
    /** What opens it. */
    trigger: "click" | "hover" | "manual";
    /** Panel title. */
    heading: string;
    /** Accessible name when there is no heading. */
    label: string;
    /** confirm adds Cancel and Confirm buttons. */
    variant: "default" | "confirm";
    /** Body text for the confirm variant. */
    message: string;
    /** Confirm button text. */
    confirmLabel: string;
    /** Cancel button text. */
    cancelLabel: string;
    /** Styles Confirm as destructive. */
    danger: boolean;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
    /** Toggles it */
    toggle()(...args: unknown[]): unknown;
}

export interface PkProgressElement extends HTMLElement {
    /** Current value. */
    value: number;
    /** Upper bound. */
    max: number;
    /** Colour. */
    variant: "accent" | "ok" | "warn" | "danger";
    /** Height. */
    size: "md" | "sm" | "lg";
    /** Diagonal stripes. */
    striped: boolean;
    /** Unknown duration: the bar sweeps. */
    indeterminate: boolean;
    /** Text above the bar; also its accessible name. */
    label: string;
    /** Append the percentage next to the label. */
    showValue: boolean;
    /** Two percentages, for example "70,90": the bar turns warn then danger. */
    levelThresholds: string;
}

export interface PkRadioGroupElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The current value; what the form submits. */
    value: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** radio draws a list of radios; segmented draws one bar of segments. */
    variant: "radio" | "segmented";
    /** Radio variant: stacked or in a wrapping row. */
    direction: "vertical" | "horizontal";
    /** Segmented variant: spread the segments across the full width. */
    fill: boolean;
    /** Focuses the chosen radio, or the first enabled one. */
    focus()(...args: unknown[]): unknown;
}

export interface PkRangeElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** Lowest value. */
    min: number;
    /** Highest value. */
    max: number;
    /** Increment. */
    step: number;
    /** Single mode: the value. */
    value: number;
    /** Dual mode: the low value. */
    valueLow: number;
    /** Dual mode: the high value. */
    valueHigh: number;
    /** Two thumbs: a low and a high value; the low thumb never passes the high one. */
    dual: boolean;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** Shows the current value under the slider. */
    output: boolean;
    /** Focuses the first thumb. */
    focus()(...args: unknown[]): unknown;
}

export interface PkRatingElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The rating, 0 for none. */
    value: number;
    /** Number of stars. */
    max: number;
    /** The display form: not interactive, exposed as an image with a text alternative. */
    readonly: boolean;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** Focuses the chosen star. */
    focus()(...args: unknown[]): unknown;
}

export interface PkScrollProgressElement extends HTMLElement {
    /** Scroll container (default: the window). */
    for: string;
}

export interface PkSelectElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The current value; what the form submits. */
    value: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Shows the warning state (a value the user may want to check). */
    warning: boolean;
    /** Shows the valid state. */
    valid: boolean;
    /** Allows several selections (value is comma-separated; the form submits one entry each). */
    multiple: boolean;
    /** Moves focus to the select. */
    focus()(...args: unknown[]): unknown;
}

export interface PkSelectMenuElement extends HTMLElement {
    /** Selected option value. */
    value: string;
    /** Form field name. */
    name: string;
    /** Shown with no selection. */
    placeholder: string;
    /** Whether the list is shown. */
    open: boolean;
    /** Prevents interaction */
    disabled: boolean;
    /** A value is required */
    required: boolean;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
}

export interface PkSideNavElement extends HTMLElement {
    /** Accessible name */
    label: string;
    /** Icon rail. */
    collapsed: boolean;
    /** Drawer open (phones and tablets). */
    open: boolean;
    /** Show the filter box. */
    filterable: boolean;
    /** localStorage key for collapsed and open branches. */
    persist: string;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
    /** Toggles the icon rail */
    toggleCollapsed()(...args: unknown[]): unknown;
}

export interface PkSkeletonElement extends HTMLElement {
    /** Shape. */
    variant: "line" | "text" | "circle" | "block";
    /** Lines in the text variant. */
    lines: number;
    /** Diameter of a circle or height of a block, any CSS length. */
    size: string;
    /** Text for assistive technology. */
    label: string;
}

export interface PkSkipLinkElement extends HTMLElement {
    /** Same-page target. */
    href: string;
}

export interface PkSpinnerElement extends HTMLElement {
    /** Shape. */
    variant: "ring" | "dots" | "bar";
    /** Size. */
    size: "md" | "sm" | "lg";
    /** Accessible status text. */
    label: string;
    /** Dim and cover a position: relative parent. */
    overlay: boolean;
}

export interface PkSplitButtonElement extends HTMLElement {
    /** Colour of both halves. */
    variant: "primary" | "ghost";
    /** What the main action does inside a form. */
    type: "button" | "submit" | "reset";
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** Accessible name of the caret button. */
    toggleLabel: string;
    /** Whether the menu is open. */
    open: boolean;
    /** Which edge the menu lines up with. */
    menuAlign: "start" | "end";
    /** Opens the menu. */
    show()(...args: unknown[]): unknown;
    /** Closes the menu. */
    hide()(...args: unknown[]): unknown;
}

export interface PkStackElement extends HTMLElement {
    /** Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default md (the flow space). */
    gap: "none" | "xs" | "sm" | "md" | "lg" | "xl";
    /** Cross-axis alignment: stretch fills the width, the others size each child to its content. */
    align: "stretch" | "start" | "center" | "end";
    /** Draw a rule between children. */
    dividers: boolean;
}

export interface PkStatElement extends HTMLElement {
    /** What the number is. */
    label: string;
    /** The headline number, already formatted. */
    value: string;
    /** A line under the value. */
    subtext: string;
    /** Value colour. */
    tone: "neutral" | "positive" | "warning" | "critical";
    /** Percent change, for example 12.5 or -3; shows an arrow and a signed percentage. */
    delta: string;
    /** Override the direction derived from delta. */
    deltaDirection: "auto" | "up" | "down" | "flat";
    /** Down is good news (costs, returns): swaps the trend colours. */
    invert: boolean;
    /** What the change compares with, spoken to assistive technology. */
    versus: string;
    /** Points for the sparkline (needs at least two). */
    values: json;
    /** Makes the tile a link. */
    href: string;
    /** The compact dashboard tile look: less padding, a 600-weight larger value, a panel background. */
    tile: boolean;
    /** Without an href, make the tile a button (Enter and Space) that fires pk-activate. */
    interactive: boolean;
    /** Direction and percent. */
    delta(current, previous)(...args: unknown[]): unknown;
    /** A signed percentage. */
    formatDelta(d)(...args: unknown[]): unknown;
    /** The spoken form. */
    deltaSpeech(d, versus)(...args: unknown[]): unknown;
}

export interface PkStepElement extends HTMLElement {
    /** The title setting */
    heading: string;
    /** The description setting */
    description: string;
    /** Set by the stepper. */
    state: "todo" | "active" | "done" | "error";
    /** One-based, set by the stepper. */
    index: number;
    /** Prevents interaction */
    disabled: boolean;
    /** Set by the stepper: the last step draws no connector. */
    last: boolean;
    /** Set by the stepper: the step can be entered. */
    clickable: boolean;
    /** Set by the stepper. */
    orientation: "horizontal" | "vertical";
}

export interface PkStepperElement extends HTMLElement {
    /** Zero-based current step. */
    current: number;
    /** The orientation setting */
    orientation: "horizontal" | "vertical";
    /** Steps can be entered by click or key. */
    clickable: boolean;
    /** Allow skipping ahead of the furthest step reached (non-linear). */
    free: boolean;
    /** Comma-separated step indexes with an error. */
    errors: string;
    /** Accessible name. */
    label: string;
    /** See the description */
    goTo(index)(...args: unknown[]): unknown;
    /** Next step */
    next()(...args: unknown[]): unknown;
    /** Previous step */
    previous()(...args: unknown[]): unknown;
}

export interface PkSwitchElement extends HTMLElement {
    /** Whether the switch is on. */
    checked: boolean;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form field name. */
    name: string;
    /** The value submitted while checked. */
    value: string;
    /** Shows the invalid state (a red ring) and sets aria-invalid. */
    invalid: boolean;
    /** Track size. */
    size: "sm" | "md" | "lg";
    /** end puts the label after the switch, start before it, between pushes them to opposite edges of the row. */
    labelPosition: "end" | "start" | "between";
}

export interface PkTabElement extends HTMLElement {
    /** The key that pairs this tab with the pk-tab-panel of the same value. */
    value: string;
    /** Set by the surrounding pk-tabs. */
    selected: boolean;
    /** Cannot be selected or focused by arrow keys. */
    disabled: boolean;
    /** A badge shown in muted parentheses after the label (a queue count); empty shows none. */
    count: string;
    /** Shows a close button; pk-tab-close is raised when it is clicked and the page removes the tab. */
    closable: boolean;
    /** all shows the tab everywhere; phone only at phone width (640px and below); desktop hides it at phone width. */
    only: "all" | "phone" | "desktop";
}

export interface PkTabPanelElement extends HTMLElement {
    /** The key that pairs this panel with the pk-tab of the same value. */
    value: string;
    /** Set by the surrounding pk-tabs. */
    selected: boolean;
}

export interface PkTableElement extends HTMLElement {
    /** Column definitions: { key, label, type?: text|number|date, align?: start|end, sortable?, hidePhone? }[]. A JSON attribute or a property. */
    columns: json;
    /** Row data, one object per row. A JSON attribute or a property. */
    rows: json;
    /** The field that identifies a row. */
    rowKey: string;
    /** Alternate row tint. */
    striped: boolean;
    /** Tint the row under the pointer. */
    hover: boolean;
    /** Border on every cell. */
    bordered: boolean;
    /** Cell padding step. */
    density: "default" | "compact" | "comfortable";
    /** Pin the header row (give the table a maxHeight). */
    stickyHeader: boolean;
    /** Pin the first column. */
    stickyColumn: boolean;
    /** Checkbox column with select-all. */
    selectable: boolean;
    /** Rows are activatable: they show a pointer and fire pk-row-click. */
    clickable: boolean;
    /** A filter input under each header. */
    filterable: boolean;
    /** The host sorts, filters and pages; rows are shown exactly as given. */
    manual: boolean;
    /** Key of the column being sorted. */
    sort: string;
    /** Sort direction. */
    sortDir: "ascending" | "descending";
    /** Filter text per column key: { [key]: text }. */
    filters: json;
    /** Ids of the selected rows. */
    selected: json;
    /** Show placeholder rows and mark the table busy. */
    loading: boolean;
    /** Each row becomes a card below 640px. */
    cards: boolean;
    /** Table caption. */
    caption: string;
    /** Accessible name of the scrolling region. */
    label: string;
    /** Bounds the frame; sticky header and column need it. */
    maxHeight: string;
    /** Slotted-table mode: no scroll frame, for a table inside a container that already scrolls. */
    flow: boolean;
    /** Sort programmatically. */
    sortBy(key, direction)(...args: unknown[]): unknown;
}

export interface PkTabsElement extends HTMLElement {
    /** The value of the selected tab. Empty selects the first enabled tab. */
    value: string;
    /** auto selects a tab when arrow keys focus it; manual moves focus and waits for Enter or Space. */
    activation: "auto" | "manual";
    /** No tab is shown as selected, and choosing any tab (even the last one chosen) raises pk-tab-change. For a page whose cross-cutting view sits above its tabs. */
    noneActive: boolean;
    /** One row that slides (touch swipe, wheel, drag) when the tabs outgrow the width, with a fade at the edge that has more tabs, instead of wrapping onto more rows. */
    scroll: boolean;
}

export interface PkTagElement extends HTMLElement {
    /** Show the remove button. */
    removable: boolean;
    /** Sent in the remove event; defaults to the text. */
    value: string;
    /** Dim the tag and disable the button. */
    disabled: boolean;
}

export interface PkTagInputElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The tags, comma-joined. */
    value: string;
    /** Hint shown in the empty field. */
    placeholder: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Characters that end a tag (besides Enter). */
    separators: string;
    /** Most tags allowed; 0 means no limit. */
    max: number;
    /** Allows the same tag twice (case-insensitive by default). */
    allowDuplicates: boolean;
    /** Focuses the text field. */
    focus()(...args: unknown[]): unknown;
}

export interface PkTextareaElement extends HTMLElement {
    /** The form field name. */
    name: string;
    /** The current value; what the form submits. */
    value: string;
    /** Hint shown while empty. */
    placeholder: string;
    /** The accessible name (aria-label). pk-field fills it from its own label when this is empty. */
    label: string;
    /** Help and error text, exposed as aria-description; pk-field fills it. */
    description: string;
    /** Blocks interaction; also set by a disabled fieldset. */
    disabled: boolean;
    /** The value can be read but not edited. */
    readonly: boolean;
    /** The form cannot be submitted while the value is empty. */
    required: boolean;
    /** Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). */
    invalid: boolean;
    /** Shows the warning state (a value the user may want to check). */
    warning: boolean;
    /** Shows the valid state. */
    valid: boolean;
    /** Visible rows of a fixed-height textarea. */
    rows: number;
    /** Minimum text length. */
    minlength: string;
    /** Maximum text length. */
    maxlength: string;
    /** Grows with the content instead of scrolling. */
    autogrow: boolean;
    /** Auto-grow cap in pixels; 0 means no cap and the box scrolls beyond it otherwise. */
    maxHeight: number;
    /** Moves focus to the textarea. */
    focus()(...args: unknown[]): unknown;
    /** Selects the text. */
    select()(...args: unknown[]): unknown;
}

export interface PkTimelineElement extends HTMLElement {
    /** Accessible name of the list. */
    label: string;
}

export interface PkTimelineItemElement extends HTMLElement {
    /** Title of the event. */
    heading: string;
    /** An ISO date or any text; shown in a time element. */
    time: string;
    /** Marker colour and fill. */
    status: "pending" | "done" | "active" | "danger";
}

export interface PkToastElement extends HTMLElement {
    /** The kind setting */
    kind: "info" | "success" | "warning" | "danger";
    /** The heading setting */
    heading: string;
    /** Used when nothing is slotted. */
    message: string;
    /** Milliseconds before it dismisses itself; 0 keeps it until dismissed; -1 is automatic (5s, 8s with an action). */
    duration: number;
    /** Hide the dismiss button. */
    noClose: boolean;
    /** Dismisses it */
    dismiss()(...args: unknown[]): unknown;
}

export interface PkToastStackElement extends HTMLElement {
    /** The position setting */
    position: "top-start" | "top" | "top-end" | "bottom-start" | "bottom" | "bottom-end";
    /** Visible at once; the rest queue. */
    max: number;
    /** See the description */
    push(toast)(...args: unknown[]): unknown;
}

export interface PkTocElement extends HTMLElement {
    /** The element whose headings are listed. */
    for: string;
    /** Which headings to list. */
    levels: string;
    /** Distance from the top a heading must reach to count as current. */
    offset: number;
    /** Scroll container (default: the window). */
    scroller: string;
    /** The title setting */
    heading: string;
    /** Accessible name */
    label: string;
    /** Re-reads the headings */
    refresh()(...args: unknown[]): unknown;
}

export interface PkToolbarElement extends HTMLElement {
    /** The lead title, in strong text. */
    heading: string;
    /** A muted note beside the title, such as a count. */
    note: string;
}

export interface PkTooltipElement extends HTMLElement {
    /** The label. */
    text: string;
    /** Preferred side; flips when it does not fit. */
    placement: string;
    /** Mouse hover delay; focus is immediate. */
    delay: number;
    /** Read-only state for CSS (:host([shown])). */
    shown: boolean;
    /** A hint card: the panel stays open while the pointer is over it, a tap on touch toggles it, and it can hold links and rich content. */
    interactive: boolean;
    /** Draws its own info button as the target (the default slot then holds text that follows it). */
    help: boolean;
    /** Underlines the wrapped value with a dotted line to show it has context. */
    enrich: boolean;
    /** A bold heading inside the panel. */
    heading: string;
    /** Accessible name of the built-in info button. */
    label: string;
    /** Opens it */
    show()(...args: unknown[]): unknown;
    /** Closes it */
    hide()(...args: unknown[]): unknown;
}

export interface PkTreeElement extends HTMLElement {
    /** Accessible name. */
    label: string;
    /** Selection mode. */
    selection: "single" | "none";
    /** The value (or label) of the selected item. */
    value: string;
    /** Visible nodes with level and parent. */
    flattenTree(nodes)(...args: unknown[]): unknown;
    /** What a key does. */
    treeKey(items, index, key)(...args: unknown[]): unknown;
    /** Next matching label. */
    typeahead(items, index, text)(...args: unknown[]): unknown;
}

export interface PkTreeItemElement extends HTMLElement {
    /** The text. */
    label: string;
    /** The value reported in events; defaults to the label. */
    value: string;
    /** Children visible. */
    expanded: boolean;
    /** The chosen item. */
    selected: boolean;
    /** Not selectable. */
    disabled: boolean;
    /** Set by the element when it has child items. */
    expandable: boolean;
    /** Open or close the branch and fire pk-toggle. */
    setExpanded(value)(...args: unknown[]): unknown;
    /** Fire pk-select. */
    select()(...args: unknown[]): unknown;
}

export interface PkWorkspaceElement extends HTMLElement {
    /** Fill the height the parent gives it (a parent with a definite height, or a flex or grid parent) and drop the frame, for a tool that owns the whole page. Without it the workspace is as tall as the viewport minus a reserve. */
    fill: boolean;
    /** The pane shown on a phone. On a wider screen every pane shows. A pane that does not exist falls back to the main pane. */
    activePane: "nav" | "main" | "aside";
    /** Show the aside: a docked column on a wide screen, a panel over the main pane on a tablet and its own pane on a phone. */
    asideOpen: boolean;
    /** The nav pane's accessible name and its tab on a phone. */
    navLabel: string;
    /** The main pane's accessible name and its tab on a phone. */
    mainLabel: string;
    /** The aside's accessible name and its tab on a phone. */
    asideLabel: string;
}

declare global {
    interface HTMLElementTagNameMap {
        'pk-accordion': PkAccordionElement;
        'pk-accordion-item': PkAccordionItemElement;
        'pk-alert': PkAlertElement;
        'pk-app-shell': PkAppShellElement;
        'pk-avatar': PkAvatarElement;
        'pk-avatar-group': PkAvatarGroupElement;
        'pk-back-to-top': PkBackToTopElement;
        'pk-badge': PkBadgeElement;
        'pk-breadcrumb': PkBreadcrumbElement;
        'pk-button': PkButtonElement;
        'pk-button-group': PkButtonGroupElement;
        'pk-calendar': PkCalendarElement;
        'pk-card': PkCardElement;
        'pk-chart': PkChartElement;
        'pk-checkbox': PkCheckboxElement;
        'pk-cluster': PkClusterElement;
        'pk-code-block': PkCodeBlockElement;
        'pk-colour-input': PkColourInputElement;
        'pk-combobox': PkComboboxElement;
        'pk-command-palette': PkCommandPaletteElement;
        'pk-context-menu': PkContextMenuElement;
        'pk-dialog': PkDialogElement;
        'pk-divider': PkDividerElement;
        'pk-drawer': PkDrawerElement;
        'pk-dropdown': PkDropdownElement;
        'pk-dropzone': PkDropzoneElement;
        'pk-empty-state': PkEmptyStateElement;
        'pk-field': PkFieldElement;
        'pk-field-list': PkFieldListElement;
        'pk-field-row': PkFieldRowElement;
        'pk-form': PkFormElement;
        'pk-form-actions': PkFormActionsElement;
        'pk-form-section': PkFormSectionElement;
        'pk-gallery': PkGalleryElement;
        'pk-grid': PkGridElement;
        'pk-hint': PkHintElement;
        'pk-icon': PkIconElement;
        'pk-image-gallery': PkImageGalleryElement;
        'pk-input': PkInputElement;
        'pk-lightbox': PkLightboxElement;
        'pk-list-group': PkListGroupElement;
        'pk-loading-overlay': PkLoadingOverlayElement;
        'pk-local-time': PkLocalTimeElement;
        'pk-media': PkMediaElement;
        'pk-menu-item': PkMenuItemElement;
        'pk-nav-item': PkNavItemElement;
        'pk-navbar': PkNavbarElement;
        'pk-otp-input': PkOtpInputElement;
        'pk-page-header': PkPageHeaderElement;
        'pk-pager': PkPagerElement;
        'pk-pagination': PkPaginationElement;
        'pk-popover': PkPopoverElement;
        'pk-progress': PkProgressElement;
        'pk-radio-group': PkRadioGroupElement;
        'pk-range': PkRangeElement;
        'pk-rating': PkRatingElement;
        'pk-scroll-progress': PkScrollProgressElement;
        'pk-select': PkSelectElement;
        'pk-select-menu': PkSelectMenuElement;
        'pk-side-nav': PkSideNavElement;
        'pk-skeleton': PkSkeletonElement;
        'pk-skip-link': PkSkipLinkElement;
        'pk-spinner': PkSpinnerElement;
        'pk-split-button': PkSplitButtonElement;
        'pk-stack': PkStackElement;
        'pk-stat': PkStatElement;
        'pk-step': PkStepElement;
        'pk-stepper': PkStepperElement;
        'pk-switch': PkSwitchElement;
        'pk-tab': PkTabElement;
        'pk-tab-panel': PkTabPanelElement;
        'pk-table': PkTableElement;
        'pk-tabs': PkTabsElement;
        'pk-tag': PkTagElement;
        'pk-tag-input': PkTagInputElement;
        'pk-textarea': PkTextareaElement;
        'pk-timeline': PkTimelineElement;
        'pk-timeline-item': PkTimelineItemElement;
        'pk-toast': PkToastElement;
        'pk-toast-stack': PkToastStackElement;
        'pk-toc': PkTocElement;
        'pk-toolbar': PkToolbarElement;
        'pk-tooltip': PkTooltipElement;
        'pk-tree': PkTreeElement;
        'pk-tree-item': PkTreeItemElement;
        'pk-workspace': PkWorkspaceElement;
    }
    interface HTMLElementEventMap {
        'pk-toggle': CustomEvent<unknown>;
        'pk-dismiss': CustomEvent<unknown>;
        'pk-nav-toggle': CustomEvent<unknown>;
        'pk-expand': CustomEvent<unknown>;
        'pk-select': CustomEvent<unknown>;
        'pk-month': CustomEvent<unknown>;
        'pk-change': CustomEvent<{ checked: boolean: unknown }>;
        'pk-copy': CustomEvent<unknown>;
        'pk-colour': CustomEvent<{ value: string: unknown }>;
        'pk-combo-select': CustomEvent<{ value: string: unknown; label: string: unknown }>;
        'pk-combo-query': CustomEvent<{ query: string: unknown }>;
        'pk-open': CustomEvent<unknown>;
        'pk-close': CustomEvent<unknown>;
        'pk-files': CustomEvent<{ count: number: unknown; rejectedCount: number: unknown; files: File[]: unknown; rejected:  file: File: unknown; reason: string []: unknown }>;
        'pk-invalid': CustomEvent<{ count: number: unknown; controls: Element[]: unknown; messages: string[]: unknown }>;
        'pk-valid': CustomEvent<unknown>;
        'pk-primary-change': CustomEvent<unknown>;
        'pk-remove': CustomEvent<unknown>;
        'pk-add': CustomEvent<unknown>;
        'pk-value-change': CustomEvent<{ value: string: unknown }>;
        'pk-search': CustomEvent<{ value: string: unknown }>;
        'pk-otp-change': CustomEvent<{ value: string: unknown }>;
        'pk-otp-complete': CustomEvent<{ value: string: unknown }>;
        'pk-page': CustomEvent<unknown>;
        'pk-page-size': CustomEvent<unknown>;
        'pk-load-more': CustomEvent<unknown>;
        'pk-confirm': CustomEvent<unknown>;
        'pk-range': CustomEvent<{ value: number: unknown; low: number: unknown; high: number: unknown }>;
        'pk-rating-change': CustomEvent<{ value: number: unknown }>;
        'pk-split-select': CustomEvent<{ value: string: unknown; label: string: unknown }>;
        'pk-menu-toggle': CustomEvent<{ open: boolean: unknown }>;
        'pk-activate': CustomEvent<unknown>;
        'pk-step-change': CustomEvent<unknown>;
        'pk-tab-close': CustomEvent<{ value: unknown }>;
        'pk-sort': CustomEvent<unknown>;
        'pk-filter': CustomEvent<unknown>;
        'pk-row-click': CustomEvent<unknown>;
        'pk-tab-change': CustomEvent<{ value: unknown; previous: unknown }>;
        'pk-tags-change': CustomEvent<{ value: string: unknown; tags: string[]: unknown }>;
        'pk-section-change': CustomEvent<unknown>;
        'pk-show': CustomEvent<unknown>;
        'pk-hide': CustomEvent<unknown>;
        'pk-pane-change': CustomEvent<unknown>;
    }
    namespace JSX {
        interface IntrinsicElements {
            'pk-accordion': PkJsx<PkAccordionElement, "exclusive" | "flush">;
            'pk-accordion-item': PkJsx<PkAccordionItemElement, "heading" | "open">;
            'pk-alert': PkJsx<PkAlertElement, "kind" | "heading" | "dismissible" | "banner" | "plain" | "inline" | "compact">;
            'pk-app-shell': PkJsx<PkAppShellElement, "navOpen">;
            'pk-avatar': PkJsx<PkAvatarElement, "name" | "src" | "size" | "shape" | "status" | "colour">;
            'pk-avatar-group': PkJsx<PkAvatarGroupElement, "max" | "label">;
            'pk-back-to-top': PkJsx<PkBackToTopElement, "for" | "threshold" | "label" | "visible">;
            'pk-badge': PkJsx<PkBadgeElement, "variant" | "count" | "max" | "dot">;
            'pk-breadcrumb': PkJsx<PkBreadcrumbElement, "label" | "max">;
            'pk-button': PkJsx<PkButtonElement, "variant" | "size" | "type" | "disabled" | "label" | "busy" | "busyText" | "block" | "icon" | "toggle" | "pressed" | "value">;
            'pk-button-group': PkJsx<PkButtonGroupElement, "orientation" | "fill" | "label" | "mode">;
            'pk-calendar': PkJsx<PkCalendarElement, "value" | "month" | "min" | "max" | "weekStart" | "locale" | "marks" | "readonly" | "disabled">;
            'pk-card': PkJsx<PkCardElement, "heading" | "level" | "tone" | "orientation" | "flush" | "href" | "fill">;
            'pk-chart': PkJsx<PkChartElement, "kind" | "caption" | "height" | "data" | "dataLabel">;
            'pk-checkbox': PkJsx<PkCheckboxElement, "name" | "value" | "checked" | "indeterminate" | "label" | "description" | "disabled" | "required" | "invalid" | "group" | "master" | "size">;
            'pk-cluster': PkJsx<PkClusterElement, "direction" | "gap" | "align" | "justify" | "nowrap">;
            'pk-code-block': PkJsx<PkCodeBlockElement, "label" | "lineNumbers" | "wrap" | "noCopy" | "maxHeight">;
            'pk-colour-input': PkJsx<PkColourInputElement, "name" | "value" | "label" | "description" | "disabled" | "required" | "invalid">;
            'pk-combobox': PkJsx<PkComboboxElement, "mode" | "name" | "value" | "placeholder" | "label" | "description" | "disabled" | "required" | "invalid" | "free" | "filtering" | "open">;
            'pk-command-palette': PkJsx<PkCommandPaletteElement, "open" | "placeholder" | "recentsKey" | "noShortcut" | "label">;
            'pk-context-menu': PkJsx<PkContextMenuElement, "open" | "disabled">;
            'pk-dialog': PkJsx<PkDialogElement, "open" | "heading" | "size" | "scrollable" | "hideClose" | "maxWidth" | "tint" | "controlled">;
            'pk-divider': PkJsx<PkDividerElement, "vertical" | "labelPosition" | "decorative">;
            'pk-drawer': PkJsx<PkDrawerElement, "open" | "side" | "heading" | "persistent" | "wide" | "controlled" | "docked">;
            'pk-dropdown': PkJsx<PkDropdownElement, "open" | "placement" | "keepOpen" | "mega">;
            'pk-dropzone': PkJsx<PkDropzoneElement, "name" | "accept" | "multiple" | "maxSize" | "maxFiles" | "label" | "description" | "disabled" | "required" | "invalid">;
            'pk-empty-state': PkJsx<PkEmptyStateElement, "heading" | "level" | "description" | "tone" | "announce">;
            'pk-field': PkJsx<PkFieldElement, "label" | "help" | "error" | "warning" | "required" | "max" | "layout" | "size">;
            'pk-field-list': PkJsx<PkFieldListElement, "heading" | "level" | "layout" | "dividers" | "dense">;
            'pk-field-row': PkJsx<PkFieldRowElement, "align">;
            'pk-form': PkJsx<PkFormElement, "validate" | "showValid" | "summary">;
            'pk-form-actions': PkJsx<PkFormActionsElement, "sticky" | "align">;
            'pk-form-section': PkJsx<PkFormSectionElement, "heading" | "description">;
            'pk-gallery': PkJsx<PkGalleryElement, "kind" | "group" | "control" | "theme" | "width" | "filter" | "chrome" | "height" | "src">;
            'pk-grid': PkJsx<PkGridElement, "min" | "columns" | "gap">;
            'pk-hint': PkJsx<PkHintElement, "label" | "open" | "tone">;
            'pk-icon': PkJsx<PkIconElement, "name" | "size" | "label">;
            'pk-image-gallery': PkJsx<PkImageGalleryElement, "images" | "primary" | "columns" | "min" | "editable" | "addLabel" | "accept">;
            'pk-input': PkJsx<PkInputElement, "type" | "name" | "value" | "placeholder" | "label" | "description" | "disabled" | "readonly" | "required" | "invalid" | "warning" | "valid" | "min" | "max" | "step" | "minlength" | "maxlength" | "pattern" | "autocomplete" | "inputmode" | "size" | "clearable" | "reveal" | "stepper" | "format" | "decimals" | "currency" | "locale" | "floating" | "autohide" | "debounce">;
            'pk-lightbox': PkJsx<PkLightboxElement, "index" | "label" | "open">;
            'pk-list-group': PkJsx<PkListGroupElement, "variant" | "checklist" | "label">;
            'pk-loading-overlay': PkJsx<PkLoadingOverlayElement, "busy" | "label" | "fullscreen">;
            'pk-local-time': PkJsx<PkLocalTimeElement, "datetime" | "format" | "length" | "locale" | "timeZone">;
            'pk-media': PkJsx<PkMediaElement, "ratio" | "fit" | "caption" | "lightbox" | "square">;
            'pk-menu-item': PkJsx<PkMenuItemElement, "type" | "checked" | "disabled" | "danger" | "value" | "href" | "open">;
            'pk-nav-item': PkJsx<PkNavItemElement, "href" | "current" | "disabled" | "expanded" | "rail" | "flyout">;
            'pk-navbar': PkJsx<PkNavbarElement, "label" | "open" | "sticky">;
            'pk-otp-input': PkJsx<PkOtpInputElement, "name" | "value" | "label" | "description" | "disabled" | "required" | "invalid" | "length" | "type" | "separatorAt">;
            'pk-page-header': PkJsx<PkPageHeaderElement, "heading" | "level" | "variant">;
            'pk-pager': PkJsx<PkPagerElement, "label">;
            'pk-pagination': PkJsx<PkPaginationElement, "page" | "pages" | "total" | "pageSize" | "sizes" | "siblings" | "boundary" | "mode" | "label" | "loading" | "edges">;
            'pk-popover': PkJsx<PkPopoverElement, "open" | "placement" | "trigger" | "heading" | "label" | "variant" | "message" | "confirmLabel" | "cancelLabel" | "danger">;
            'pk-progress': PkJsx<PkProgressElement, "value" | "max" | "variant" | "size" | "striped" | "indeterminate" | "label" | "showValue" | "levelThresholds">;
            'pk-radio-group': PkJsx<PkRadioGroupElement, "name" | "value" | "label" | "description" | "disabled" | "required" | "invalid" | "variant" | "direction" | "fill">;
            'pk-range': PkJsx<PkRangeElement, "name" | "min" | "max" | "step" | "value" | "valueLow" | "valueHigh" | "dual" | "label" | "description" | "disabled" | "output">;
            'pk-rating': PkJsx<PkRatingElement, "name" | "value" | "max" | "readonly" | "label" | "disabled">;
            'pk-scroll-progress': PkJsx<PkScrollProgressElement, "for">;
            'pk-select': PkJsx<PkSelectElement, "name" | "value" | "label" | "description" | "disabled" | "required" | "invalid" | "warning" | "valid" | "multiple">;
            'pk-select-menu': PkJsx<PkSelectMenuElement, "value" | "name" | "placeholder" | "open" | "disabled" | "required">;
            'pk-side-nav': PkJsx<PkSideNavElement, "label" | "collapsed" | "open" | "filterable" | "persist">;
            'pk-skeleton': PkJsx<PkSkeletonElement, "variant" | "lines" | "size" | "label">;
            'pk-skip-link': PkJsx<PkSkipLinkElement, "href">;
            'pk-spinner': PkJsx<PkSpinnerElement, "variant" | "size" | "label" | "overlay">;
            'pk-split-button': PkJsx<PkSplitButtonElement, "variant" | "type" | "disabled" | "toggleLabel" | "open" | "menuAlign">;
            'pk-stack': PkJsx<PkStackElement, "gap" | "align" | "dividers">;
            'pk-stat': PkJsx<PkStatElement, "label" | "value" | "subtext" | "tone" | "delta" | "deltaDirection" | "invert" | "versus" | "values" | "href" | "tile" | "interactive">;
            'pk-step': PkJsx<PkStepElement, "heading" | "description" | "state" | "index" | "disabled" | "last" | "clickable" | "orientation">;
            'pk-stepper': PkJsx<PkStepperElement, "current" | "orientation" | "clickable" | "free" | "errors" | "label">;
            'pk-switch': PkJsx<PkSwitchElement, "checked" | "disabled" | "name" | "value" | "invalid" | "size" | "labelPosition">;
            'pk-tab': PkJsx<PkTabElement, "value" | "selected" | "disabled" | "count" | "closable" | "only">;
            'pk-tab-panel': PkJsx<PkTabPanelElement, "value" | "selected">;
            'pk-table': PkJsx<PkTableElement, "columns" | "rows" | "rowKey" | "striped" | "hover" | "bordered" | "density" | "stickyHeader" | "stickyColumn" | "selectable" | "clickable" | "filterable" | "manual" | "sort" | "sortDir" | "filters" | "selected" | "loading" | "cards" | "caption" | "label" | "maxHeight" | "flow">;
            'pk-tabs': PkJsx<PkTabsElement, "value" | "activation" | "noneActive" | "scroll">;
            'pk-tag': PkJsx<PkTagElement, "removable" | "value" | "disabled">;
            'pk-tag-input': PkJsx<PkTagInputElement, "name" | "value" | "placeholder" | "label" | "description" | "disabled" | "required" | "invalid" | "separators" | "max" | "allowDuplicates">;
            'pk-textarea': PkJsx<PkTextareaElement, "name" | "value" | "placeholder" | "label" | "description" | "disabled" | "readonly" | "required" | "invalid" | "warning" | "valid" | "rows" | "minlength" | "maxlength" | "autogrow" | "maxHeight">;
            'pk-timeline': PkJsx<PkTimelineElement, "label">;
            'pk-timeline-item': PkJsx<PkTimelineItemElement, "heading" | "time" | "status">;
            'pk-toast': PkJsx<PkToastElement, "kind" | "heading" | "message" | "duration" | "noClose">;
            'pk-toast-stack': PkJsx<PkToastStackElement, "position" | "max">;
            'pk-toc': PkJsx<PkTocElement, "for" | "levels" | "offset" | "scroller" | "heading" | "label">;
            'pk-toolbar': PkJsx<PkToolbarElement, "heading" | "note">;
            'pk-tooltip': PkJsx<PkTooltipElement, "text" | "placement" | "delay" | "shown" | "interactive" | "help" | "enrich" | "heading" | "label">;
            'pk-tree': PkJsx<PkTreeElement, "label" | "selection" | "value">;
            'pk-tree-item': PkJsx<PkTreeItemElement, "label" | "value" | "expanded" | "selected" | "disabled" | "expandable">;
            'pk-workspace': PkJsx<PkWorkspaceElement, "fill" | "activePane" | "asideOpen" | "navLabel" | "mainLabel" | "asideLabel">;
        }
    }
}

/** Props (as attributes or properties), slot, class and children of an pk-* element in JSX. */
export type PkJsx<T, K extends keyof T> = Partial<Pick<T, K>> & { slot?: string; class?: string; className?: string; id?: string; children?: unknown; ref?: unknown; [event: `on${string}`]: unknown };
