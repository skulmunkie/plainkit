# Components: Navigation

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from Generated/*.razor, blazor/mappings, generated.manifest.json and core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every component renders its element with the parameters you set as attributes. An attribute that is not a parameter (`id`, `class`, `data-*`, `aria-*`, ...) is put on the element as it is, and a `class` is added to the component's own (`ExtraClass` on a component that lists it). Names are `Pk` plus the tag in PascalCase.

## `PkBackToTop`

`<pk-back-to-top>`: **Back to top** (Navigation). A floating button that appears once the reader is well down the page and scrolls the page (or a named scroller) back to the top, smoothly unless reduced motion is on.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `For` | `string?` |  | attribute `for` | Scroll container (default: the window). |
| `Threshold` | `int` |  | attribute `threshold` | The threshold setting |
| `Label` | `string?` |  | attribute `label` | Accessible name |
| `Visible` | `bool` |  | attribute `visible` | Set by the element. |
| `ChildContent` | `RenderFragment?` |  | default slot | Label. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-back-to-top` in the `plainkit-sdk` skill.

## `PkBreadcrumb`

`<pk-breadcrumb>`: **Breadcrumb** (Navigation). The path to the current page as a trail of links with chevrons; the middle collapses behind a "..." button when the trail is long, sooner on a phone.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Landmark name. |
| `Max` | `int` |  | attribute `max` | Crumbs shown before the middle folds (3 on a phone). |
| `ChildContent` | `RenderFragment?` |  | default slot | a elements; the last one is the current page. |
| `OnExpand` | `EventCallback` |  | event `pk-expand` | The fold button revealed the hidden crumbs. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-breadcrumb` in the `plainkit-sdk` skill.

## `PkNavItem`

`<pk-nav-item>`: **Nav item** (Navigation). A row of a side nav: a link, or a branch that folds its children; icon, badge, active bar and disabled state.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Href` | `string?` |  | attribute `href` | Makes it a link; omit for a branch. |
| `Current` | `bool` |  | attribute `current` | The current setting |
| `Disabled` | `bool` |  | attribute `disabled` | Prevents interaction |
| `Expanded` | `bool` |  | attribute `expanded` | Branch open. |
| `ExpandedChanged` | `EventCallback<bool>` |  | two-way pair of `Expanded` | Raised when Expanded changes (two-way binding: `@bind-Expanded`). |
| `Rail` | `bool` |  | attribute `rail` | Set by the nav in icon-rail mode. |
| `Flyout` | `bool` |  | attribute `flyout` | In the icon rail, the branch is open as a flyout beside the rail. |
| `Group` | `bool` |  | attribute `group` | A group title instead of a row: static muted text that introduces the rows below it. Not focusable, skipped by the arrow keys and hidden while the filter is used; in the icon rail it shows as a divider. |
| `ChildContent` | `RenderFragment?` |  | default slot | Label. |
| `IconContent` | `RenderFragment?` |  | slot `icon` | Icon. |
| `BadgeContent` | `RenderFragment?` |  | slot `badge` | Count or tag. |
| `ChildrenContent` | `RenderFragment?` |  | slot `children` | Nested pk-nav-item. |
| `OnToggle` | `EventCallback<PkToggleEventArgs>` |  | event `pk-toggle` | A branch folded or unfolded. |

Two-way binding: `@bind-Expanded`.

When you set a named fragment (`IconContent`, `BadgeContent`, `ChildrenContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-toggle` | `PkToggleEventArgs` | Open: bool?, Expanded: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-nav-item` in the `plainkit-sdk` skill.

## `PkNavbar`

`<pk-navbar>`: **Navbar** (Navigation). The top navigation: brand, links, dropdown and mega menus, right-aligned actions, sticky option, and a hamburger that folds the links below 1024px.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Accessible name |
| `Open` | `bool` |  | attribute `open` | Links unfolded (small screens). |
| `Sticky` | `bool` |  | attribute `sticky` | The sticky setting |
| `BrandContent` | `RenderFragment?` |  | slot `brand` | Brand link. |
| `ChildContent` | `RenderFragment?` |  | default slot | Links and pk-dropdown menus. |
| `ActionsContent` | `RenderFragment?` |  | slot `actions` | Right-aligned controls. |
| `OnToggle` | `EventCallback<PkToggleEventArgs>` |  | event `pk-toggle` | Hamburger toggled. |

When you set a named fragment (`BrandContent`, `ActionsContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-toggle` | `PkToggleEventArgs` | Open: bool?, Expanded: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-navbar` in the `plainkit-sdk` skill.

## `PkPager`

`<pk-pager>`: **Pager** (Navigation). Page-level previous and next links between sibling pages, with an optional centre note ("Step 2 of 5"); table pagination is a separate control.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Accessible name |
| `PrevContent` | `RenderFragment?` |  | slot `prev` | The previous-page link. |
| `NextContent` | `RenderFragment?` |  | slot `next` | The next-page link. |
| `ChildContent` | `RenderFragment?` |  | default slot | Centre note. |

When you set a named fragment (`PrevContent`, `NextContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-pager` in the `plainkit-sdk` skill.

## `PkPagination`

`<pk-pagination>`: **Pagination** (Navigation). Page navigation: numbered with ellipses, a range summary, a page-size select, a simple previous and next form, or load more. On a phone the numbers collapse to previous, Page N of M and next.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Page` | `int` |  | attribute `page` | Current page, 1-based. |
| `Pages` | `int` |  | attribute `pages` | Page count; 0 derives it from total and pageSize. |
| `Total` | `int` |  | attribute `total` | Item count; enables the range summary. |
| `PageSize` | `int` |  | attribute `page-size` | Items per page. |
| `PageSizeOptions` | `int[]?` |  | attribute `sizes` | Options for the page-size select, for example [10, 25, 50, 100]; empty hides the select. |
| `Siblings` | `int` |  | attribute `siblings` | Pages either side of the current one. |
| `Boundary` | `int` |  | attribute `boundary` | Pages always shown at each end. |
| `Mode` | `PaginationMode?` | `Numbered` `Simple` `LoadMore` | attribute `mode` | Layout. |
| `Label` | `string?` |  | attribute `label` | Accessible name of the nav. |
| `Loading` | `bool` |  | attribute `loading` | Load-more busy state. |
| `PageChanged` | `EventCallback<int>` |  | two-way pair of `Page` | Raised with the new value of Page (two-way: Page follows it). |
| `PageSizeChanged` | `EventCallback<int>` |  | two-way pair of `PageSize` | Raised with the new value of PageSize (two-way: PageSize follows it). |
| `OnLoadMore` | `EventCallback` |  | event `pk-load-more` | Load more was pressed. |
| `Edges` | `bool` |  | attribute `edges` | Show First and Last buttons beside Previous and Next. |

Two-way binding: `@bind-Page`, `@bind-PageSize`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-page` | `PkPageEventArgs` | Page: double? |
| `pk-page-size` | `PkPageSizeEventArgs` | PageSize: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-pagination` in the `plainkit-sdk` skill.

## `PkScrollProgress`

`<pk-scroll-progress>`: **Scroll progress** (Navigation). A thin bar at the top of the screen that fills as the page (or a named scroller) is read.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `For` | `string?` |  | attribute `for` | Scroll container (default: the window). |

Element details (parts, CSS custom properties, methods, accessibility): `pk-scroll-progress` in the `plainkit-sdk` skill.

## `PkSideNav`

`<pk-side-nav>`: **Side nav** (Navigation). The app’s side navigation: brand, filter box, nested groups with arrow-key tree navigation, collapse to an icon rail (tooltips on rows, branches open as flyouts), persisted state, and an off-canvas drawer with backdrop at 1024px and below.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Accessible name |
| `Collapsed` | `bool` |  | attribute `collapsed` | Icon rail. |
| `CollapsedChanged` | `EventCallback<bool>` |  | two-way pair of `Collapsed` | Raised when Collapsed changes (two-way binding: `@bind-Collapsed`). |
| `Open` | `bool` |  | attribute `open` | Drawer open (phones and tablets). |
| `OpenChanged` | `EventCallback<bool>` |  | two-way pair of `Open` | Raised when Open changes (two-way binding: `@bind-Open`). |
| `Filterable` | `bool` |  | attribute `filterable` | Show the filter box. |
| `Persist` | `string?` |  | attribute `persist` | localStorage key for collapsed and open branches. |
| `BrandContent` | `RenderFragment?` |  | slot `brand` | Brand link. |
| `ChildContent` | `RenderFragment?` |  | default slot | pk-nav-item children; a pk-nav-item with the group attribute is a group title. |
| `FooterContent` | `RenderFragment?` |  | slot `footer` | Pinned bottom items. |
| `OnToggle` | `EventCallback<bool>` |  | event `pk-nav-toggle` | Rail toggled by the user, or restored from the saved state (persist) on connect. |

Two-way binding: `@bind-Collapsed`, `@bind-Open`.

When you set a named fragment (`BrandContent`, `FooterContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-close` | `PkCloseEventArgs` | Reason: string? |
| `pk-nav-toggle` | `PkNavToggleEventArgs` | Open: bool?, Collapsed: bool? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-side-nav` in the `plainkit-sdk` skill.

## `PkSkipLink`

`<pk-skip-link>`: **Skip link** (Navigation). The first focusable element on a page: invisible until it takes focus, then a button that jumps past the navigation to the main content.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Href` | `string?` |  | attribute `href` | Same-page target. |
| `ChildContent` | `RenderFragment?` |  | default slot | Label (default "Skip to content"). |

Element details (parts, CSS custom properties, methods, accessibility): `pk-skip-link` in the `plainkit-sdk` skill.

## `PkStep`

`<pk-step>`: **Step** (Navigation). One step of a stepper: a marker (number, tick or !), a title, an optional description and the connector to the next step.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Heading` | `string?` |  | attribute `heading` | The title setting |
| `Description` | `string?` |  | attribute `description` | The description setting |
| `State` | `StepState` | `Todo` `Active` `Done` `Error` | attribute `state` | Set by the stepper. |
| `Index` | `int` |  | attribute `index` | One-based, set by the stepper. |
| `Disabled` | `bool` |  | attribute `disabled` | Prevents interaction |
| `Last` | `bool` |  | attribute `last` | Set by the stepper: the last step draws no connector. |
| `Clickable` | `bool` |  | attribute `clickable` | Set by the stepper: the step can be entered. |
| `Orientation` | `StepOrientation` | `Horizontal` `Vertical` | attribute `orientation` | Set by the stepper. |
| `ChildContent` | `RenderFragment?` |  | default slot | Title (overrides the title prop). |
| `DescriptionContent` | `RenderFragment?` |  | slot `description` | Description. |

When you set a named fragment (`DescriptionContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-step` in the `plainkit-sdk` skill.

## `PkStepper`

`<pk-stepper>`: **Stepper** (Navigation). A wizard header: numbered steps with done, current, upcoming and error states, horizontal or vertical, optionally clickable (linear: you can go back, not skip ahead).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Current` | `int` |  | attribute `current` | Zero-based current step. |
| `CurrentChanged` | `EventCallback<int>` |  | two-way pair of `Current` | Raised when Current changes (two-way binding: `@bind-Current`). |
| `Orientation` | `StepperOrientation` | `Horizontal` `Vertical` | attribute `orientation` | The orientation setting |
| `Clickable` | `bool` |  | attribute `clickable` | Steps can be entered by click or key. |
| `Free` | `bool` |  | attribute `free` | Allow skipping ahead of the furthest step reached (non-linear). |
| `Errors` | `string?` |  | attribute `errors` | Comma-separated step indexes with an error. |
| `Label` | `string?` |  | attribute `label` | Accessible name. |
| `ChildContent` | `RenderFragment?` |  | default slot | pk-step children. |
| `OnStepChange` | `EventCallback<PkStepChangeEventArgs>` |  | event `pk-step-change` | A step was entered; cancelable. |

Two-way binding: `@bind-Current`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-step-change` | `PkStepChangeEventArgs` | Index: double?, Previous: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-stepper` in the `plainkit-sdk` skill.

## `PkTab`

`<pk-tab>`: **Tab** (Navigation). One tab of an pk-tabs. Place it inside pk-tabs; it selects its panel by value. It can show a count, a close button, and appear only on a phone or only on a desktop.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The key that pairs this tab with the pk-tab-panel of the same value. |
| `Disabled` | `bool` |  | attribute `disabled` | Cannot be selected or focused by arrow keys. |
| `Count` | `string?` |  | attribute `count` | A badge shown in muted parentheses after the label (a queue count); empty shows none. |
| `Closable` | `bool` |  | attribute `closable` | Shows a close button, keyboard reachable on the selected tab; pk-tab-close is raised when it is activated and the page removes the tab. |
| `Only` | `PkTabOnly?` | `All` `Phone` `Desktop` | attribute `only` | all shows the tab everywhere; phone only at phone width (640px and below); desktop hides it at phone width. |
| `OnClose` | `EventCallback<PkTabCloseEventArgs>` |  | event `pk-tab-close` | The close button of a closable tab was clicked. |
| `ChildContent` | `RenderFragment?` |  | default slot | The tab label. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-tab-close` | `PkTabCloseEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tab` in the `plainkit-sdk` skill.

## `PkTabPanel`

`<pk-tab-panel>`: **Tab panel** (Navigation). The content of one tab; shown while its tab is selected.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The key that pairs this panel with the pk-tab of the same value. |
| `ChildContent` | `RenderFragment?` |  | default slot | The panel content. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tab-panel` in the `plainkit-sdk` skill.

## `PkTabs`

`<pk-tabs>`: **Tabs** (Navigation). A tab strip with optional panels. Children: pk-tab and pk-tab-panel paired by value. Arrow keys, Home and End move between tabs (roving tabindex). It can show no tab as selected, slide sideways with edge fades, and hold trailing content after the last tab.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The value of the selected tab. Empty selects the first enabled tab. |
| `Activation` | `PkTabsActivation?` | `Auto` `Manual` | attribute `activation` | auto selects a tab when arrow keys focus it; manual moves focus and waits for Enter or Space. |
| `NoneActive` | `bool` |  | attribute `none-active` | No tab is shown as selected, and choosing any tab (even the last one chosen) raises pk-tab-change. For a page whose cross-cutting view sits above its tabs. |
| `Scroll` | `bool` |  | attribute `scroll` | One row that slides (touch swipe, wheel, drag) when the tabs outgrow the width, with a fade at the edge that has more tabs, instead of wrapping onto more rows. |
| `OnChange` | `EventCallback<PkTabChangeEventArgs>` |  | event `pk-tab-change` | A tab was chosen. Cancelable: preventDefault keeps the previous tab. Also raised, not cancelable, when the value falls back to the first enabled tab (empty or no matching tab), so the host learns the value the element settled on. |
| `TrailingContent` | `RenderFragment?` |  | slot `trailing` | The content. The pk-tab and pk-tab-panel children assign themselves to the tab and panel slots; ChildContent holds them. [Parameter] public RenderFragment? ChildContent { get; set; } Content inside the strip after the last tab: a hint, links or buttons. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-tab-change` | `PkTabChangeEventArgs` | Value: string?, Previous: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tabs` in the `plainkit-sdk` skill.

## `PkToc`

`<pk-toc>`: **On this page** (Navigation). An anchor list of the headings inside a target element with scroll-spy: the heading in view is marked as you read; works with the window or an inner scroller such as the shell body.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `For` | `string?` |  | attribute `for` | The element whose headings are listed. |
| `Levels` | `string?` |  | attribute `levels` | Which headings to list. |
| `Offset` | `int` |  | attribute `offset` | Distance from the top a heading must reach to count as current. |
| `Scroller` | `string?` |  | attribute `scroller` | Scroll container (default: the window). |
| `Heading` | `string?` |  | attribute `heading` | The title setting |
| `Label` | `string?` |  | attribute `label` | Accessible name |
| `OnSectionChange` | `EventCallback<PkSectionChangeEventArgs>` |  | event `pk-section-change` | The current heading changed. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-section-change` | `PkSectionChangeEventArgs` | Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-toc` in the `plainkit-sdk` skill.

## `PkTree`

`<pk-tree>`: **Tree** (Navigation). An expandable hierarchy of tree items with roving focus, single selection and type-ahead, following the WAI-ARIA tree pattern.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Accessible name. |
| `Selection` | `string?` |  | attribute `selection` | Selection mode. |
| `Value` | `string?` |  | attribute `value` | The value (or label) of the selected item. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised with the new value of Value (two-way: Value follows it). |
| `OnToggle` | `EventCallback<PkToggleEventArgs>` |  | event `pk-toggle` | A branch opened or closed (bubbles from the item). |
| `ChildContent` | `RenderFragment?` |  | default slot | Root pk-tree-item children. |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-select` | `PkSelectEventArgs` | Value: string?, Item: JsonElement?, Checked: bool?, Id: string? |
| `pk-toggle` | `PkToggleEventArgs` | Open: bool?, Expanded: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tree` in the `plainkit-sdk` skill.

## `PkTreeItem`

`<pk-tree-item>`: **Tree item** (Navigation). One node of a tree: a toggle, a label and nested child items.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | The text. |
| `Value` | `string?` |  | attribute `value` | The value reported in events; defaults to the label. |
| `Expanded` | `bool` |  | attribute `expanded` | Children visible. |
| `Selected` | `bool` |  | attribute `selected` | The chosen item. |
| `Disabled` | `bool` |  | attribute `disabled` | Not selectable. |
| `Expandable` | `bool` |  | attribute `expandable` | Set by the element when it has child items. |
| `ChildContent` | `RenderFragment?` |  | default slot | Child pk-tree-item elements. |
| `LabelContent` | `RenderFragment?` |  | slot `label` | A rich label; overrides label. |
| `IconContent` | `RenderFragment?` |  | slot `icon` | An icon before the label. |
| `OnSelect` | `EventCallback<PkSelectEventArgs>` |  | event `pk-select` | The row was activated. |
| `OnToggle` | `EventCallback<PkToggleEventArgs>` |  | event `pk-toggle` | The branch opened or closed. |

When you set a named fragment (`LabelContent`, `IconContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-select` | `PkSelectEventArgs` | Value: string?, Item: JsonElement?, Checked: bool?, Id: string? |
| `pk-toggle` | `PkToggleEventArgs` | Open: bool?, Expanded: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tree-item` in the `plainkit-sdk` skill.
