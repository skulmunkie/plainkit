# Components: Data display, media and code

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from Generated/*.razor, blazor/mappings, generated.manifest.json and core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every component renders its element with the parameters you set as attributes. An attribute that is not a parameter (`id`, `class`, `data-*`, `aria-*`, ...) is put on the element as it is, and a `class` is added to the component's own (`ExtraClass` on a component that lists it). Names are `Pk` plus the tag in PascalCase.

## `PkAvatar`

`<pk-avatar>`: **Avatar** (Data display). A person or entity as an image or initials, with sizes, shapes, a stable colour per name and an online, away or busy status dot.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Name` | `string?` |  | attribute `name` | The accessible name and the source of the initials. |
| `Src` | `string?` |  | attribute `src` | An image URL; initials show until it loads or if it fails. |
| `Size` | `string?` |  | attribute `size` | Size. |
| `Shape` | `string?` |  | attribute `shape` | Shape. |
| `Status` | `string?` |  | attribute `status` | Status dot. |
| `Colour` | `string?` |  | attribute `colour` | A colour slot 1 to 5, or muted; by default a stable slot chosen from the name. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-avatar` in the `plainkit-sdk` skill.

## `PkAvatarGroup`

`<pk-avatar-group>`: **AvatarGroup** (Data display). Overlapping avatars with a +N tile for the overflow.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Max` | `double` |  | attribute `max` | Avatars shown before the +N tile. |
| `Label` | `string?` |  | attribute `label` | Accessible name of the group. |
| `ChildContent` | `RenderFragment?` |  | default slot | pk-avatar children. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-avatar-group` in the `plainkit-sdk` skill.

## `PkBadge`

`<pk-badge>`: **Badge** (Data display). A count or short label in a pill, or a status dot with a label; counts cap at a maximum (99+).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Variant` | `string?` |  | attribute `variant` | Colour. |
| `Count` | `string?` |  | attribute `count` | Value to show when the slot is empty, capped by max. |
| `Max` | `double` |  | attribute `max` | Counts above read as max+; 0 disables capping. |
| `Dot` | `bool` |  | attribute `dot` | A status dot with the text as its label instead of a pill. |
| `ChildContent` | `RenderFragment?` |  | default slot | Text; overrides the count. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-badge` in the `plainkit-sdk` skill.

## `PkChart`

`<pk-chart>`: **Chart** (Data display). Bar, line, donut, stacked-bar and sparkline charts in pure SVG, drawn from a slotted data table (which stays as the accessible fallback) or from a data property.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Kind` | `string?` |  | attribute `kind` | Chart type. |
| `Caption` | `string?` |  | attribute `caption` | Title (figcaption). |
| `Height` | `int` |  | attribute `height` | viewBox height of bar and line charts. |
| `Data` | `object?` |  | attribute `data` | Data as a property instead of a table: { labels: string[], series: { name, values: number[] }[] }. |
| `CaptionContent` | `RenderFragment?` |  | slot `caption` | A rich caption; overrides caption. |
| `ChildContent` | `RenderFragment?` |  | default slot | A table: the first column holds labels, each further column a series. A cell may carry data-value. |

When you set a named fragment (`CaptionContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Type not defined yet**

| Parameter | Why |
|---|---|
| `Data` | type not yet defined in PlainKit.Blazor (issue #9): ChartData |

Element details (parts, CSS custom properties, methods, accessibility): `pk-chart` in the `plainkit-sdk` skill.

## `PkCodeBlock`

`<pk-code-block>`: **Code block** (Code). A block of code with an optional label bar, line numbers, a wrap toggle and a copy button. The code is the element's text content.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | File or language shown in the bar; also the region's name. |
| `LineNumbers` | `bool` |  | attribute `line-numbers` | A gutter of line numbers. |
| `Wrap` | `bool` |  | attribute `wrap` | Wrap long lines. |
| `NoCopy` | `bool` |  | attribute `no-copy` | Hide the copy button. |
| `MaxHeight` | `string?` |  | attribute `max-height` | Cap the height, any CSS length. |
| `OnCopy` | `EventCallback<PkCopyEventArgs>` |  | event `pk-copy` | After a copy attempt. |
| `ChildContent` | `string?` |  | text content | The content. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-copy` | `PkCopyEventArgs` | Ok: bool? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-code-block` in the `plainkit-sdk` skill.

## `PkFieldList`

`<pk-field-list>`: **Field list** (Data display). A titled description list: term and value pairs as slotted dt and dd, beside each other, stacked or run in; with dividers and a dense mode.

Hand-written component (not generated).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Title` | `string?` |  | attribute `heading` | The list's title (the element's `heading`). |
| `Layout` | `string?` |  | attribute `layout` | The layout: `grid` (term beside value) or `stacked`. Left off, the element's own default applies. |
| `Dividers` | `bool` |  | attribute `dividers` | Draws a rule between the pairs. |
| `Dense` | `bool` |  | attribute `dense` | Tighter spacing. |
| `ChildContent` | `RenderFragment?` |  | default slot | The term and value pairs: slotted `dt` and `dd` elements. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-field-list` in the `plainkit-sdk` skill.

## `PkGallery`

`<pk-gallery>`: **Gallery** (Data display). The SDK gallery inside your page: every control, element, layout and template with live samples, narrowed by attributes to just what you want to show. Loads on demand in its own frame.

Hand-written component (not generated).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Kind` | `PkGalleryKind` | `All` `Foundations` `Controls` `Elements` `Layouts` `Templates` | attribute `kind` | What to show. All shows the controls when a group or control is named, otherwise everything. |
| `Group` | `string?` |  | attribute `group` | One control group by name ("Forms & inputs") or slug. |
| `Control` | `string?` |  | attribute `control` | One control, element or sample id, or a comma separated list ("button,input"). |
| `Theme` | `PkTheme` | `Auto` `Dark` `Light` | attribute `theme` | The theme of the gallery and its samples. |
| `Width` | `PkWidth` | `Desktop` `Phone` | attribute `width` | Render samples at desktop width or in a 375px phone frame. |
| `Filter` | `string?` |  | attribute `filter` | Search text: only controls whose name (or group) contains it are shown. |
| `Chrome` | `PkChrome` | `None` `Full` | attribute `chrome` | None (the default) is content only, sized to fit; Full adds the nav, toolbar and inspector. |
| `Height` | `int` |  | attribute `height` | A fixed height in pixels; 0 fits the content (chrome none) or uses the default (chrome full). |
| `Src` | `string?` |  | attribute `src` | The address of the gallery's embed page. Defaults to the one shipped with PlainKit.Blazor. |
| `AdditionalAttributes` | `Dictionary<string, object>?` |  |  | Extra attributes for the element. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-gallery` in the `plainkit-sdk` skill.

## `PkIcon`

`<pk-icon>`: **Icon** (Media). A symbol from the SDK sprite, drawn in the surrounding text colour and size. The sprite is found next to the element module, so it works wherever dist is copied.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Name` | `string?` |  | attribute `name` | The symbol id in the sprite (search, settings, orders, ...). |
| `Size` | `string?` |  | attribute `size` | md is 1.25em and follows the text; sm, lg and xl are fixed steps of the space scale. |
| `Label` | `string?` |  | attribute `label` | The accessible name. Without it the icon is decorative and hidden from assistive technology. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-icon` in the `plainkit-sdk` skill.

## `PkImageGallery`

`<pk-image-gallery>`: **Image gallery** (Data display). A grid of square thumbnails with one marked primary (a badge and an accent frame). Clicking a thumbnail opens the lightbox. Editable, it adds a make-primary and a remove button under each image and an add tile that picks files.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Images` | `object?` |  | attribute `images` | The images: [{ src, alt, primary?, status? }]. status is a short warning badge such as Staged. Property or a JSON attribute. Only same-site paths, http(s) and raster data URLs are shown. |
| `Primary` | `int` |  | attribute `primary` | The index of the primary image; -1 uses the first image flagged primary in images, or none. |
| `PrimaryChanged` | `EventCallback<int>` |  | two-way pair of `Primary` | Raised when Primary changes (two-way binding: `@bind-Primary`). |
| `Columns` | `int` |  | attribute `columns` | A fixed number of equal columns (1 to 12). 0 fits as many as min allows. |
| `Min` | `string?` |  | attribute `min` | The narrowest a thumbnail may get, as a css length in px, rem or em. Used when columns is 0. |
| `Editable` | `bool` |  | attribute `editable` | Show make-primary and remove buttons under each image and the add tile. |
| `AddLabel` | `string?` |  | attribute `add-label` | Text on the add tile. |
| `Accept` | `string?` |  | attribute `accept` | The file types the add tile's picker offers. |
| `OnPrimaryChange` | `EventCallback<PkPrimaryChangeEventArgs>` |  | event `pk-primary-change` | Make primary was pressed. Cancelable: preventDefault keeps the current primary (a controlled gallery updates images and primary itself). |
| `OnRemove` | `EventCallback<PkRemoveEventArgs>` |  | event `pk-remove` | Remove was pressed. Cancelable: preventDefault keeps the image. Otherwise it is taken out of images, and if it was the primary the first image becomes the primary. |
| `OnAdd` | `EventCallback<PkAddEventArgs>` |  | event `pk-add` | Files were chosen on the add tile. The gallery adds nothing itself: read the files (a data URL) and set images. |

Two-way binding: `@bind-Primary`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-add` | `PkAddEventArgs` | Files: PkFileInfo[]?, Names: string[]? |
| `pk-primary-change` | `PkPrimaryChangeEventArgs` | Index: double?, Src: string?, Previous: double? |
| `pk-remove` | `PkRemoveEventArgs` | Index: double?, Src: string?, Value: string? |

**Type not defined yet**

| Parameter | Why |
|---|---|
| `Images` | type not yet defined in PlainKit.Blazor (issue #9): GalleryImage |

Element details (parts, CSS custom properties, methods, accessibility): `pk-image-gallery` in the `plainkit-sdk` skill.

## `PkListGroup`

`<pk-list-group>`: **List group** (Data display). A bordered list of rows: plain, actionable (links and buttons), with badges at the end, or a checklist.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Variant` | `string?` |  | attribute `variant` | Row behaviour and frame. |
| `Checklist` | `bool` |  | attribute `checklist` | Rows are labels with a checkbox. |
| `Label` | `string?` |  | attribute `label` | Accessible name of the list. |
| `ChildContent` | `RenderFragment?` |  | default slot | Rows: div, a, button or label elements. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-list-group` in the `plainkit-sdk` skill.

## `PkLocalTime`

`<pk-local-time>`: **Local time** (Data display). A UTC instant written in the reader's own locale and time zone, as a date, a time, both, or relative to now (3 hours ago). Keeps a real time element inside, so the machine value stays readable.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `datetime` | The instant: an ISO 8601 string (2026-09-19T14:30:00Z). A date-only value is a calendar day and is never shifted by the time zone. |
| `Format` | `string?` |  | attribute `format` | What to show. relative reads 3 hours ago and refreshes every 30 seconds while connected; its title holds the full date. |
| `Length` | `string?` |  | attribute `length` | How much of the date to spell out (9/19/26, Sep 19, 2026, September 19, 2026). |
| `Locale` | `string?` |  | attribute `locale` | A BCP 47 tag such as de-DE; empty uses the reader's browser locale. |
| `TimeZone` | `string?` |  | attribute `time-zone` | An IANA zone such as Europe/Berlin; empty uses the reader's own zone. |
| `ChildContent` | `RenderFragment?` |  | default slot | Fallback text, shown only while the datetime is empty or not a date. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-local-time` in the `plainkit-sdk` skill.

## `PkMedia`

`<pk-media>`: **Media** (Media). An image or video in a fixed aspect-ratio box with an optional figure caption and lightbox-ready behaviour.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Ratio` | `string?` |  | attribute `ratio` | Aspect ratio as width/height: 16/9, 4/3, 1/1, 3/4. |
| `Fit` | `string?` |  | attribute `fit` | How media fills the box. |
| `Caption` | `string?` |  | attribute `caption` | Figure caption. |
| `Lightbox` | `bool` |  | attribute `lightbox` | The box becomes a button that fires pk-open. |
| `Square` | `bool` |  | attribute `square` | Square corners. |
| `ChildContent` | `RenderFragment?` |  | default slot | A lightbox request. Cancel if you handle it yourself. The event can be cancelled in the browser (preventDefault); a callback cannot cancel it. [Parameter] public EventCallback<PkOpenEventArgs> OnOpen { get; set; } An img, picture, video or svg. |
| `CaptionContent` | `RenderFragment?` |  | slot `caption` | A rich caption; overrides caption. |

When you set a named fragment (`CaptionContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-open` | `PkOpenEventArgs` | Src: string?, Alt: string?, Caption: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-media` in the `plainkit-sdk` skill.

## `PkStat`

`<pk-stat>`: **Stat tile** (Data display). A headline number with a label, an optional change indicator (arrow, sign and a spoken form), a sparkline and a tone. With an href the whole tile is one link. The tile prop gives the compact dashboard look; interactive makes a tile with no href a button.

Hand-written component (not generated).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | What the number measures. |
| `Value` | `string?` |  | attribute `value` | The headline value, as text (format it yourself). |
| `Subtext` | `string?` |  | attribute `subtext` | A line of text under the value. |
| `Variant` | `StatCardVariant` | `Neutral` `Positive` `Warning` `Critical` | attribute `tone` | The colour of the value. Neutral is the element's default and leaves the attribute off. |
| `Href` | `string?` |  | attribute `href` | Makes the whole tile one link to this address. |
| `Delta` | `string?` |  | attribute `delta` | The percent change, for example `12.5` or `-3`; the element draws the arrow and the signed percentage. |
| `Invert` | `bool` |  | attribute `invert` | Down is good news (costs, returns): swaps the trend colours. |
| `Series` | `IReadOnlyList<double>?` |  | attribute `values` | Points for the sparkline (at least two). |
| `SubtextContent` | `RenderFragment?` |  | default slot | A subtext with markup; it is the body of the element. |
| `IconContent` | `RenderFragment?` |  | slot `icon` | An icon before the value. |
| `OnClick` | `EventCallback` |  | event `pk-activate` | The tile was pressed (`pk-activate`): a link tile or, with Interactive, a button tile. |
| `Tile` | `bool` |  | attribute `tile` | The compact dashboard look. |
| `Interactive` | `bool` |  | attribute `interactive` | Without an Href, makes the tile a button (Enter and Space) that raises OnClick. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-activate` | `PkActivateEventArgs` | Href: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-stat` in the `plainkit-sdk` skill.

## `PkTable`

`<pk-table>`: **Table** (Data display). A data table. Two modes: hand it a raw table in the default slot (rows and cells exactly as authored) and it supplies the scroll frame, toolbar, bulk, empty and footer slots; or give it columns and rows and it renders the table itself, with sorting, filtering, selection, loading and phone cards. The data-driven mode: striped, hover, bordered, density, sticky header and first column, sortable headers, filter row, row selection with a bulk bar, loading and empty states, expandable detail rows and a card layout on a phone. Custom cell content goes in slots named cell-<rowId>-<key>; expandable rows show the slot detail-<rowId>. With manual set, the host owns sorting, filtering and paging and the element only renders and reports.

**Not available as a component in this package.** Use the element directly in markup: `<pk-table>` with its props, slots and events as documented in the `plainkit-sdk` skill (`elements-data-display.md`). See `known-gaps.md`.

## `PkTag`

`<pk-tag>`: **Tag** (Data display). A neutral label that can carry a remove button.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Removable` | `bool` |  | attribute `removable` | Show the remove button. |
| `Value` | `string?` |  | attribute `value` | Sent in the remove event; defaults to the text. |
| `Disabled` | `bool` |  | attribute `disabled` | Dim the tag and disable the button. |
| `ChildContent` | `RenderFragment?` |  | default slot | The label. |
| `OnRemove` | `EventCallback<PkRemoveEventArgs>` |  | event `pk-remove` |  |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-remove` | `PkRemoveEventArgs` | Index: double?, Src: string?, Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tag` in the `plainkit-sdk` skill.

## `PkTimeline`

`<pk-timeline>`: **Timeline** (Data display). A vertical activity feed of timeline items joined by a line.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | Accessible name of the list. |
| `ChildContent` | `RenderFragment?` |  | default slot | pk-timeline-item children, in the order to show them. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-timeline` in the `plainkit-sdk` skill.

## `PkTimelineItem`

`<pk-timeline-item>`: **Timeline item** (Data display). One event on a timeline: a marker, a heading, a time and a body.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Heading` | `string?` |  | attribute `heading` | Title of the event. |
| `Time` | `string?` |  | attribute `time` | An ISO date or any text; shown in a time element. |
| `Status` | `string?` |  | attribute `status` | Marker colour and fill. |
| `ChildContent` | `RenderFragment?` |  | default slot | The body. |
| `HeadingContent` | `RenderFragment?` |  | slot `heading` | A rich heading; overrides heading. |
| `TimeContent` | `RenderFragment?` |  | slot `time` | A rich time; overrides time. |

When you set a named fragment (`HeadingContent`, `TimeContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-timeline-item` in the `plainkit-sdk` skill.
