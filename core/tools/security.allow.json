{
  "note": "innerHTML-style sinks allowed per file, with the source of the markup. Prefer textContent or DOM APIs; a new sink needs a line here.",
  "htmlSinks": {
    "site/gallery/gallery.js": {
      "count": 6,
      "source": "Markup assembled from gallery.data.js (generated from the repo's component folders) with every dynamic value passed through esc()."
    },
    "js/code-explorer/element.js": {
      "count": 10,
      "source": "Provider data (file paths, code lines, search hits) is escaped with esc() before interpolation; tokens are wrapped in fixed spans."
    },
    "modules/scorecard/scorecard.js": {
      "count": 1,
      "source": "One helper (fromHtml) parses the module's own fixed markup and the ranked table built from finding text and scores, every dynamic value passed through esc(). The framework sections build nodes with the DOM and use no markup strings."
    },
    "modules/scorecard/measure.js": {
      "count": 1,
      "source": "The scale test renders a generated table of fixed sample rows (numbers only) to time layout; nothing from outside reaches it."
    },
    "site/gallery/elements-view.js": {
      "count": 4,
      "source": "The element's own example markup from its meta file, and markup the reader types into the playground (their own page, their own input)."
    },
    "js/element.js": {
      "count": 1,
      "source": "The element class's static template: markup authored in elements/<name>/<name>.html and inlined by the build, parsed once into a template element."
    },
    "site/shell.js": {
      "count": 1,
      "source": "Static navigation markup from the PAGES constant and fixed strings."
    },
    "samples/templates/chrome.js": {
      "count": 4,
      "source": "Static example-page chrome from constants; page title and crumbs come from the page's own script."
    },
    "site/reference/page.js": {
      "count": 1,
      "source": "Fixed sample markup constants in the same file, rendered twice (dark and light)."
    }
  },
  "dynamicScript": [],
  "legacyVar": []
}
