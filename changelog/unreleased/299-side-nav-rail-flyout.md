---
type: fixed
issue: 299
---
`pk-side-nav` icon rail: the children inside an open branch flyout are no longer in rail mode (only top-level rows get `rail`), so they show their labels, and the flyout carries the branch name as a heading (new `sub-title` part on `pk-nav-item`) and as the accessible name of its group. ArrowRight on an open flyout enters it and ArrowLeft returns to the branch.
