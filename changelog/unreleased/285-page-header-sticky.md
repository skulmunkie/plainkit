---
type: added
issue: 285
---
pk-page-header and PkPageHeader take `sticky` (Blazor `Sticky`): the header stays pinned to the top of the scrolling page body, flush under the app bar inside pk-app-shell, with `--pk-page-header-sticky-top` as the offset hook.
