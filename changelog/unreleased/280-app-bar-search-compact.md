---
type: added
issue: 280
---
`pk-app-bar-search` has a `compact` attribute (`Compact` in Blazor) that keeps the icon button at any width and expands the field over the header row, as it does on a phone.
