---
type: changed
issue: 281
---
`pk-detail-layout` sticks a sidebar that is taller than the viewport by its bottom edge (a shorter one still by its top), so its lower cards can be reached while the main column scrolls; the sidebar stays flush right in the content area, and `--pk-detail-layout-main-max` caps the main column.
