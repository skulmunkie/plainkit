---
type: added
issue: 230
---
A duration/easing token scale (`--duration-fast`, `--duration-base`, `--duration-slow`, `--ease-standard`) for quick UI feedback transitions, so a consumer no longer guesses a millisecond value; the SDK's own hand-written transitions now use them too.
