---
type: added
issue: 255
---
`PkRawTable` gains `TableClass` (a class for the inner table), and `IsEmpty` with `EmptyText` or `EmptyContent`, shown instead of the table when there are no rows. Attributes and `class` already reach the `pk-table` frame.
