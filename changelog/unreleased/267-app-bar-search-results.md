---
type: added
issue: 267
---
`pk-app-bar-search` results panel: a minimum width (`--pk-app-bar-search-popup-width`, default 24rem) so titles are not cut to the field width, a `footer` slot for an action such as "Full search", and items without an id shown as unselectable notes (a group's "+12 more"). `PkAppBarSearch` gains `EmptyContent` and `FooterContent`.
