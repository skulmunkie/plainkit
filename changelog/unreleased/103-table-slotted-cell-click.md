---
type: fixed
issue: 103
---
A click on content the host puts in a `cell-<rowId>-<key>` slot of a clickable `pk-table` raises `pk-row-click` with that row; a button, link or field the host slotted keeps its own click.
