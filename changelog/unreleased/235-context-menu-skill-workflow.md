---
type: notes
issue: 235
---
Both agent skills document a "Share a context menu across targets" workflow: when to reach for `pk-context-menu`/`PkContextMenu` instead of a row of always-visible per-row or per-card icon buttons, and how to serve different menu content per target using `pk-open`'s `context` field.
