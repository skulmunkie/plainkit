---
type: added
issue: 232
---
`pk-tag-input` (`PkTagInput`) gets `lockedTags`/`LockedTags`: always-present, comma-joined tags shown before the editable ones, without a remove button and excluded from `value`, the form value and Backspace removal; `lockedTitle`/`LockedTitle` sets their tooltip.
