---
type: changed
issue: 51
---
The `pk-workspace` `active-pane` docs say that a tab tap raises a cancelable `pk-pane-change` first, and that a host that owns the pane from a route calls `preventDefault()` and changes the route instead.
