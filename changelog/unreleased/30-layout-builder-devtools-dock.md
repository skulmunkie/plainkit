---
type: added
issue: 30
---
The dev tools dock (`mountDevTools`, Ctrl+`) gains a Layout builder tab, a scratch instance of `mountLayoutBuilder` next to Console, Logs, Logging, Quality, Inspector and Theme; the standalone `site/layout-builder/` page was already reachable from the site's top nav.
