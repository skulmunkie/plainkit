---
type: fixed
issue: 142
---
The gallery site's own chrome is more accessible: the side nav's section-title links reach a 44px tap target on a phone, the 26 template, pattern and
layout preview routes each render a real level-1 heading instead of none, the `pk-app-shell` and `pk-page-header` element pages no longer show more
than one, and a fill template (a workspace) now reaches the shell's footer instead of stopping short by its page padding.
