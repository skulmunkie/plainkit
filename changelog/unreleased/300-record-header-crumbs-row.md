---
type: fixed
issue: 300
---
A `pk-page-header variant="record"` whose title is its last breadcrumb (no `heading`) keeps its chips and actions on the crumbs row (narrow: the actions stay there and only the chips drop under it), and the `pk-breadcrumb` separator chevron is no longer part of the link (not clickable, not underlined, hidden from assistive tech).
