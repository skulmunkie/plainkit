---
type: fixed
issue: 268
---
`PkFieldGroup` no longer loses the label text on a field with `Help` or `HelpWhen`: the help tooltip goes in the label-action slot beside the label instead of replacing the label slot.
