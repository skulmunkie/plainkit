---
type: added
issue: 229
---
A content-width token scale (`--content-sm` 40rem, `--content-md` 60rem, `--content-lg` 75rem), alongside `--space-*`, for a page's own `max-width: var(--content-md)` instead of a hand-picked number.
