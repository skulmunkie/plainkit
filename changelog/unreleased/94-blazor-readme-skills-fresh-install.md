---
type: fixed
issue: 94
---
The PlainKit.Blazor README and skills are corrected after a fresh-install check: no hard-coded release version, `TItem="..."` on `PkTable` and `PkDataList` when a handler is a method group, the explicit `<ChildContent>` next to `FooterContent`, a table of the most-used parameters, and the `/_plainkit` set-up marked optional. The contributor build notes moved to CONTRIBUTING.md, and the table limits in `known-gaps.md` and `data-list.md` are derived from the `pk-table` meta instead of stale text.
