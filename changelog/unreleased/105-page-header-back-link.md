---
type: added
issue: 105
---
`PkPageHeader` has an opt-in `BackLink` parameter: a chevron link named `Back to <crumb>` to the last parent crumb that has an `Href`, drawn before the title in the `ShellSection` outlet (or above the header without one), so a page in the app shell can go up on a phone.
