---
type: added
issue: 63
---
The rows of a `clickable` `pk-table` take the keyboard: each row is a tab stop with a focus ring, and Enter or Space on the focused row raises `pk-row-click` (keys on a checkbox, button or link inside the row stay with that control). The keyboard code loads lazily with the expandable-rows module.
