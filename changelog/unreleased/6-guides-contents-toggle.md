---
type: fixed
issue: 6
---
On a phone the Guides page's contents button keeps its pressed state in step with the drawer (the toggle button flipped its own state before the page set it; the page now sets it from the bar afterwards).
