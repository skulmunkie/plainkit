---
type: fixed
issue: 325
---
A property set on an element before it upgrades is now reflected to its attribute like any assignment, so a `pk-side-nav` rail row keeps its `rail` attribute and its flyout displays whichever order the elements load in.
