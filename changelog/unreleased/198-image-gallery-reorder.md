---
type: added
issue: 198
---
`pk-image-gallery`, editable, gets a reorder handle on each tile (drag, or focus a tile and press Alt+Up/Alt+Down), built on `pk-sortable`; it raises `pk-reorder` with the new image order.
