@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/range.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-range value="@PkAttr.Num(Value)"
          value-low="@PkAttr.Num(ValueLow)"
          value-high="@PkAttr.Num(ValueHigh)"
          min="@PkAttr.Num(Min)"
          max="@PkAttr.Num(Max)"
          step="@PkAttr.Num(Step)"
          dual="@Dual"
          label="@Label"
          disabled="@Disabled"
          output="@ShowOutput"
          class="@Css()"
          @attributes="Splat"></pk-range>

@code {
    /// <summary>Single mode: the value.</summary>
    [Parameter] public double Value { get; set; } = 0;

    /// <summary>Dual mode: the low value.</summary>
    [Parameter] public double ValueLow { get; set; } = 0;

    /// <summary>Dual mode: the high value.</summary>
    [Parameter] public double ValueHigh { get; set; } = 100;

    /// <summary>Lowest value.</summary>
    [Parameter] public double Min { get; set; } = 0;

    /// <summary>Highest value.</summary>
    [Parameter] public double Max { get; set; } = 100;

    /// <summary>Increment.</summary>
    [Parameter] public double Step { get; set; } = 1;

    /// <summary>Two thumbs: a low and a high value; the low thumb never passes the high one.</summary>
    [Parameter] public bool Dual { get; set; }

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>Shows the current value under the slider.</summary>
    [Parameter] public bool ShowOutput { get; set; }

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<double> ValueChanged { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-range"] = EventCallback.Factory.Create<PkRangeEventArgs>(this, HandlePkRange);
    }

    private async Task HandlePkRange(PkRangeEventArgs e)
    {
        Value = e.Value ?? 0;
        await ValueChanged.InvokeAsync(Value);
    }
}
