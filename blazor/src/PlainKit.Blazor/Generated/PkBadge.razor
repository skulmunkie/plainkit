@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/badge.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-badge variant="@Variant"
          count="@Count"
          max="@PkAttr.Num(Max)"
          dot="@Dot"
          class="@Css()"
          @attributes="Splat">@ChildContent</pk-badge>

@code {
    /// <summary>Colour.</summary>
    [Parameter] public string? Variant { get; set; }

    /// <summary>Value to show when the slot is empty, capped by max.</summary>
    [Parameter] public string? Count { get; set; }

    /// <summary>Counts above read as max+; 0 disables capping.</summary>
    [Parameter] public double Max { get; set; } = 99;

    /// <summary>A status dot with the text as its label instead of a pill.</summary>
    [Parameter] public bool Dot { get; set; }

    /// <summary>Text; overrides the count.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
