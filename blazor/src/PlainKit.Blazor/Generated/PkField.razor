@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/field.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-field label="@Label"
          help="@Hint"
          error="@Error"
          required="@Required"
          max="@PkAttr.Num(MaxLength)"
          layout="@Layout"
          size="@Size">@ChildContent@if (LabelExtra is not null) {<span slot="label">@LabelExtra</span>}@if (LabelAction is not null) {<span slot="label-action">@LabelAction</span>}@if (HintContent is not null) {<span slot="help">@HintContent</span>}</pk-field>

@code {
    /// <summary>The label text (or use the label slot).</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>Help text under the control.</summary>
    [Parameter] public string? Hint { get; set; }

    /// <summary>An error message; puts the control into the invalid state.</summary>
    [Parameter] public string? Error { get; set; }

    /// <summary>Marks the field required (a star, and required on the control).</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Counter maximum: shows length / max and turns warm from 90% and red past the max; 0 hides the counter.</summary>
    [Parameter] public int MaxLength { get; set; } = 0;

    /// <summary>stack puts the label above the control; row puts it in a fixed column beside it (stacks on a phone); inline puts label and control on one line at their natural width.</summary>
    [Parameter] public string? Layout { get; set; }

    /// <summary>The control: an pk-* form control or a native input, select or textarea.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>A rich label (overrides the label prop).</summary>
    [Parameter] public RenderFragment? LabelExtra { get; set; }

    /// <summary>A small action beside the label, such as a reset to source button (a pk-button size mini, variant ghost). Clicking it does not focus the control.</summary>
    [Parameter] public RenderFragment? LabelAction { get; set; }

    /// <summary>Rich help text (overrides the help prop).</summary>
    [Parameter] public RenderFragment? HintContent { get; set; }

    /// <summary>sm is the compact field of a toolbar or filter row: smaller label, tighter gap.</summary>
    [Parameter] public string? Size { get; set; }
}
