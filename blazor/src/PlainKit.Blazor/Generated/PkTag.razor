@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/tag.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-tag removable="@Removable"
        value="@Value"
        disabled="@Disabled"
        @attributes="Splat">@ChildContent</pk-tag>

@code {
    /// <summary>Show the remove button.</summary>
    [Parameter] public bool Removable { get; set; }

    /// <summary>Sent in the remove event; defaults to the text.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>Dim the tag and disable the button.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The label.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The remove button was pressed. Cancel to keep the tag; otherwise it removes itself.</summary>
    /// <remarks>The event can be cancelled in the browser (preventDefault); a callback cannot cancel it.</remarks>
    [Parameter] public EventCallback<PkRemoveEventArgs> OnRemove { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-remove"] = EventCallback.Factory.Create<PkRemoveEventArgs>(this, HandlePkRemove),
        };
    }

    private async Task HandlePkRemove(PkRemoveEventArgs e)
    {
        await OnRemove.InvokeAsync(e);
    }
}
