@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/navbar.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-navbar label="@Label"
           open="@Open"
           sticky="@Sticky"
           class="@Css()"
           @attributes="Splat">@if (BrandContent is not null) {<span slot="brand">@BrandContent</span>}@ChildContent@if (ActionsContent is not null) {<span slot="actions">@ActionsContent</span>}</pk-navbar>

@code {
    /// <summary>Accessible name</summary>
    [Parameter] public string? Label { get; set; } = "Main";

    /// <summary>Links unfolded (small screens).</summary>
    [Parameter] public bool Open { get; set; }

    /// <summary>The sticky setting</summary>
    [Parameter] public bool Sticky { get; set; }

    /// <summary>Brand link.</summary>
    [Parameter] public RenderFragment? BrandContent { get; set; }

    /// <summary>Links and pk-dropdown menus.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Right-aligned controls.</summary>
    [Parameter] public RenderFragment? ActionsContent { get; set; }

    /// <summary>Hamburger toggled.</summary>
    [Parameter] public EventCallback<PkToggleEventArgs> OnToggle { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-toggle"] = EventCallback.Factory.Create<PkToggleEventArgs>(this, HandlePkToggle);
    }

    private async Task HandlePkToggle(PkToggleEventArgs e)
    {
        await OnToggle.InvokeAsync(e);
    }
}
