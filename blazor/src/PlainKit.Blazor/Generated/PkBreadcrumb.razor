@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/breadcrumb.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-breadcrumb label="@Label"
               max="@PkAttr.Num(Max)"
               @attributes="Splat">@ChildContent</pk-breadcrumb>

@code {
    /// <summary>Landmark name.</summary>
    [Parameter] public string? Label { get; set; } = "Breadcrumb";

    /// <summary>Crumbs shown before the middle folds (3 on a phone).</summary>
    [Parameter] public int Max { get; set; } = 4;

    /// <summary>a elements; the last one is the current page.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The fold button revealed the hidden crumbs.</summary>
    [Parameter] public EventCallback OnExpand { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-expand"] = EventCallback.Factory.Create<EventArgs>(this, HandlePkExpand),
        };
    }

    private async Task HandlePkExpand(EventArgs e)
    {
        await OnExpand.InvokeAsync();
    }
}
