@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/button-group.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-button-group orientation="@Orientation"
                 fill="@Fill"
                 label="@AriaLabel"
                 mode="@Mode">@ChildContent</pk-button-group>

@code {
    /// <summary>Direction of the bar.</summary>
    [Parameter] public string? Orientation { get; set; }

    /// <summary>Fills the container width and shares it between the buttons.</summary>
    [Parameter] public bool Fill { get; set; }

    /// <summary>Accessible name of the group (aria-label).</summary>
    [Parameter] public string? AriaLabel { get; set; }

    /// <summary>single: the toggle buttons in the group release each other (one is always pressed); multi and none leave each toggle alone.</summary>
    [Parameter] public string? Mode { get; set; }

    /// <summary>pk-button elements.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
