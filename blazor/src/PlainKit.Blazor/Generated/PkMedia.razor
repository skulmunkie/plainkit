@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/media.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-media ratio="@Ratio"
          fit="@Fit"
          caption="@Caption"
          lightbox="@Lightbox"
          square="@Square"
          class="@Css()"
          @attributes="Splat">@ChildContent@if (CaptionContent is not null) {<span slot="caption">@CaptionContent</span>}</pk-media>

@code {
    /// <summary>Aspect ratio as width/height: 16/9, 4/3, 1/1, 3/4.</summary>
    [Parameter] public string? Ratio { get; set; }

    /// <summary>How media fills the box.</summary>
    [Parameter] public string? Fit { get; set; }

    /// <summary>Figure caption.</summary>
    [Parameter] public string? Caption { get; set; }

    /// <summary>The box becomes a button that fires pk-open.</summary>
    [Parameter] public bool Lightbox { get; set; }

    /// <summary>Square corners.</summary>
    [Parameter] public bool Square { get; set; }

    /// <summary>A lightbox request. Cancel if you handle it yourself.</summary>
    /// <remarks>The event can be cancelled in the browser (preventDefault); a callback cannot cancel it.</remarks>
    [Parameter] public EventCallback<PkOpenEventArgs> OnOpen { get; set; }

    /// <summary>An img, picture, video or svg.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>A rich caption; overrides caption.</summary>
    [Parameter] public RenderFragment? CaptionContent { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-open"] = EventCallback.Factory.Create<PkOpenEventArgs>(this, HandlePkOpen);
    }

    private async Task HandlePkOpen(PkOpenEventArgs e)
    {
        await OnOpen.InvokeAsync(e);
    }
}
