@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/unit-input.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-unit-input value="@Value"
               name="@Name"
               units="@Units"
               placeholder="@Placeholder"
               label="@AriaLabel"
               show-label="@ShowLabel"
               disabled="@Disabled"
               readonly="@ReadOnly"
               required="@Required"
               invalid="@IsInvalid"
               min="@Min"
               max="@Max"
               step="@Step"
               @attributes="Splat"></pk-unit-input>

@code {
    /// <summary>The number and unit joined, for example 1.5rem or 12px (empty until there is a number); what the form submits. A value that is not a number with a unit (auto, calc(...)) leaves the number field empty and is kept until the user edits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently.</summary>
    [Parameter] public string? Value { get; set; } = "";

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }

    /// <summary>The form field name.</summary>
    [Parameter] public string? Name { get; set; } = "";

    /// <summary>The units the select offers, comma or space separated; the first is used until a value or the user picks another. A value whose unit is not listed adds that unit to the select.</summary>
    [Parameter] public string? Units { get; set; } = "px,rem,em,%";

    /// <summary>Hint shown in the number field while it is empty.</summary>
    [Parameter] public string? Placeholder { get; set; } = "";

    /// <summary>The accessible name (aria-label) of the number field; the unit select is named "&lt;label&gt; unit". pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? AriaLabel { get; set; } = "";

    /// <summary>Shows label as visible text above the field (linked to it), for use without a pk-field.</summary>
    [Parameter] public bool ShowLabel { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>Shows the value but does not let it change (the unit select is disabled too).</summary>
    [Parameter] public bool ReadOnly { get; set; }

    /// <summary>The form cannot be submitted while the number is empty.</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>Lowest number (the number, whatever the unit).</summary>
    [Parameter] public string? Min { get; set; } = "";

    /// <summary>Highest number.</summary>
    [Parameter] public string? Max { get; set; } = "";

    /// <summary>The number's step: any (the default) allows decimals, or a size such as 0.25 that the arrow keys move by.</summary>
    [Parameter] public string? Step { get; set; } = "any";

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-value-change"] = EventCallback.Factory.Create<PkValueChangeEventArgs>(this, HandlePkValueChange),
        };
    }

    private async Task HandlePkValueChange(PkValueChangeEventArgs e)
    {
        Value = e.Value;
        await ValueChanged.InvokeAsync(Value);
    }
}
