@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/calendar.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-calendar value="@PkAttr.Date(Value)"
             month="@PkAttr.Date(Month)"
             min="@PkAttr.Date(Min)"
             max="@PkAttr.Date(Max)"
             week-start="@PkAttr.Num(WeekStart)"
             locale="@Locale"
             marks="@PkAttr.Json(Marks)"
             readonly="@ReadOnly"
             disabled="@Disabled"
             @attributes="Splat"></pk-calendar>

@code {
    /// <summary>The selected day, an ISO date such as 2026-09-19.</summary>
    [Parameter] public DateOnly? Value { get; set; }

    /// <summary>Raised with the new value of Value (two-way: Value follows it).</summary>
    [Parameter] public EventCallback<DateOnly?> ValueChanged { get; set; }

    /// <summary>An ISO date whose month is shown; defaults to the value or today.</summary>
    [Parameter] public DateOnly? Month { get; set; }

    /// <summary>Earliest selectable ISO date.</summary>
    [Parameter] public DateOnly? Min { get; set; }

    /// <summary>Latest selectable ISO date.</summary>
    [Parameter] public DateOnly? Max { get; set; }

    /// <summary>First weekday: 0 Sunday, 1 Monday.</summary>
    [Parameter] public int WeekStart { get; set; } = 0;

    /// <summary>Language of month and weekday names; defaults to the page language.</summary>
    [Parameter] public string? Locale { get; set; }

    /// <summary>ISO dates that get a dot.</summary>
    [Parameter] public IReadOnlyList<DateOnly>? Marks { get; set; }

    /// <summary>Show only; no selection.</summary>
    [Parameter] public bool ReadOnly { get; set; }

    /// <summary>Inert.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The shown month changed (yyyy-mm).</summary>
    [Parameter] public EventCallback<PkMonthEventArgs> OnMonthChanged { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-select"] = EventCallback.Factory.Create<PkSelectEventArgs>(this, HandlePkSelect),
            ["onpk-month"] = EventCallback.Factory.Create<PkMonthEventArgs>(this, HandlePkMonth),
        };
    }

    private async Task HandlePkSelect(PkSelectEventArgs e)
    {
        Value = PkAttr.ParseDate(e.Value);
        await ValueChanged.InvokeAsync(Value);
    }

    private async Task HandlePkMonth(PkMonthEventArgs e)
    {
        await OnMonthChanged.InvokeAsync(e);
    }
}
