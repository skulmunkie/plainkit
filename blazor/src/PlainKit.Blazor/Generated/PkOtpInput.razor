@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/otp-input.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-otp-input value="@Value"
              length="@PkAttr.Num(Length)"
              type="@Type"
              separator-at="@PkAttr.Num(SeparatorAt)"
              disabled="@Disabled"
              invalid="@IsInvalid"
              label="@AriaLabel"
              @attributes="Splat"></pk-otp-input>

@code {
    /// <summary>The code typed so far.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>Number of cells.</summary>
    [Parameter] public int Length { get; set; } = 6;

    /// <summary>The alphabet: digits, letters and digits, or anything.</summary>
    [Parameter] public string? Type { get; set; }

    /// <summary>Puts a dash after this many cells (0 for none).</summary>
    [Parameter] public int SeparatorAt { get; set; } = 0;

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? AriaLabel { get; set; }

    /// <summary>Every cell is filled.</summary>
    [Parameter] public EventCallback<PkOtpCompleteEventArgs> OnComplete { get; set; }

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-otp-complete"] = EventCallback.Factory.Create<PkOtpCompleteEventArgs>(this, HandlePkOtpComplete),
            ["onpk-otp-change"] = EventCallback.Factory.Create<PkOtpChangeEventArgs>(this, HandlePkOtpChange),
        };
    }

    private async Task HandlePkOtpComplete(PkOtpCompleteEventArgs e)
    {
        await OnComplete.InvokeAsync(e);
    }

    private async Task HandlePkOtpChange(PkOtpChangeEventArgs e)
    {
        Value = e.Value;
        await ValueChanged.InvokeAsync(Value);
    }
}
