@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/local-time.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-local-time datetime="@Value"
               format="@Format"
               length="@Length"
               locale="@Locale"
               time-zone="@TimeZone">@ChildContent</pk-local-time>

@code {
    /// <summary>The instant: an ISO 8601 string (2026-09-19T14:30:00Z). A date-only value is a calendar day and is never shifted by the time zone.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>What to show. relative reads 3 hours ago and refreshes every 30 seconds while connected; its title holds the full date.</summary>
    [Parameter] public string? Format { get; set; }

    /// <summary>How much of the date to spell out (9/19/26, Sep 19, 2026, September 19, 2026).</summary>
    [Parameter] public string? Length { get; set; }

    /// <summary>A BCP 47 tag such as de-DE; empty uses the reader's browser locale.</summary>
    [Parameter] public string? Locale { get; set; }

    /// <summary>An IANA zone such as Europe/Berlin; empty uses the reader's own zone.</summary>
    [Parameter] public string? TimeZone { get; set; }

    /// <summary>Fallback text, shown only while the datetime is empty or not a date.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
