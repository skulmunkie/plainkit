@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/split-button.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-split-button variant="@Variant"
                 disabled="@Disabled"
                 toggle-label="@ToggleLabel"
                 open="@Open"
                 menu-align="@MenuAlign"
                 @onclick="HandleClick"
                 class="@Css()"
                 @attributes="Splat">@ChildContent@if (MenuContent is not null) {<span slot="menu">@MenuContent</span>}</pk-split-button>

@code {
    /// <summary>Colour of both halves.</summary>
    [Parameter] public string? Variant { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>Accessible name of the caret button.</summary>
    [Parameter] public string? ToggleLabel { get; set; }

    /// <summary>Whether the menu is open.</summary>
    [Parameter] public bool Open { get; set; }

    /// <summary>Which edge the menu lines up with.</summary>
    [Parameter] public string? MenuAlign { get; set; }

    /// <summary>The main action was clicked.</summary>
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }

    /// <summary>A menu item was chosen.</summary>
    [Parameter] public EventCallback<PkSplitSelectEventArgs> OnSelect { get; set; }

    /// <summary>The main action label.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The menu items: buttons (with a value or data-value).</summary>
    [Parameter] public RenderFragment? MenuContent { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-split-select"] = EventCallback.Factory.Create<PkSplitSelectEventArgs>(this, HandlePkSplitSelect);
    }

    private async Task HandleClick(MouseEventArgs e)
    {
        await OnClick.InvokeAsync(e);
    }

    private async Task HandlePkSplitSelect(PkSplitSelectEventArgs e)
    {
        await OnSelect.InvokeAsync(e);
    }
}
