@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/pagination.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-pagination page="@PkAttr.Num(Page)"
               pages="@PkAttr.Num(Pages)"
               total="@PkAttr.Num(Total)"
               page-size="@PkAttr.Num(PageSize)"
               sizes="@PkAttr.Json(PageSizeOptions)"
               siblings="@PkAttr.Num(Siblings)"
               boundary="@PkAttr.Num(Boundary)"
               mode="@(Mode?.ToAttr())"
               label="@Label"
               loading="@Loading"
               edges="@Edges"
               class="@Css()"
               @attributes="Splat"></pk-pagination>

@code {
    /// <summary>Current page, 1-based.</summary>
    [Parameter] public int Page { get; set; } = 1;

    /// <summary>Page count; 0 derives it from total and pageSize.</summary>
    [Parameter] public int Pages { get; set; } = 0;

    /// <summary>Item count; enables the range summary.</summary>
    [Parameter] public int Total { get; set; } = 0;

    /// <summary>Items per page.</summary>
    [Parameter] public int PageSize { get; set; } = 25;

    /// <summary>Options for the page-size select, for example [10, 25, 50, 100]; empty hides the select.</summary>
    [Parameter] public int[]? PageSizeOptions { get; set; }

    /// <summary>Pages either side of the current one.</summary>
    [Parameter] public int Siblings { get; set; } = 1;

    /// <summary>Pages always shown at each end.</summary>
    [Parameter] public int Boundary { get; set; } = 1;

    /// <summary>Layout.</summary>
    [Parameter] public PaginationMode? Mode { get; set; }

    /// <summary>Accessible name of the nav.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>Load-more busy state.</summary>
    [Parameter] public bool Loading { get; set; }

    /// <summary>Raised with the new value of Page (two-way: Page follows it).</summary>
    [Parameter] public EventCallback<int> PageChanged { get; set; }

    /// <summary>Raised with the new value of PageSize (two-way: PageSize follows it).</summary>
    [Parameter] public EventCallback<int> PageSizeChanged { get; set; }

    /// <summary>Load more was pressed.</summary>
    [Parameter] public EventCallback OnLoadMore { get; set; }

    /// <summary>Show First and Last buttons beside Previous and Next.</summary>
    [Parameter] public bool Edges { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-page"] = EventCallback.Factory.Create<PkPageEventArgs>(this, HandlePkPage);
        handlers["onpk-page-size"] = EventCallback.Factory.Create<PkPageSizeEventArgs>(this, HandlePkPageSize);
        handlers["onpk-load-more"] = EventCallback.Factory.Create<EventArgs>(this, HandlePkLoadMore);
    }

    private async Task HandlePkPage(PkPageEventArgs e)
    {
        Page = (int)(e.Page ?? 0);
        await PageChanged.InvokeAsync(Page);
    }

    private async Task HandlePkPageSize(PkPageSizeEventArgs e)
    {
        PageSize = (int)(e.PageSize ?? 0);
        await PageSizeChanged.InvokeAsync(PageSize);
    }

    private async Task HandlePkLoadMore(EventArgs e)
    {
        await OnLoadMore.InvokeAsync();
    }
}
