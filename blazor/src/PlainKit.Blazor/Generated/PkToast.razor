@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/toast.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-toast kind="@(Kind.ToAttr())"
          heading="@Heading"
          message="@Message"
          duration="@PkAttr.Num(Duration)"
          no-close="@NoClose"
          @attributes="Splat">@ChildContent@if (ActionContent is not null) {<span slot="action">@ActionContent</span>}</pk-toast>

@code {
    /// <summary>The kind setting</summary>
    [Parameter] public ToastKind Kind { get; set; } = ToastKind.Info;

    /// <summary>The heading setting</summary>
    [Parameter] public string? Heading { get; set; } = "";

    /// <summary>Used when nothing is slotted.</summary>
    [Parameter] public string? Message { get; set; } = "";

    /// <summary>Milliseconds before it dismisses itself; 0 keeps it until dismissed; -1 is automatic (5s, 8s with an action).</summary>
    [Parameter] public int Duration { get; set; } = -1;

    /// <summary>Hide the dismiss button.</summary>
    [Parameter] public bool NoClose { get; set; }

    /// <summary>Message.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>One or two buttons.</summary>
    [Parameter] public RenderFragment? ActionContent { get; set; }

    /// <summary>timeout, close, action or method.</summary>
    [Parameter] public EventCallback<PkDismissEventArgs> OnDismiss { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-dismiss"] = EventCallback.Factory.Create<PkDismissEventArgs>(this, HandlePkDismiss),
        };
    }

    private async Task HandlePkDismiss(PkDismissEventArgs e)
    {
        await OnDismiss.InvokeAsync(e);
    }
}
