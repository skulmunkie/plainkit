@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/dropzone.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-dropzone accept="@Accept"
             multiple="@Multiple"
             max-size="@MaxFileSizeBytes"
             max-files="@PkAttr.Num(MaxFiles)"
             label="@Label"
             disabled="@Disabled"
             @attributes="Splat">@ChildContent@if (HintContent is not null) {<span slot="hint">@HintContent</span>}</pk-dropzone>

@code {
    /// <summary>Accepted types: extensions, MIME types or type/* (like the accept attribute).</summary>
    [Parameter] public string? Accept { get; set; }

    /// <summary>Allows several files.</summary>
    [Parameter] public bool Multiple { get; set; }

    /// <summary>Largest file: 500KB, 5MB, 1GB or bytes.</summary>
    [Parameter] public string? MaxFileSizeBytes { get; set; }

    /// <summary>Most files (multiple); 0 means no limit.</summary>
    [Parameter] public int MaxFiles { get; set; } = 0;

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>Files were picked or dropped: those kept and those refused with a reason.</summary>
    [Parameter] public EventCallback<PkFilesEventArgs> OnFiles { get; set; }

    /// <summary>The zone title.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>A hint under the title (types and limits).</summary>
    [Parameter] public RenderFragment? HintContent { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-files"] = EventCallback.Factory.Create<PkFilesEventArgs>(this, HandlePkFiles),
        };
    }

    private async Task HandlePkFiles(PkFilesEventArgs e)
    {
        await OnFiles.InvokeAsync(e);
    }
}
