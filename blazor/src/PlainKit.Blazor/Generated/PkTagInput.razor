@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/tag-input.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-tag-input value="@Value"
              placeholder="@Placeholder"
              disabled="@Disabled"
              required="@Required"
              invalid="@IsInvalid"
              separators="@Separators"
              max="@PkAttr.Num(Max)"
              label="@AriaLabel"
              @attributes="Splat"></pk-tag-input>

@code {
    /// <summary>The tags, comma-joined. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>Hint shown in the empty field.</summary>
    [Parameter] public string? Placeholder { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The form cannot be submitted while the value is empty.</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>Characters that end a tag (besides Enter).</summary>
    [Parameter] public string? Separators { get; set; }

    /// <summary>Most tags allowed; 0 means no limit.</summary>
    [Parameter] public int Max { get; set; } = 0;

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? AriaLabel { get; set; }

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-tags-change"] = EventCallback.Factory.Create<PkTagsChangeEventArgs>(this, HandlePkTagsChange),
        };
    }

    private async Task HandlePkTagsChange(PkTagsChangeEventArgs e)
    {
        Value = e.Value;
        await ValueChanged.InvokeAsync(Value);
    }
}
