@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/cluster.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-cluster direction="@Direction"
            gap="@Gap"
            align="@Align"
            justify="@Justify"
            nowrap="@NoWrap"
            class="@Css()"
            @attributes="Splat">@ChildContent</pk-cluster>

@code {
    /// <summary>Row (wraps at the edge) or column.</summary>
    [Parameter] public string? Direction { get; set; }

    /// <summary>Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default sm (the control gap).</summary>
    [Parameter] public string? Gap { get; set; }

    /// <summary>Cross-axis alignment of the items.</summary>
    [Parameter] public string? Align { get; set; }

    /// <summary>Main-axis distribution of the items.</summary>
    [Parameter] public string? Justify { get; set; }

    /// <summary>Keep a horizontal cluster on one line instead of wrapping (wrapping is the default).</summary>
    [Parameter] public bool NoWrap { get; set; }

    /// <summary>The items to lay out.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
