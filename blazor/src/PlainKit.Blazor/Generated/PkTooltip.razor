@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/tooltip.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-tooltip text="@Text"
            placement="@(Placement.ToAttr())"
            delay="@PkAttr.Num(HoverDelay)"
            help="@Help"
            enrich="@Enrich"
            heading="@Title"
            class="@Css()"
            @attributes="Splat">@if (TooltipContent is not null) {<span slot="content">@TooltipContent</span>}@if (LinksContent is not null) {<span slot="links">@LinksContent</span>}@ChildContent</pk-tooltip>

@code {
    /// <summary>The label.</summary>
    [Parameter] public string? Text { get; set; }

    /// <summary>Rich tip content in place of text.</summary>
    [Parameter] public RenderFragment? TooltipContent { get; set; }

    /// <summary>Links shown under the panel text (a doc link, a more link).</summary>
    [Parameter] public RenderFragment? LinksContent { get; set; }

    /// <summary>The element the tip describes.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>Preferred side; flips when it does not fit.</summary>
    [Parameter] public PkTooltipPlacement Placement { get; set; } = PkTooltipPlacement.Top;

    /// <summary>Mouse hover delay; focus is immediate.</summary>
    [Parameter] public int HoverDelay { get; set; } = 200;

    /// <summary>Draws its own info button as the target (the default slot then holds text that follows it).</summary>
    [Parameter] public bool Help { get; set; }

    /// <summary>Underlines the wrapped value with a dotted line to show it has context.</summary>
    [Parameter] public bool Enrich { get; set; }

    /// <summary>A bold heading inside the panel.</summary>
    [Parameter] public string? Title { get; set; }
}
