@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/stepper.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-stepper current="@PkAttr.Num(Current)"
            orientation="@(Orientation.ToAttr())"
            clickable="@Clickable"
            free="@Free"
            errors="@Errors"
            label="@Label"
            @attributes="Splat">@ChildContent</pk-stepper>

@code {
    /// <summary>Zero-based current step.</summary>
    [Parameter] public int Current { get; set; } = 0;

    /// <summary>Raised when Current changes (two-way binding: &lt;c&gt;@bind-Current&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<int> CurrentChanged { get; set; }

    /// <summary>The orientation setting</summary>
    [Parameter] public StepperOrientation Orientation { get; set; } = StepperOrientation.Horizontal;

    /// <summary>Steps can be entered by click or key.</summary>
    [Parameter] public bool Clickable { get; set; }

    /// <summary>Allow skipping ahead of the furthest step reached (non-linear).</summary>
    [Parameter] public bool Free { get; set; }

    /// <summary>Comma-separated step indexes with an error.</summary>
    [Parameter] public string? Errors { get; set; } = "";

    /// <summary>Accessible name.</summary>
    [Parameter] public string? Label { get; set; } = "Progress";

    /// <summary>pk-step children.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>A step was entered; cancelable.</summary>
    [Parameter] public EventCallback<PkStepChangeEventArgs> OnStepChange { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-step-change"] = EventCallback.Factory.Create<PkStepChangeEventArgs>(this, HandlePkStepChange),
        };
    }

    private async Task HandlePkStepChange(PkStepChangeEventArgs e)
    {
        Current = (int)(e.Index ?? 0);
        await CurrentChanged.InvokeAsync(Current);
        await OnStepChange.InvokeAsync(e);
    }
}
