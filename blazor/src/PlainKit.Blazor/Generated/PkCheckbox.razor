@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/checkbox.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-checkbox checked="@Checked"
             disabled="@Disabled"
             required="@Required"
             invalid="@IsInvalid"
             indeterminate="@Indeterminate"
             group="@Group"
             master="@Master"
             label="@Label"
             size="@Size"
             class="@Css()"
             @attributes="Splat">@ChildContent</pk-checkbox>

@code {
    /// <summary>Whether the box is checked.</summary>
    [Parameter] public bool Checked { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The form cannot be submitted while the value is empty.</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>The mixed state (some of a group are checked).</summary>
    [Parameter] public bool Indeterminate { get; set; }

    /// <summary>Puts this checkbox in a select-all group of that name.</summary>
    [Parameter] public string? Group { get; set; }

    /// <summary>Makes this the select-all checkbox of the group of that name; it shows checked, unchecked or indeterminate as its members change.</summary>
    [Parameter] public string? Master { get; set; }

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>The label.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>sm is the compact checkbox of a toolbar: smaller label text.</summary>
    [Parameter] public string? Size { get; set; }

    /// <summary>Raised when Checked changes (two-way binding: &lt;c&gt;@bind-Checked&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<bool> CheckedChanged { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-change"] = EventCallback.Factory.Create<PkChangeEventArgs>(this, HandlePkChange);
    }

    private async Task HandlePkChange(PkChangeEventArgs e)
    {
        Checked = e.Checked == true;
        await CheckedChanged.InvokeAsync(Checked);
    }
}
