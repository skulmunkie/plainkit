@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/progress.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-progress value="@PkAttr.Num(Value)"
             max="@PkAttr.Num(Max)"
             variant="@Variant"
             size="@Size"
             striped="@Striped"
             indeterminate="@Indeterminate"
             label="@Label"
             show-value="@ShowValue"
             level-thresholds="@LevelThresholds"></pk-progress>

@code {
    /// <summary>Current value.</summary>
    [Parameter] public double Value { get; set; } = 0;

    /// <summary>Upper bound.</summary>
    [Parameter] public double Max { get; set; } = 100;

    /// <summary>Colour.</summary>
    [Parameter] public string? Variant { get; set; }

    /// <summary>Height.</summary>
    [Parameter] public string? Size { get; set; }

    /// <summary>Diagonal stripes.</summary>
    [Parameter] public bool Striped { get; set; }

    /// <summary>Unknown duration: the bar sweeps.</summary>
    [Parameter] public bool Indeterminate { get; set; }

    /// <summary>Text above the bar; also its accessible name.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>Append the percentage next to the label.</summary>
    [Parameter] public bool ShowValue { get; set; }

    /// <summary>Two percentages, for example "70,90": the bar turns warn then danger.</summary>
    [Parameter] public string? LevelThresholds { get; set; }
}
