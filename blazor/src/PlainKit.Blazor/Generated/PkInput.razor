@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/input.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-input value="@Value"
          type="@Type"
          name="@Name"
          placeholder="@Placeholder"
          label="@AriaLabel"
          disabled="@Disabled"
          readonly="@ReadOnly"
          required="@Required"
          invalid="@IsInvalid"
          min="@Min"
          max="@Max"
          step="@Step"
          maxlength="@MaxLength"
          pattern="@Pattern"
          autocomplete="@AutoComplete"
          size="@Size"
          clearable="@Clearable"
          reveal="@Revealable"
          stepper="@Stepper"
          format="@Format"
          currency="@Currency"
          floating="@Floating"
          show-label="@ShowLabel"
          debounce="@PkAttr.Num(DebounceMs)"
          @attributes="Splat">@if (PrefixContent is not null) {<span slot="prefix">@PrefixContent</span>}@if (SuffixContent is not null) {<span slot="suffix">@SuffixContent</span>}</pk-input>

@code {
    /// <summary>The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>The kind of value. Date and time types open the platform picker.</summary>
    [Parameter] public string? Type { get; set; }

    /// <summary>The form field name.</summary>
    [Parameter] public string? Name { get; set; }

    /// <summary>Hint shown while empty (ignored by a floating label).</summary>
    [Parameter] public string? Placeholder { get; set; }

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? AriaLabel { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The value can be read and selected but not edited.</summary>
    [Parameter] public bool ReadOnly { get; set; }

    /// <summary>The form cannot be submitted while the value is empty.</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>Minimum value (number and date types).</summary>
    [Parameter] public string? Min { get; set; }

    /// <summary>Maximum value (number and date types).</summary>
    [Parameter] public string? Max { get; set; }

    /// <summary>Step for number and date types; also the stepper increment.</summary>
    [Parameter] public string? Step { get; set; }

    /// <summary>Maximum text length.</summary>
    [Parameter] public string? MaxLength { get; set; }

    /// <summary>A regular expression the whole value must match.</summary>
    [Parameter] public string? Pattern { get; set; }

    /// <summary>The autofill token, for example current-password, new-password or one-time-code.</summary>
    [Parameter] public string? AutoComplete { get; set; }

    /// <summary>Control height.</summary>
    [Parameter] public string? Size { get; set; }

    /// <summary>Shows a clear button while there is a value.</summary>
    [Parameter] public bool Clearable { get; set; }

    /// <summary>Password only: a show and hide button.</summary>
    [Parameter] public bool Revealable { get; set; }

    /// <summary>Number only: minus and plus buttons that respect min, max and step.</summary>
    [Parameter] public bool Stepper { get; set; }

    /// <summary>money formats the value as an amount (1,234.50) when the field is left and shows the plain number while editing; value stays the plain number.</summary>
    [Parameter] public string? Format { get; set; }

    /// <summary>ISO currency code whose symbol leads a money field.</summary>
    [Parameter] public string? Currency { get; set; }

    /// <summary>Shows label inside the field until it has focus or a value, then floats it to the top edge.</summary>
    [Parameter] public bool Floating { get; set; }

    /// <summary>Shows label as visible text above the field (linked to it), for use without a pk-field. Ignored by a floating label.</summary>
    [Parameter] public bool ShowLabel { get; set; }

    /// <summary>Search only: milliseconds to wait after typing before pk-search fires; 0 fires on every change.</summary>
    [Parameter] public int DebounceMs { get; set; } = 0;

    /// <summary>Search type: the debounced search text, or immediately on Enter and clear.</summary>
    [Parameter] public EventCallback<PkSearchEventArgs> OnSearch { get; set; }

    /// <summary>A leading add-on: a currency sign, an icon, a unit.</summary>
    [Parameter] public RenderFragment? PrefixContent { get; set; }

    /// <summary>A trailing add-on: a unit, a domain, a button.</summary>
    [Parameter] public RenderFragment? SuffixContent { get; set; }

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-search"] = EventCallback.Factory.Create<PkSearchEventArgs>(this, HandlePkSearch),
            ["onpk-value-change"] = EventCallback.Factory.Create<PkValueChangeEventArgs>(this, HandlePkValueChange),
        };
    }

    private async Task HandlePkSearch(PkSearchEventArgs e)
    {
        await OnSearch.InvokeAsync(e);
    }

    private async Task HandlePkValueChange(PkValueChangeEventArgs e)
    {
        Value = e.Value;
        await ValueChanged.InvokeAsync(Value);
    }
}
