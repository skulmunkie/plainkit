@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/chart.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-chart kind="@Kind"
          caption="@Caption"
          height="@PkAttr.Num(Height)"
          data="@PkAttr.Json(Data)">@if (CaptionContent is not null) {<span slot="caption">@CaptionContent</span>}@ChildContent</pk-chart>

@code {
    /// <summary>Chart type.</summary>
    [Parameter] public string? Kind { get; set; }

    /// <summary>Title (figcaption).</summary>
    [Parameter] public string? Caption { get; set; }

    /// <summary>viewBox height of bar and line charts.</summary>
    [Parameter] public int Height { get; set; } = 140;

    /// <summary>Data as a property instead of a table: { labels: string[], series: { name, values: number[] }[] }.</summary>
    /// <remarks>Pass a PkChartData: { Labels, Series: [{ Name, Values }] }. It is sent as a JSON attribute (camelCase) and replaces the slotted table.</remarks>
    [Parameter] public object? Data { get; set; }

    /// <summary>A rich caption; overrides caption.</summary>
    [Parameter] public RenderFragment? CaptionContent { get; set; }

    /// <summary>A table: the first column holds labels, each further column a series. A cell may carry data-value.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
