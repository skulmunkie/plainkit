@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/alert.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-alert kind="@(Kind.ToAttr())"
          heading="@Title"
          dismissible="@Dismissible"
          plain="@(!Boxed)"
          inline="@Inline"
          compact="@Compact"
          class="@Css(ExtraClass)"
          @attributes="Splat">@Message@ChildContent</pk-alert>

@code {
    /// <summary>The kind setting</summary>
    [Parameter] public PkAlertKind Kind { get; set; } = PkAlertKind.Error;

    /// <summary>The heading setting</summary>
    [Parameter] public string? Title { get; set; }

    /// <summary>Message.</summary>
    [Parameter] public string? Message { get; set; }

    /// <summary>Message.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The dismissible setting</summary>
    [Parameter] public bool Dismissible { get; set; }

    /// <summary>Dismissed; cancelable.</summary>
    [Parameter] public EventCallback OnDismiss { get; set; }

    /// <summary>Coloured text with no box, icon or panel: the quiet form for a message beside a field or under a button.</summary>
    /// <remarks>False is the plain form (coloured text, no box); the parameter is the opposite of the plain prop.</remarks>
    [Parameter] public bool Boxed { get; set; } = true;

    /// <summary>Flows in a line of text (like a span) instead of a block; use with plain.</summary>
    [Parameter] public bool Inline { get; set; }

    /// <summary>Smaller text and no margin, for an actions strip.</summary>
    [Parameter] public bool Compact { get; set; }

    /// <summary>Extra CSS classes for the element.</summary>
    [Parameter] public string? ExtraClass { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-dismiss"] = EventCallback.Factory.Create<EventArgs>(this, HandlePkDismiss);
    }

    private async Task HandlePkDismiss(EventArgs e)
    {
        await OnDismiss.InvokeAsync();
    }
}
