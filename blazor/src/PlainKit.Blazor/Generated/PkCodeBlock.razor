@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/code-block.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-code-block label="@Label"
               line-numbers="@LineNumbers"
               wrap="@Wrap"
               no-copy="@NoCopy"
               max-height="@MaxHeight"
               class="@Css()"
               @attributes="Splat">@ChildContent</pk-code-block>

@code {
    /// <summary>File or language shown in the bar; also the region's name.</summary>
    [Parameter] public string? Label { get; set; }

    /// <summary>A gutter of line numbers.</summary>
    [Parameter] public bool LineNumbers { get; set; }

    /// <summary>Wrap long lines.</summary>
    [Parameter] public bool Wrap { get; set; }

    /// <summary>Hide the copy button.</summary>
    [Parameter] public bool NoCopy { get; set; }

    /// <summary>Cap the height, any CSS length.</summary>
    [Parameter] public string? MaxHeight { get; set; }

    /// <summary>The wrap toggle was pressed; wrap already has the new value.</summary>
    [Parameter] public EventCallback<PkWrapChangeEventArgs> OnWrapChange { get; set; }

    /// <summary>After a copy attempt.</summary>
    [Parameter] public EventCallback<PkCopyEventArgs> OnCopy { get; set; }

    /// <summary>The content.</summary>
    [Parameter] public string? ChildContent { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-wrap-change"] = EventCallback.Factory.Create<PkWrapChangeEventArgs>(this, HandlePkWrapChange);
        handlers["onpk-copy"] = EventCallback.Factory.Create<PkCopyEventArgs>(this, HandlePkCopy);
    }

    private async Task HandlePkWrapChange(PkWrapChangeEventArgs e)
    {
        await OnWrapChange.InvokeAsync(e);
    }

    private async Task HandlePkCopy(PkCopyEventArgs e)
    {
        await OnCopy.InvokeAsync(e);
    }
}
