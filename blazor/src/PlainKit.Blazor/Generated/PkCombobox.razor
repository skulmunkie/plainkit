@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/combobox.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-combobox value="@Value"
             placeholder="@Placeholder"
             disabled="@Disabled"
             required="@Required"
             invalid="@IsInvalid"
             mode="@Mode"
             free="@Free"
             filtering="@Filtering"
             label="@AriaLabel"
             @attributes="Splat">@ChildContent@if (EmptyContent is not null) {<span slot="empty">@EmptyContent</span>}</pk-combobox>

@code {
    /// <summary>The current value; what the form submits.</summary>
    [Parameter] public string? Value { get; set; }

    /// <summary>Hint shown while empty.</summary>
    [Parameter] public string? Placeholder { get; set; }

    /// <summary>Blocks interaction; also set by a disabled fieldset.</summary>
    [Parameter] public bool Disabled { get; set; }

    /// <summary>The form cannot be submitted while the value is empty.</summary>
    [Parameter] public bool Required { get; set; }

    /// <summary>Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext).</summary>
    [Parameter] public bool IsInvalid { get; set; }

    /// <summary>autocomplete filters as you type; select is a button that opens the list (type a letter to jump). Prefer the native pk-select on a phone.</summary>
    [Parameter] public string? Mode { get; set; }

    /// <summary>Autocomplete: keep typed text that is not an option, and use it as the value.</summary>
    [Parameter] public bool Free { get; set; }

    /// <summary>client also filters the options here; off leaves filtering to the host, which listens for pk-combo-query and replaces the options.</summary>
    [Parameter] public string? Filtering { get; set; }

    /// <summary>The accessible name (aria-label). pk-field fills it from its own label when this is empty.</summary>
    [Parameter] public string? AriaLabel { get; set; }

    /// <summary>The user typed: the text so far, so a host can fetch more options (with filtering="off" it also replaces the client filter).</summary>
    [Parameter] public EventCallback<PkComboQueryEventArgs> OnSearchInput { get; set; }

    /// <summary>option elements (value, disabled and text).</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <summary>The message shown when nothing matches.</summary>
    [Parameter] public RenderFragment? EmptyContent { get; set; }

    /// <summary>Raised when Value changes (two-way binding: &lt;c&gt;@bind-Value&lt;/c&gt;).</summary>
    [Parameter] public EventCallback<string?> ValueChanged { get; set; }

    private Dictionary<string, object> Splat { get; set; } = new();

    /// <inheritdoc />
    protected override void OnParametersSet()
    {
        Splat = new Dictionary<string, object>
        {
            ["onpk-combo-query"] = EventCallback.Factory.Create<PkComboQueryEventArgs>(this, HandlePkComboQuery),
            ["onpk-combo-select"] = EventCallback.Factory.Create<PkComboSelectEventArgs>(this, HandlePkComboSelect),
        };
    }

    private async Task HandlePkComboQuery(PkComboQueryEventArgs e)
    {
        await OnSearchInput.InvokeAsync(e);
    }

    private async Task HandlePkComboSelect(PkComboSelectEventArgs e)
    {
        Value = e.Value;
        await ValueChanged.InvokeAsync(Value);
    }
}
