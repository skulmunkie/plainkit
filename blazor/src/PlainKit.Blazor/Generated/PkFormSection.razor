@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/form-section.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-form-section heading="@Title"
                 description="@Description"
                 class="@Css()"
                 @attributes="Splat">@ChildContent</pk-form-section>

@code {
    /// <summary>The section heading (or use the heading slot).</summary>
    [Parameter] public string? Title { get; set; }

    /// <summary>A sentence under the heading.</summary>
    [Parameter] public string? Description { get; set; }

    /// <summary>The fields. Give a child class form-span to make it span the full width.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
