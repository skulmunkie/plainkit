@* Generated by scripts/generate-blazor.mjs from core/dist/elements/api.json and blazor/mappings/form.json. Do not edit: change the mapping or the SDK and run it again. *@
@namespace PlainKit.Blazor
@using Microsoft.AspNetCore.Components.Web
@inherits PkElementBase

<pk-form validate="@Validate"
         show-valid="@ShowValid"
         summary="@Summary"
         class="@Css()"
         @attributes="Splat">@ChildContent</pk-form>

@code {
    /// <summary>When to check: blur (after leaving a field, and on submit), input (while typing) or submit (only on submit). A field already showing an error re-checks as you type.</summary>
    [Parameter] public string? Validate { get; set; }

    /// <summary>Also shows the valid state on fields that hold a value.</summary>
    [Parameter] public bool ShowValid { get; set; }

    /// <summary>Shows a summary of the problems with links to the fields.</summary>
    [Parameter] public bool Summary { get; set; }

    /// <summary>A submit was stopped because these controls are invalid.</summary>
    [Parameter] public EventCallback<PkInvalidEventArgs> OnInvalid { get; set; }

    /// <summary>A submit passed validation.</summary>
    [Parameter] public EventCallback OnValid { get; set; }

    /// <summary>The form was reset and every control has its initial value again. A reset raises no input or change event on the controls (as with native controls), so a host that mirrors their values reads them again here. Not raised when the reset was cancelled.</summary>
    [Parameter] public EventCallback OnReset { get; set; }

    /// <summary>A form element.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    /// <inheritdoc />
    protected override void AddEventHandlers(Dictionary<string, object> handlers)
    {
        handlers["onpk-invalid"] = EventCallback.Factory.Create<PkInvalidEventArgs>(this, HandlePkInvalid);
        handlers["onpk-valid"] = EventCallback.Factory.Create<EventArgs>(this, HandlePkValid);
        handlers["onpk-reset"] = EventCallback.Factory.Create<EventArgs>(this, HandlePkReset);
    }

    private async Task HandlePkInvalid(PkInvalidEventArgs e)
    {
        await OnInvalid.InvokeAsync(e);
    }

    private async Task HandlePkValid(EventArgs e)
    {
        await OnValid.InvokeAsync();
    }

    private async Task HandlePkReset(EventArgs e)
    {
        await OnReset.InvokeAsync();
    }
}
