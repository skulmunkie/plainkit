# Components: Form layout

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from Generated/*.razor, blazor/mappings, generated.manifest.json and core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every component renders its element with the parameters you set as attributes. An attribute that is not a parameter (`id`, `class`, `data-*`, `aria-*`, ...) is put on the element as it is, and a `class` is added to the component's own (`ExtraClass` on a component that lists it). Names are `Pk` plus the tag in PascalCase.

## `PkField`

`<pk-field>`: **Field** (Form layout). The label, help, error, warning and counter around a form control, with the wiring between them. It hands its label and messages to the control, because ids do not cross shadow roots.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Label` | `string?` |  | attribute `label` | The label text (or use the label slot). |
| `Hint` | `string?` |  | attribute `help` | Help text under the control. |
| `Error` | `string?` |  | attribute `error` | An error message; puts the control into the invalid state. |
| `Required` | `bool` |  | attribute `required` | Marks the field required (a star, and required on the control). |
| `MaxLength` | `int` |  | attribute `max` | Counter maximum: shows length / max and turns warm from 90% and red past the max; 0 hides the counter. |
| `Layout` | `string?` |  | attribute `layout` | stack puts the label above the control; row puts it in a fixed column beside it (stacks on a phone); inline puts label and control on one line at their natural width. |
| `ChildContent` | `RenderFragment?` |  | default slot | The control: an pk-* form control or a native input, select or textarea. |
| `LabelExtra` | `RenderFragment?` |  | slot `label` | A rich label (overrides the label prop). |
| `LabelAction` | `RenderFragment?` |  | slot `label-action` | A small action beside the label, such as a reset to source button (a pk-button size mini, variant ghost). Clicking it does not focus the control. |
| `HintContent` | `RenderFragment?` |  | slot `help` | Rich help text (overrides the help prop). |
| `Size` | `string?` |  | attribute `size` | sm is the compact field of a toolbar or filter row: smaller label, tighter gap. |

When you set a named fragment (`LabelExtra`, `LabelAction`, `HintContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-field` in the `plainkit-sdk` skill.

## `PkFieldRow`

`<pk-field-row>`: **Field row** (Form layout). A wrapping row of fields that share the width: each field grows from a 120px minimum, a wide field takes a row of its own.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Align` | `string?` |  | attribute `align` | Vertical alignment of the fields in the row. |
| `ChildContent` | `RenderFragment?` |  | default slot | The fields, checkboxes or switches. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-field-row` in the `plainkit-sdk` skill.

## `PkForm`

`<pk-form>`: **Form validation** (Form layout). Wraps a form and shows the browser's constraint validation well: messages in each field, a summary with links, focus on the first problem, and live re-checking.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Validate` | `string?` |  | attribute `validate` | When to check: blur (after leaving a field, and on submit), input (while typing) or submit (only on submit). A field already showing an error re-checks as you type. |
| `ShowValid` | `bool` |  | attribute `show-valid` | Also shows the valid state on fields that hold a value. |
| `Summary` | `bool` |  | attribute `summary` | Shows a summary of the problems with links to the fields. |
| `OnInvalid` | `EventCallback<PkInvalidEventArgs>` |  | event `pk-invalid` | A submit was stopped because these controls are invalid. |
| `OnValid` | `EventCallback` |  | event `pk-valid` | A submit passed validation. |
| `ChildContent` | `RenderFragment?` |  | default slot | A form element. |

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-invalid` | `PkInvalidEventArgs` | Count: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-form` in the `plainkit-sdk` skill.

## `PkFormActions`

`<pk-form-actions>`: **Form actions** (Form layout). The bar of buttons under a form, aligned start, end or between, and sticky to the bottom of a long form.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Sticky` | `bool` |  | attribute `sticky` | Sticks to the bottom of the scrolling area with a top edge, clearing the phone home indicator. |
| `Align` | `string?` |  | attribute `align` | Where the buttons sit. |
| `ChildContent` | `RenderFragment?` |  | default slot | The buttons. |
| `StatusContent` | `RenderFragment?` |  | slot `status` | A status message such as "Unsaved changes". |

When you set a named fragment (`StatusContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-form-actions` in the `plainkit-sdk` skill.

## `PkFormSection`

`<pk-form-section>`: **Form section** (Form layout). A titled section of a form: heading and description beside a responsive grid of fields (stacked below 1024px).

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Title` | `string?` |  | attribute `heading` | The section heading (or use the heading slot). |
| `Description` | `string?` |  | attribute `description` | A sentence under the heading. |
| `ChildContent` | `RenderFragment?` |  | default slot | The fields. Give a child class form-span to make it span the full width. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-form-section` in the `plainkit-sdk` skill.
