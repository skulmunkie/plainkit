# Components: Form controls

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from Generated/*.razor, blazor/mappings, generated.manifest.json and core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every generated component also renders its element with the attributes you set; it accepts only the parameters listed (an unlisted attribute such as `class` throws when the component renders, so put classes on a wrapping element). Names are `Pk` plus the tag in PascalCase.

## `PkCalendar`

`<pk-calendar>`: **Calendar** (Forms). A small month grid to pick or show a date, with marks, minimum and maximum, and full keyboard control.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `DateOnly?` |  | attribute `value` | The selected day, an ISO date such as 2026-09-19. |
| `ValueChanged` | `EventCallback<DateOnly?>` |  | two-way pair of `Value` | Raised with the new value of Value (two-way: Value follows it). |
| `Month` | `DateOnly?` |  | attribute `month` | An ISO date whose month is shown; defaults to the value or today. |
| `Min` | `DateOnly?` |  | attribute `min` | Earliest selectable ISO date. |
| `Max` | `DateOnly?` |  | attribute `max` | Latest selectable ISO date. |
| `WeekStart` | `int` |  | attribute `week-start` | First weekday: 0 Sunday, 1 Monday. |
| `Locale` | `string?` |  | attribute `locale` | Language of month and weekday names; defaults to the page language. |
| `Marks` | `IReadOnlyList<DateOnly>?` |  | attribute `marks` | ISO dates that get a dot. |
| `ReadOnly` | `bool` |  | attribute `readonly` | Show only; no selection. |
| `Disabled` | `bool` |  | attribute `disabled` | Inert. |
| `OnMonthChanged` | `EventCallback<PkMonthEventArgs>` |  | event `pk-month` | The shown month changed (yyyy-mm). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-month` | `PkMonthEventArgs` | Month: string? |
| `pk-select` | `PkSelectEventArgs` | Value: string?, Item: JsonElement?, Checked: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-calendar` in the `plainkit-sdk` skill.

## `PkCheckbox`

`<pk-checkbox>`: **Checkbox** (Form controls). A checkbox drawn from tokens, with an indeterminate state and select-all groups: a master checkbox mirrors every checkbox of its group.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Checked` | `bool` |  | attribute `checked` | Whether the box is checked. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Indeterminate` | `bool` |  | attribute `indeterminate` | The mixed state (some of a group are checked). |
| `Group` | `string?` |  | attribute `group` | Puts this checkbox in a select-all group of that name. |
| `Master` | `string?` |  | attribute `master` | Makes this the select-all checkbox of the group of that name; it shows checked, unchecked or indeterminate as its members change. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `ChildContent` | `RenderFragment?` |  | default slot | The label. |
| `Size` | `string?` |  | attribute `size` | sm is the compact checkbox of a toolbar: smaller label text. |
| `CheckedChanged` | `EventCallback<bool>` |  | two-way pair of `Checked` | Raised when Checked changes (two-way binding: `@bind-Checked`). |

Two-way binding: `@bind-Checked`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-change` | `PkChangeEventArgs` | Checked: bool?, Index: double?, Value: string?, Label: string?, Previous: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-checkbox` in the `plainkit-sdk` skill.

## `PkColourInput`

`<pk-colour-input>`: **Colour input** (Form controls). The native colour picker beside an editable hex field that stay in step; the field accepts #abc, abc and #aabbcc and is normalised to #rrggbb.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The colour as #rrggbb. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `ShowLabel` | `bool` |  | attribute `show-label` | Shows label as visible text above the field (linked to it), for use without a pk-field. |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-colour` | `PkColourEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-colour-input` in the `plainkit-sdk` skill.

## `PkCombobox`

`<pk-combobox>`: **Combobox** (Form controls). An autocomplete (type to filter a list) or, with mode="select", a custom select. ARIA combobox with the highlight tracked by aria-activedescendant, so focus stays on the control. Its options are its own option children.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The current value; what the form submits. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Hint shown while empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Mode` | `string?` |  | attribute `mode` | autocomplete filters as you type; select is a button that opens the list (type a letter to jump). Prefer the native pk-select on a phone. |
| `Free` | `bool` |  | attribute `free` | Autocomplete: keep typed text that is not an option, and use it as the value. |
| `Filtering` | `string?` |  | attribute `filtering` | client also filters the options here; off leaves filtering to the host, which listens for pk-combo-query and replaces the options. |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `OnSearchInput` | `EventCallback<PkComboQueryEventArgs>` |  | event `pk-combo-query` | The user typed: the text so far, so a host can fetch more options (with filtering="off" it also replaces the client filter). |
| `ChildContent` | `RenderFragment?` |  | default slot | option elements (value, disabled and text). |
| `EmptyContent` | `RenderFragment?` |  | slot `empty` | The message shown when nothing matches. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

When you set a named fragment (`EmptyContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-combo-query` | `PkComboQueryEventArgs` | Query: string? |
| `pk-combo-select` | `PkComboSelectEventArgs` | Value: string?, Label: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-combobox` in the `plainkit-sdk` skill.

## `PkDropzone`

`<pk-dropzone>`: **Dropzone** (Form controls). A drop target over a real file input: drag files in or click, with type, size and count checks, a list of accepted files with Remove, and a reason for each rejection. Files are submitted with the form.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Accept` | `string?` |  | attribute `accept` | Accepted types: extensions, MIME types or type/* (like the accept attribute). |
| `Multiple` | `bool` |  | attribute `multiple` | Allows several files. |
| `MaxFileSizeBytes` | `string?` |  | attribute `max-size` | Largest file: 500KB, 5MB, 1GB or bytes. |
| `MaxFiles` | `int` |  | attribute `max-files` | Most files (multiple); 0 means no limit. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `OnFiles` | `EventCallback<PkFilesEventArgs>` |  | event `pk-files` | Files were picked or dropped: those kept and those refused with a reason. |
| `ChildContent` | `RenderFragment?` |  | default slot | The zone title. |
| `HintContent` | `RenderFragment?` |  | slot `hint` | A hint under the title (types and limits). |

When you set a named fragment (`HintContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-files` | `PkFilesEventArgs` | Count: double?, RejectedCount: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-dropzone` in the `plainkit-sdk` skill.

## `PkInput`

`<pk-input>`: **Input** (Form controls). The single-line control: text, email, url, tel, search, password, number and the date and time types, with an optional clear button, password reveal, number stepper, money format, prefix and suffix, and a floating label.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The current value; what the form submits. |
| `Type` | `string?` |  | attribute `type` | The kind of value. Date and time types open the platform picker. |
| `Name` | `string?` |  | attribute `name` | The form field name. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Hint shown while empty (ignored by a floating label). |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `ReadOnly` | `bool` |  | attribute `readonly` | The value can be read and selected but not edited. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Min` | `string?` |  | attribute `min` | Minimum value (number and date types). |
| `Max` | `string?` |  | attribute `max` | Maximum value (number and date types). |
| `Step` | `string?` |  | attribute `step` | Step for number and date types; also the stepper increment. |
| `MaxLength` | `string?` |  | attribute `maxlength` | Maximum text length. |
| `Pattern` | `string?` |  | attribute `pattern` | A regular expression the whole value must match. |
| `AutoComplete` | `string?` |  | attribute `autocomplete` | The autofill token, for example current-password, new-password or one-time-code. |
| `Size` | `string?` |  | attribute `size` | Control height. |
| `Clearable` | `bool` |  | attribute `clearable` | Shows a clear button while there is a value. |
| `Revealable` | `bool` |  | attribute `reveal` | Password only: a show and hide button. |
| `Stepper` | `bool` |  | attribute `stepper` | Number only: minus and plus buttons that respect min, max and step. |
| `Format` | `string?` |  | attribute `format` | money formats the value as an amount (1,234.50) when the field is left and shows the plain number while editing; value stays the plain number. |
| `Currency` | `string?` |  | attribute `currency` | ISO currency code whose symbol leads a money field. |
| `Floating` | `bool` |  | attribute `floating` | Shows label inside the field until it has focus or a value, then floats it to the top edge. |
| `ShowLabel` | `bool` |  | attribute `show-label` | Shows label as visible text above the field (linked to it), for use without a pk-field. Ignored by a floating label. |
| `DebounceMs` | `int` |  | attribute `debounce` | Search only: milliseconds to wait after typing before pk-search fires; 0 fires on every change. |
| `OnSearch` | `EventCallback<PkSearchEventArgs>` |  | event `pk-search` | Search type: the debounced search text, or immediately on Enter and clear. |
| `PrefixContent` | `RenderFragment?` |  | slot `prefix` | A leading add-on: a currency sign, an icon, a unit. |
| `SuffixContent` | `RenderFragment?` |  | slot `suffix` | A trailing add-on: a unit, a domain, a button. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-search` | `PkSearchEventArgs` | Value: string? |
| `pk-value-change` | `PkValueChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-input` in the `plainkit-sdk` skill.

## `PkOtpInput`

`<pk-otp-input>`: **OTP input** (Form controls). A one-time code or PIN as a row of cells: typing advances, Backspace steps back, a pasted or autofilled code spreads across the cells, and completion is an event.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The code typed so far. |
| `Length` | `int` |  | attribute `length` | Number of cells. |
| `Type` | `string?` |  | attribute `type` | The alphabet: digits, letters and digits, or anything. |
| `SeparatorAt` | `int` |  | attribute `separator-at` | Puts a dash after this many cells (0 for none). |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `OnComplete` | `EventCallback<PkOtpCompleteEventArgs>` |  | event `pk-otp-complete` | Every cell is filled. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-otp-change` | `PkOtpChangeEventArgs` | Value: string? |
| `pk-otp-complete` | `PkOtpCompleteEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-otp-input` in the `plainkit-sdk` skill.

## `PkRadioGroup`

`<pk-radio-group>`: **Radio group** (Form controls). One choice from a few options, as radios or as a segmented bar. The options are its own option children; the radios are native inputs, so arrow keys and grouping are the browser's.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The current value; what the form submits. |
| `Name` | `string?` |  | attribute `name` | The form field name. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Variant` | `string?` |  | attribute `variant` | radio draws a list of radios; segmented draws one bar of segments. |
| `Direction` | `string?` |  | attribute `direction` | Radio variant: stacked or in a wrapping row. |
| `Fill` | `bool` |  | attribute `fill` | Segmented variant: spread the segments across the full width. |
| `ChildContent` | `RenderFragment?` |  | default slot | option elements (value, disabled and text). |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-value-change` | `PkValueChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-radio-group` in the `plainkit-sdk` skill.

## `PkRange`

`<pk-range>`: **Range** (Form controls). A slider, or with dual a minimum and maximum slider, drawn from tokens with a live value output.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `double` |  | attribute `value` | Single mode: the value. |
| `ValueLow` | `double` |  | attribute `value-low` | Dual mode: the low value. |
| `ValueHigh` | `double` |  | attribute `value-high` | Dual mode: the high value. |
| `Min` | `double` |  | attribute `min` | Lowest value. |
| `Max` | `double` |  | attribute `max` | Highest value. |
| `Step` | `double` |  | attribute `step` | Increment. |
| `Dual` | `bool` |  | attribute `dual` | Two thumbs: a low and a high value; the low thumb never passes the high one. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `ShowOutput` | `bool` |  | attribute `output` | Shows the current value under the slider. |
| `ValueChanged` | `EventCallback<double>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-range` | `PkRangeEventArgs` | Value: double?, Low: double?, High: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-range` in the `plainkit-sdk` skill.

## `PkRating`

`<pk-rating>`: **Rating** (Form controls). A whole-star rating input (radios underneath), and a compact read-only display form.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `int` |  | attribute `value` | The rating, 0 for none. |
| `Max` | `int` |  | attribute `max` | Number of stars. |
| `ReadOnly` | `bool` |  | attribute `readonly` | The display form: not interactive, exposed as an image with a text alternative. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `ValueChanged` | `EventCallback<int>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-rating-change` | `PkRatingChangeEventArgs` | Value: double? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-rating` in the `plainkit-sdk` skill.

## `PkSelect`

`<pk-select>`: **Select** (Form controls). The browser select in the same skin as the text input. Its options are its own option and optgroup children; a phone shows its native picker.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The current value; what the form submits. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `Label` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Multiple` | `bool` |  | attribute `multiple` | Allows several selections (value is comma-separated; the form submits one entry each). |
| `ChildContent` | `RenderFragment?` |  | default slot | option and optgroup elements. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-value-change` | `PkValueChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-select` in the `plainkit-sdk` skill.

## `PkSelectMenu`

`<pk-select-menu>`: **Select menu** (Forms & inputs). A single-choice listbox popup that reads native option children, so it degrades to nothing worse than a list; form-associated, keyboard driven with typeahead.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | Selected option value. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |
| `Name` | `string?` |  | attribute `name` | Form field name. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Shown with no selection. |
| `Open` | `bool` |  | attribute `open` | Whether the list is shown. |
| `Disabled` | `bool` |  | attribute `disabled` | Prevents interaction |
| `Required` | `bool` |  | attribute `required` | A value is required |
| `ChildContent` | `RenderFragment?` |  | default slot | option elements. |
| `OnChange` | `EventCallback<PkChangeEventArgs>` |  | event `pk-change` | The selection changed. |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-change` | `PkChangeEventArgs` | Checked: bool?, Index: double?, Value: string?, Label: string?, Previous: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-select-menu` in the `plainkit-sdk` skill.

## `PkSwitch`

`<pk-switch>`: **Switch** (Form controls). An on/off toggle that takes part in a form: it submits its value when on and resets with the form.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Checked` | `bool` |  | attribute `checked` | Whether the switch is on. |
| `CheckedChanged` | `EventCallback<bool>` |  | two-way pair of `Checked` | Raised with the new value of Checked (two-way: Checked follows it). |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Name` | `string?` |  | attribute `name` | The form field name. |
| `Value` | `string?` |  | attribute `value` | The value submitted while checked. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state (a red ring) and sets aria-invalid. |
| `Size` | `string?` |  | attribute `size` | Track size. |
| `LabelPosition` | `string?` |  | attribute `label-position` | end puts the label after the switch, start before it, between pushes them to opposite edges of the row. |
| `ChildContent` | `RenderFragment?` |  | default slot | The label. |

Two-way binding: `@bind-Checked`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-change` | `PkChangeEventArgs` | Checked: bool?, Index: double?, Value: string?, Label: string?, Previous: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-switch` in the `plainkit-sdk` skill.

## `PkTagInput`

`<pk-tag-input>`: **Tag input** (Form controls). Free-text tags typed into one control: Enter or a separator adds, paste splits, Backspace in an empty field removes the last, duplicates and a maximum are handled.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The tags, comma-joined. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Hint shown in the empty field. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Separators` | `string?` |  | attribute `separators` | Characters that end a tag (besides Enter). |
| `Max` | `int` |  | attribute `max` | Most tags allowed; 0 means no limit. |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-tags-change` | `PkTagsChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-tag-input` in the `plainkit-sdk` skill.

## `PkTextarea`

`<pk-textarea>`: **Textarea** (Form controls). A multi-line control that can grow with its content up to a cap.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The current value; what the form submits. |
| `Name` | `string?` |  | attribute `name` | The form field name. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Hint shown while empty. |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `ReadOnly` | `bool` |  | attribute `readonly` | The value can be read but not edited. |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the value is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Rows` | `int` |  | attribute `rows` | Visible rows of a fixed-height textarea. |
| `MaxLength` | `string?` |  | attribute `maxlength` | Maximum text length. |
| `ShowLabel` | `bool` |  | attribute `show-label` | Shows label as visible text above the field (linked to it), for use without a pk-field. |
| `AutoGrow` | `bool` |  | attribute `autogrow` | Grows with the content instead of scrolling. |
| `MaxHeight` | `int` |  | attribute `max-height` | Auto-grow cap in pixels; 0 means no cap and the box scrolls beyond it otherwise. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-value-change` | `PkValueChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-textarea` in the `plainkit-sdk` skill.

## `PkUnitInput`

`<pk-unit-input>`: **Unit input** (Form controls). A number and a unit select in one field for lengths such as 1.5rem or 24px; the value is the two joined, and a value with a unit outside the list keeps its unit.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Value` | `string?` |  | attribute `value` | The number and unit joined, for example 1.5rem or 12px (empty until there is a number); what the form submits. A value that is not a number with a unit (auto, calc(...)) leaves the number field empty and is kept until the user edits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `ValueChanged` | `EventCallback<string?>` |  | two-way pair of `Value` | Raised when Value changes (two-way binding: `@bind-Value`). |
| `Name` | `string?` |  | attribute `name` | The form field name. |
| `Units` | `string?` |  | attribute `units` | The units the select offers, comma or space separated; the first is used until a value or the user picks another. A value whose unit is not listed adds that unit to the select. |
| `Placeholder` | `string?` |  | attribute `placeholder` | Hint shown in the number field while it is empty. |
| `AriaLabel` | `string?` |  | attribute `label` | The accessible name (aria-label) of the number field; the unit select is named "<label> unit". pk-field fills it from its own label when this is empty. |
| `ShowLabel` | `bool` |  | attribute `show-label` | Shows label as visible text above the field (linked to it), for use without a pk-field. |
| `Disabled` | `bool` |  | attribute `disabled` | Blocks interaction; also set by a disabled fieldset. |
| `ReadOnly` | `bool` |  | attribute `readonly` | Shows the value but does not let it change (the unit select is disabled too). |
| `Required` | `bool` |  | attribute `required` | The form cannot be submitted while the number is empty. |
| `IsInvalid` | `bool` |  | attribute `invalid` | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `Min` | `string?` |  | attribute `min` | Lowest number (the number, whatever the unit). |
| `Max` | `string?` |  | attribute `max` | Highest number. |
| `Step` | `string?` |  | attribute `step` | The number's step: any (the default) allows decimals, or a size such as 0.25 that the arrow keys move by. |

Two-way binding: `@bind-Value`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-value-change` | `PkValueChangeEventArgs` | Value: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-unit-input` in the `plainkit-sdk` skill.
