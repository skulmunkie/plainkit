# Components: Layout, structure and containers

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from Generated/*.razor, blazor/mappings, generated.manifest.json and core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every generated component also renders its element with the attributes you set; it accepts only the parameters listed (an unlisted attribute such as `class` throws when the component renders, so put classes on a wrapping element). Names are `Pk` plus the tag in PascalCase.

## `PkAccordion`

`<pk-accordion>`: **Accordion** (Containers). A stack of collapsible sections; exclusive mode keeps one open.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Exclusive` | `bool` |  | attribute `exclusive` | Opening one item closes the others. |
| `Flush` | `bool` |  | attribute `flush` | No gaps: one shared frame. |
| `ChildContent` | `RenderFragment?` |  | default slot | pk-accordion-item children. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-accordion` in the `plainkit-sdk` skill.

## `PkAccordionItem`

`<pk-accordion-item>`: **Accordion item** (Containers). One collapsible section on a native details element, with an animated panel where the browser supports it.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Heading` | `string?` |  | attribute `heading` | Header text. |
| `Open` | `bool` |  | attribute `open` | Expanded. |
| `OpenChanged` | `EventCallback<bool>` |  | two-way pair of `Open` | Raised with the new value of Open (two-way: Open follows it). |
| `ChildContent` | `RenderFragment?` |  | default slot | The panel. |
| `HeadingContent` | `RenderFragment?` |  | slot `heading` | A rich heading; overrides heading. |
| `IconContent` | `RenderFragment?` |  | slot `icon` | An icon before the heading. |

Two-way binding: `@bind-Open`.

When you set a named fragment (`HeadingContent`, `IconContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-toggle` | `PkToggleEventArgs` | Open: bool?, Expanded: bool?, Id: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-accordion-item` in the `plainkit-sdk` skill.

## `PkAppShell`

`<pk-app-shell>`: **App shell** (Layout & structure). The page frame: nav column, header, one scrolling body and footer. Header and footer stay put; safe-area insets are added on notched phones; a slotted control marked data-nav-toggle opens the side nav drawer.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `SidebarContent` | `RenderFragment?` |  | slot `nav` | A pk-side-nav. |
| `HeaderContent` | `RenderFragment?` |  | slot `header` | Top bar content. |
| `BodyContent` | `RenderFragment?` |  | default slot | The scrolling body. |
| `FooterContent` | `RenderFragment?` |  | slot `footer` | Footer content. |
| `NavOpen` | `bool` |  | attribute `nav-open` | Drawer state, mirrors the side nav. |
| `NavOpenChanged` | `EventCallback<bool>` |  | two-way pair of `NavOpen` | Raised when NavOpen changes (two-way binding: `@bind-NavOpen`). |
| `Flush` | `bool` |  | attribute `flush` | Removes the body padding so a workspace, table or map sits edge to edge (the safe-area insets on a notched phone stay). |

Two-way binding: `@bind-NavOpen`.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-nav-toggle` | `PkNavToggleEventArgs` | Open: bool?, Collapsed: bool? |

**Not available as parameters** (they do not exist; do not use them)

| Parameter | Why |
|---|---|
| `ErrorOverlayMessage` | wrapper behaviour, not a property of the element (As ShowErrorOverlay.) |
| `ShowErrorOverlay` | wrapper behaviour, not a property of the element (The Blazor error UI stays in the wrapper.) |

Element details (parts, CSS custom properties, methods, accessibility): `pk-app-shell` in the `plainkit-sdk` skill.

## `PkCard`

`<pk-card>`: **Card** (Containers). A titled panel with optional media, header actions and footer. Vertical or horizontal (stacking by its own width), flush for tables, and optionally one link.

**Not available as a component in this package.** Use the element directly in markup: `<pk-card>` with its props, slots and events as documented in the `plainkit-sdk` skill (`elements-layout.md`). See `known-gaps.md`.

## `PkCluster`

`<pk-cluster>`: **Cluster** (Layout). A wrapping row (or column) of controls with a fixed gap: the layout primitive for button rows, chip lists and toolbars.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Direction` | `string?` |  | attribute `direction` | Row (wraps at the edge) or column. |
| `Gap` | `string?` |  | attribute `gap` | Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default sm (the control gap). |
| `Align` | `string?` |  | attribute `align` | Cross-axis alignment of the items. |
| `Justify` | `string?` |  | attribute `justify` | Main-axis distribution of the items. |
| `NoWrap` | `bool` |  | attribute `nowrap` | Keep a horizontal cluster on one line instead of wrapping (wrapping is the default). |
| `ChildContent` | `RenderFragment?` |  | default slot | The items to lay out. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-cluster` in the `plainkit-sdk` skill.

## `PkDivider`

`<pk-divider>`: **Divider** (Layout). A rule between content, horizontal or vertical, with an optional label.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Vertical` | `bool` |  | attribute `vertical` | A vertical rule; the parent must be a flex row. |
| `LabelPosition` | `string?` |  | attribute `label-position` | Where the label sits. |
| `Decorative` | `bool` |  | attribute `decorative` | Remove the separator role. |
| `ChildContent` | `RenderFragment?` |  | default slot | The label. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-divider` in the `plainkit-sdk` skill.

## `PkGrid`

`<pk-grid>`: **Grid** (Layout). Equal-width responsive columns: as many columns of at least min as fit, one column on a phone.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Min` | `string?` |  | attribute `min` | Minimum column width as a CSS length (for example 12rem or 200px). Columns are equal width and never narrower than this, so the grid drops to one column when the container is narrower than two. |
| `Columns` | `int` |  | attribute `columns` | The most columns to use, 0 for no limit. The grid still drops columns when they would be narrower than min. |
| `Ratio` | `string?` |  | attribute `ratio` | Unequal columns as a ratio, for example 2:1 (a wide main column and a narrow side) or 1:2:1. Empty keeps equal auto-fit columns. Overrides min and columns; on a phone (640px and below) the grid falls back to auto columns so it stacks. |
| `Gap` | `string?` |  | attribute `gap` | Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default md (the card gap). |
| `ChildContent` | `RenderFragment?` |  | default slot | The items to lay out. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-grid` in the `plainkit-sdk` skill.

## `PkPageHeader`

`<pk-page-header>`: **Page header** (Layout & structure). The top of a page, a section or a record: a breadcrumb row, a title bar with actions, and a note line. Stacks by its own width, with the actions full width and touch-sized on a phone.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Heading` | `string?` |  | attribute `heading` | The title. Without it (and without default or actions content) the title bar is not shown. |
| `Level` | `int` |  | attribute `level` | The heading level the title exposes to assistive technology (aria-level). |
| `Variant` | `string?` |  | attribute `variant` | page is the standard page top; section is a titled section inside a page (title, actions, note); record is the compact strip of a single-record page (chips on the left, actions right). |
| `ChildContent` | `RenderFragment?` |  | default slot | Beside the title: status chips, badges or your own heading element. |
| `BreadcrumbContent` | `RenderFragment?` |  | slot `breadcrumb` | A pk-breadcrumb (or any trail) above the title; hidden when empty. |
| `ActionsContent` | `RenderFragment?` |  | slot `actions` | Buttons at the right of the title bar; full width and touch-sized on a narrow container. |
| `MetaContent` | `RenderFragment?` |  | slot `meta` | A note or metadata line under the title bar; hidden when empty. |

When you set a named fragment (`BreadcrumbContent`, `ActionsContent`, `MetaContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-page-header` in the `plainkit-sdk` skill.

## `PkStack`

`<pk-stack>`: **Stack** (Layout). A vertical stack of children with an even gap: the layout primitive for form sections, card bodies and page columns.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Gap` | `string?` |  | attribute `gap` | Space between children, on the spacing scale (none 0, xs --space-1, sm --space-2, md --space-4, lg --space-6, xl --space-8). Default md (the flow space). |
| `Align` | `string?` |  | attribute `align` | Cross-axis alignment: stretch fills the width, the others size each child to its content. |
| `Dividers` | `bool` |  | attribute `dividers` | Draw a rule between children. |
| `ChildContent` | `RenderFragment?` |  | default slot | The items to lay out. |

Element details (parts, CSS custom properties, methods, accessibility): `pk-stack` in the `plainkit-sdk` skill.

## `PkToolbar`

`<pk-toolbar>`: **Toolbar** (Layout & structure). A row with a lead (title and note) on the left and actions on the right. It wraps by its own width and stacks on a phone, with touch-sized actions.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Heading` | `string?` |  | attribute `heading` | The lead title, in strong text. |
| `Note` | `string?` |  | attribute `note` | A muted note beside the title, such as a count. |
| `ChildContent` | `RenderFragment?` |  | default slot | More lead content after the title and note (filters, chips). |
| `ActionsContent` | `RenderFragment?` |  | slot `actions` | Buttons at the right; hidden when empty, full width and touch-sized on a narrow container. |

When you set a named fragment (`ActionsContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

Element details (parts, CSS custom properties, methods, accessibility): `pk-toolbar` in the `plainkit-sdk` skill.

## `PkWorkspace`

`<pk-workspace>`: **Workspace** (Layout & structure). A tool page in panes: a nav pane, a main pane and an optional docked aside, each scrolling on its own. On a phone one pane shows at a time and a tab strip (Contents, Content, Details) switches it.

**Parameters**

| Parameter | Type | Enum values | Sets | Description |
|---|---|---|---|---|
| `Fill` | `bool` |  | attribute `fill` | Fill the height the parent gives it (a parent with a definite height, or a flex or grid parent) and drop the frame, for a tool that owns the whole page. Without it the workspace is as tall as the viewport minus a reserve. |
| `ActivePane` | `string?` |  | attribute `active-pane` | The pane shown on a phone. On a wider screen every pane shows. A pane that does not exist falls back to the main pane. |
| `ActivePaneChanged` | `EventCallback<string?>` |  | two-way pair of `ActivePane` | Raised when ActivePane changes (two-way binding: `@bind-ActivePane`). |
| `AsideOpen` | `bool` |  | attribute `aside-open` | Show the aside: a docked column on a wide screen, a panel over the main pane on a tablet and its own pane on a phone. |
| `NavLabel` | `string?` |  | attribute `nav-label` | The nav pane's accessible name and its tab on a phone. |
| `MainLabel` | `string?` |  | attribute `main-label` | The main pane's accessible name and its tab on a phone. |
| `AsideLabel` | `string?` |  | attribute `aside-label` | The aside's accessible name and its tab on a phone. |
| `NavContent` | `RenderFragment?` |  | slot `nav` | The nav pane: a file tree, a list of sections, filters. Leave it out for a two-pane workspace. |
| `ChildContent` | `RenderFragment?` |  | default slot | The main pane. |
| `AsideContent` | `RenderFragment?` |  | slot `aside` | The docked aside (an inspector or a details panel). It shows when asideOpen is set. |
| `OnPaneChange` | `EventCallback<PkPaneChangeEventArgs>` |  | event `pk-pane-change` | A tab in the phone strip was chosen. Cancelable: preventDefault keeps the previous pane. |

Two-way binding: `@bind-ActivePane`.

When you set a named fragment (`NavContent`, `AsideContent`), write the body as an explicit `<ChildContent>` tag too: Razor does not allow an implicit body next to a named fragment.

**Event args**

| Event | Args class | Fields |
|---|---|---|
| `pk-pane-change` | `PkPaneChangeEventArgs` | Pane: string?, Previous: string? |

Element details (parts, CSS custom properties, methods, accessibility): `pk-workspace` in the `plainkit-sdk` skill.
