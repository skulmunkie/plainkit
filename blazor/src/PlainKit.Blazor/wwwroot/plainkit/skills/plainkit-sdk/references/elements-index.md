# Elements: index

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from core/dist/elements/api.json. Do not edit: change the source and run the generator.

88 elements. Open the file named in the last column for a tag's props, slots, events, parts, CSS properties, methods and examples.

| Tag | Group | What it is | File |
|---|---|---|---|
| `pk-accordion` | Containers | A stack of collapsible sections; exclusive mode keeps one open. | `elements-layout.md` |
| `pk-accordion-item` | Containers | One collapsible section on a native details element, with an animated panel where the browser supports it. | `elements-layout.md` |
| `pk-alert` | Feedback & status | A persistent message in a kind (info, success, warning, danger): optional heading, actions, a dismiss button, a full-bleed banner variant for the top of a page, and a pl… | `elements-feedback.md` |
| `pk-app-shell` | Layout & structure | The page frame: nav column, header, one scrolling body and footer. | `elements-layout.md` |
| `pk-avatar` | Data display | A person or entity as an image or initials, with sizes, shapes, a stable colour per name and an online, away or busy status dot. | `elements-data-display.md` |
| `pk-avatar-group` | Data display | Overlapping avatars with a +N tile for the overflow. | `elements-data-display.md` |
| `pk-back-to-top` | Navigation | A floating button that appears once the reader is well down the page and scrolls the page (or a named scroller) back to the top, smoothly unless reduced motion is on. | `elements-navigation.md` |
| `pk-badge` | Data display | A count or short label in a pill, or a status dot with a label; counts cap at a maximum (99+). | `elements-data-display.md` |
| `pk-breadcrumb` | Navigation | The path to the current page as a trail of links with chevrons; the middle collapses behind a "..." button when the trail is long, sooner on a phone. | `elements-navigation.md` |
| `pk-button` | Actions | A button in five variants and three sizes, with busy, icon-only and toggle forms. | `elements-actions.md` |
| `pk-button-group` | Actions | Buttons fused into one bar, horizontal, vertical or full width; with mode="single" its toggle buttons behave as one choice, which makes it the SDK's segmented control (a… | `elements-actions.md` |
| `pk-calendar` | Forms | A small month grid to pick or show a date, with marks, minimum and maximum, and full keyboard control. | `elements-form-controls.md` |
| `pk-card` | Containers | A titled panel with optional media, header actions and footer. | `elements-layout.md` |
| `pk-chart` | Data display | Bar, line, donut, stacked-bar and sparkline charts in pure SVG, drawn from a slotted data table (which stays as the accessible fallback) or from a data property. | `elements-data-display.md` |
| `pk-checkbox` | Form controls | A checkbox drawn from tokens, with an indeterminate state and select-all groups: a master checkbox mirrors every checkbox of its group. | `elements-form-controls.md` |
| `pk-cluster` | Layout | A wrapping row (or column) of controls with a fixed gap: the layout primitive for button rows, chip lists and toolbars. | `elements-layout.md` |
| `pk-code-block` | Code | A block of code with an optional label bar, line numbers, a wrap toggle and a copy button. | `elements-data-display.md` |
| `pk-colour-input` | Form controls | The native colour picker beside an editable hex field that stay in step; the field accepts #abc, abc and #aabbcc and is normalised to #rrggbb. | `elements-form-controls.md` |
| `pk-combobox` | Form controls | An autocomplete (type to filter a list) or, with mode="select", a custom select. | `elements-form-controls.md` |
| `pk-command-palette` | Overlays | A keyboard-first launcher: Ctrl/Cmd+K opens a dialog with a search field over grouped commands, fuzzy-matched with highlights, recents when the field is empty, arrows an… | `elements-overlays.md` |
| `pk-context-menu` | Overlays | A menu opened at the pointer by right click, by Shift+F10 or the Menu key, or by a touch long press, over the region it wraps. | `elements-overlays.md` |
| `pk-dialog` | Overlays | A native modal dialog in five sizes with a scrolling body, full screen on a phone; plus confirm, alert and prompt patterns that return a promise. | `elements-overlays.md` |
| `pk-divider` | Layout | A rule between content, horizontal or vertical, with an optional label. | `elements-layout.md` |
| `pk-drawer` | Overlays | A panel that slides in from the left, right or bottom over a dimmed page, on a native modal dialog: backdrop, focus trap and Escape come with it. | `elements-overlays.md` |
| `pk-dropdown` | Overlays | A menu opened from a trigger: buttons or links, headers, dividers, checkable items and submenus, with full keyboard support (arrows, Home, End, typeahead, Right/Left for… | `elements-overlays.md` |
| `pk-dropzone` | Form controls | A drop target over a real file input: drag files in or click, with type, size and count checks, a list of accepted files with Remove, and a reason for each rejection. | `elements-form-controls.md` |
| `pk-empty-state` | Feedback | What a list shows when it has no rows: an optional icon, a title, a description and the actions that fill it. | `elements-feedback.md` |
| `pk-field` | Form layout | The label, help, error, warning and counter around a form control, with the wiring between them. | `elements-form-layout.md` |
| `pk-field-list` | Data display | A titled description list: term and value pairs as slotted dt and dd, beside each other, stacked or run in; with dividers and a dense mode. | `elements-data-display.md` |
| `pk-field-row` | Form layout | A wrapping row of fields that share the width: each field grows from a 120px minimum, a wide field takes a row of its own. | `elements-form-layout.md` |
| `pk-form` | Form layout | Wraps a form and shows the browser's constraint validation well: messages in each field, a summary with links, focus on the first problem, and live re-checking. | `elements-form-layout.md` |
| `pk-form-actions` | Form layout | The bar of buttons under a form, aligned start, end or between, and sticky to the bottom of a long form. | `elements-form-layout.md` |
| `pk-form-section` | Form layout | A titled section of a form: heading and description beside a responsive grid of fields (stacked below 1024px). | `elements-form-layout.md` |
| `pk-gallery` | Data display | The SDK gallery inside your page: every control, element, layout and template with live samples, narrowed by attributes to just what you want to show. | `elements-data-display.md` |
| `pk-grid` | Layout | Equal-width responsive columns: as many columns of at least min as fit, one column on a phone. | `elements-layout.md` |
| `pk-hint` | Feedback | An inline explanation that opens in the flow of the page: no tooltip, no hover-only content. | `elements-feedback.md` |
| `pk-icon` | Media | A symbol from the SDK sprite, drawn in the surrounding text colour and size. | `elements-data-display.md` |
| `pk-image-gallery` | Data display | A grid of square thumbnails with one marked primary (a badge and an accent frame). | `elements-data-display.md` |
| `pk-input` | Form controls | The single-line control: text, email, url, tel, search, password, number and the date and time types, with an optional clear button, password reveal, number stepper, mon… | `elements-form-controls.md` |
| `pk-lightbox` | Overlays | A full-screen image viewer on a native dialog: caption, counter, previous and next with arrow keys and swipe, neighbours preloaded, only safe image sources. | `elements-overlays.md` |
| `pk-list-group` | Data display | A bordered list of rows: plain, actionable (links and buttons), with badges at the end, or a checklist. | `elements-data-display.md` |
| `pk-loading-overlay` | Feedback & status | Covers a region (or the whole viewport with fullscreen) with a spinner and a label while busy, and makes what it covers inert so it cannot be clicked or tabbed into. | `elements-feedback.md` |
| `pk-local-time` | Data display | A UTC instant written in the reader's own locale and time zone, as a date, a time, both, or relative to now (3 hours ago). | `elements-data-display.md` |
| `pk-media` | Media | An image or video in a fixed aspect-ratio box with an optional figure caption and lightbox-ready behaviour. | `elements-data-display.md` |
| `pk-menu-item` | Overlays | One row of a dropdown or context menu: a plain item, a checkbox, a radio, a section header or a divider; optional prefix and suffix (shortcut) and a nested submenu. | `elements-overlays.md` |
| `pk-nav-item` | Navigation | A row of a side nav: a link, or a branch that folds its children; icon, badge, active bar and disabled state. | `elements-navigation.md` |
| `pk-navbar` | Navigation | The top navigation: brand, links, dropdown and mega menus, right-aligned actions, sticky option, and a hamburger that folds the links below 1024px. | `elements-navigation.md` |
| `pk-otp-input` | Form controls | A one-time code or PIN as a row of cells: typing advances, Backspace steps back, a pasted or autofilled code spreads across the cells, and completion is an event. | `elements-form-controls.md` |
| `pk-page-header` | Layout & structure | The top of a page, a section or a record: a breadcrumb row, a title bar with actions, and a note line. | `elements-layout.md` |
| `pk-pager` | Navigation | Page-level previous and next links between sibling pages, with an optional centre note ("Step 2 of 5"); table pagination is a separate control. | `elements-navigation.md` |
| `pk-pagination` | Navigation | Page navigation: numbered with ellipses, a range summary, a page-size select, a simple previous and next form, or load more. | `elements-navigation.md` |
| `pk-popover` | Overlays | A small non-modal panel anchored to its trigger with a heading, body and actions; opens on click or hover, flips and shifts to stay on screen, closes on Escape, an outsi… | `elements-overlays.md` |
| `pk-progress` | Feedback | A determinate or indeterminate progress bar on the native progress element, with sizes, colours, stripes, a label, a value readout and automatic warning colours. | `elements-feedback.md` |
| `pk-radio-group` | Form controls | One choice from a few options, as radios or as a segmented bar. | `elements-form-controls.md` |
| `pk-range` | Form controls | A slider, or with dual a minimum and maximum slider, drawn from tokens with a live value output. | `elements-form-controls.md` |
| `pk-rating` | Form controls | A whole-star rating input (radios underneath), and a compact read-only display form. | `elements-form-controls.md` |
| `pk-scroll-progress` | Navigation | A thin bar at the top of the screen that fills as the page (or a named scroller) is read. | `elements-navigation.md` |
| `pk-select` | Form controls | The browser select in the same skin as the text input. | `elements-form-controls.md` |
| `pk-select-menu` | Forms & inputs | A single-choice listbox popup that reads native option children, so it degrades to nothing worse than a list; form-associated, keyboard driven with typeahead. | `elements-form-controls.md` |
| `pk-side-nav` | Navigation | The app’s side navigation: brand, filter box, nested groups with arrow-key tree navigation, collapse to an icon rail (tooltips on rows, branches open as flyouts), persis… | `elements-navigation.md` |
| `pk-skeleton` | Feedback | A placeholder shape (line, paragraph, circle, block) that holds the layout while content loads. | `elements-feedback.md` |
| `pk-skip-link` | Navigation | The first focusable element on a page: invisible until it takes focus, then a button that jumps past the navigation to the main content. | `elements-navigation.md` |
| `pk-spinner` | Feedback | A busy indicator: a ring, three dots or an indeterminate bar, in three sizes, with an overlay mode for a loading panel. | `elements-feedback.md` |
| `pk-split-button` | Actions | A main action beside a caret that opens a menu of related actions, so the common case is one click and the variants stay reachable. | `elements-actions.md` |
| `pk-splitter` | Layout | Two panes with a draggable separator between them: resize by pointer or arrow keys, side by side or stacked, with a minimum and maximum size. | `elements-layout.md` |
| `pk-stack` | Layout | A vertical stack of children with an even gap: the layout primitive for form sections, card bodies and page columns. | `elements-layout.md` |
| `pk-stat` | Data display | A headline number with a label, an optional change indicator (arrow, sign and a spoken form), a sparkline and a tone. | `elements-data-display.md` |
| `pk-step` | Navigation | One step of a stepper: a marker (number, tick or !), a title, an optional description and the connector to the next step. | `elements-navigation.md` |
| `pk-stepper` | Navigation | A wizard header: numbered steps with done, current, upcoming and error states, horizontal or vertical, optionally clickable (linear: you can go back, not skip ahead). | `elements-navigation.md` |
| `pk-switch` | Form controls | An on/off toggle that takes part in a form: it submits its value when on and resets with the form. | `elements-form-controls.md` |
| `pk-tab` | Navigation | One tab of an pk-tabs. | `elements-navigation.md` |
| `pk-tab-panel` | Navigation | The content of one tab; shown while its tab is selected. | `elements-navigation.md` |
| `pk-table` | Data display | A data table. | `elements-data-display.md` |
| `pk-tabs` | Navigation | A tab strip with optional panels. | `elements-navigation.md` |
| `pk-tag` | Data display | A neutral label that can carry a remove button. | `elements-data-display.md` |
| `pk-tag-input` | Form controls | Free-text tags typed into one control: Enter or a separator adds, paste splits, Backspace in an empty field removes the last, duplicates and a maximum are handled. | `elements-form-controls.md` |
| `pk-textarea` | Form controls | A multi-line control that can grow with its content up to a cap. | `elements-form-controls.md` |
| `pk-timeline` | Data display | A vertical activity feed of timeline items joined by a line. | `elements-data-display.md` |
| `pk-timeline-item` | Data display | One event on a timeline: a marker, a heading, a time and a body. | `elements-data-display.md` |
| `pk-toast` | Feedback & status | One transient message: kind, optional title, message, an action slot and a dismiss button; pauses its timer on hover and focus. | `elements-feedback.md` |
| `pk-toast-stack` | Feedback & status | The live region that holds toasts at one screen position, shows at most three at once and queues the rest; PkToast.show(message, options) creates one per position on dem… | `elements-feedback.md` |
| `pk-toc` | Navigation | An anchor list of the headings inside a target element with scroll-spy: the heading in view is marked as you read; works with the window or an inner scroller such as the… | `elements-navigation.md` |
| `pk-toolbar` | Layout & structure | A row with a lead (title and note) on the left and actions on the right. | `elements-layout.md` |
| `pk-tooltip` | Overlays | A short label for the control it wraps, shown on hover, on keyboard focus and on a touch long press. | `elements-overlays.md` |
| `pk-tree` | Navigation | An expandable hierarchy of tree items with roving focus, single selection and type-ahead, following the WAI-ARIA tree pattern. | `elements-navigation.md` |
| `pk-tree-item` | Navigation | One node of a tree: a toggle, a label and nested child items. | `elements-navigation.md` |
| `pk-unit-input` | Form controls | A number and a unit select in one field for lengths such as 1.5rem or 24px; the value is the two joined, and a value with a unit outside the list keeps its unit. | `elements-form-controls.md` |
| `pk-workspace` | Layout & structure | A tool page in panes: a nav pane, a main pane and an optional docked aside, each scrolling on its own. | `elements-layout.md` |
