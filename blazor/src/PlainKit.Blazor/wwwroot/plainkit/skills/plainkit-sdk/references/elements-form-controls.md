# Elements: Form controls

> Plainkit 0.1.0-alpha.1. Generated by scripts/build-skills.mjs from core/dist/elements/api.json. Do not edit: change the source and run the generator.

Every element also takes the usual HTML global attributes (`id`, `class`, `slot`, `hidden`, `aria-*`, `data-*`). Never use a `style` attribute (the CSP blocks it): use props, parts and CSS custom properties.

## `pk-calendar`

**Calendar** (Forms). A small month grid to pick or show a date, with marks, minimum and maximum, and full keyboard control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `value` | `value` | string | `""` |  | The selected day, an ISO date such as 2026-09-19. |
| `month` | `month` | string | `""` |  | An ISO date whose month is shown; defaults to the value or today. |
| `min` | `min` | string | `""` |  | Earliest selectable ISO date. |
| `max` | `max` | string | `""` |  | Latest selectable ISO date. |
| `week-start` | `weekStart` | number | `0` |  | First weekday: 0 Sunday, 1 Monday. |
| `locale` | `locale` | string | `""` |  | Language of month and weekday names; defaults to the page language. |
| `marks` | `marks` | json (a JSON attribute, or set the property) | `[]` |  | ISO dates that get a dot. |
| `readonly` | `readonly` | boolean | `false` |  | Show only; no selection. |
| `disabled` | `disabled` | boolean | `false` |  | Inert. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `pk-select` | `{ value: string }` | A day was chosen. Cancel to keep the current value. |
| `pk-month` | `{ month: string }` | The shown month changed (yyyy-mm). |

**Methods**

| Method | Description |
|---|---|
| `monthGrid(year, month, weekStart)` | Weeks of a month. |
| `dateForKey(iso, key)` | Keyboard movement. |
| `addMonths(iso, n)` | Clamped month step. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `header` | The title row. |
| `prev` | Previous month. |
| `title` | Month and year. |
| `next` | Next month. |
| `grid` | The table. |
| `weekdays` | The weekday header. |
| `days` | The day rows. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-calendar-cell` | `var(--touch-target)` | Day size: the touch target on a phone, 2rem on desktop. |

**Accessibility.** role=grid with a live month title; one roving focused day; arrows move by a day or a week, Home and End to the month edges, PageUp and PageDown by a month (Shift a year). The selected day has aria-selected, today aria-current=date; out-of-range days are disabled buttons with full-date labels.

Example: Selected day with marks

```html
<pk-calendar value="2026-09-19" marks='["2026-09-05","2026-09-24"]'></pk-calendar>
```

Example: Monday start, limited range

```html
<pk-calendar week-start="1" min="2026-09-10" max="2026-09-28"></pk-calendar>
```

## `pk-checkbox`

**Checkbox** (Form controls). A checkbox drawn from tokens, with an indeterminate state and select-all groups: a master checkbox mirrors every checkbox of its group.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `"on"` |  | The value submitted while checked. |
| `checked` | `checked` | boolean | `false` |  | Whether the box is checked. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `indeterminate` | `indeterminate` | boolean | `false` |  | The mixed state (some of a group are checked). |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `group` | `group` | string | `""` |  | Puts this checkbox in a select-all group of that name. |
| `master` | `master` | string | `""` |  | Makes this the select-all checkbox of the group of that name; it shows checked, unchecked or indeterminate as its members change. |
| `size` | `size` | enum | `"md"` | `md` `sm` | sm is the compact checkbox of a toolbar: smaller label text. |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | The label. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-change` | `{ checked: boolean }` | The user changed the value (same moment as change; carries it). |

**Methods**

| Method | Description |
|---|---|
| `toggle()` | Toggles as a click does. |
| `focus()` | Focuses the box. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `control` | The label wrapper (the whole row is the tap target). |
| `input` | The inner checkbox. |
| `label` | The label text. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-checkbox-border` | `var(--color-input-border)` | Border colour of the box. |
| `--pk-checkbox-size` | `1.15rem` | Box size. |
| `--pk-checkbox-font` | `var(--text-read)` | Label font size. |
| `--pk-checkbox-gap` | `var(--space-2)` | Space between the box and its label. |
| `--pk-checkbox-color` | `var(--color-text)` | Label colour. |

**Accessibility.** A native checkbox inside its label, so it is named by its text and toggled by Space; indeterminate is announced as mixed; invalid sets aria-invalid and a red border. Rows are 44px tall on a phone.

Example: States

```html
<pk-checkbox checked>Track inventory</pk-checkbox>
<pk-checkbox>Include archived</pk-checkbox>
<pk-checkbox indeterminate>Some variants selected</pk-checkbox>
<pk-checkbox disabled>Disabled</pk-checkbox>
```

Example: Select all

```html
<pk-checkbox master="rows">Select all</pk-checkbox>
<pk-checkbox group="rows" checked>Widget 1</pk-checkbox>
<pk-checkbox group="rows">Widget 2</pk-checkbox>
```

## `pk-colour-input`

**Colour input** (Form controls). The native colour picker beside an editable hex field that stay in step; the field accepts #abc, abc and #aabbcc and is normalised to #rrggbb.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The colour as #rrggbb. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-colour` | `{ value: string }` | A valid colour was picked or typed (lowercase #rrggbb). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the hex field. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The row of swatch and hex field. |
| `swatch` | The native colour picker. |
| `control` | The hex text field. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |

**Accessibility.** Both inputs are labelled; text that is not a colour sets aria-invalid and a validity message; the picker is the platform's own, so it is keyboard and screen-reader accessible everywhere. The swatch is 44px on a phone.

Example: Picker and hex

```html
<pk-colour-input label="Accent colour" value="#4a90e2"></pk-colour-input>
```

## `pk-combobox`

**Combobox** (Form controls). An autocomplete (type to filter a list) or, with mode="select", a custom select. ARIA combobox with the highlight tracked by aria-activedescendant, so focus stays on the control. Its options are its own option children.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `mode` | `mode` | enum | `"autocomplete"` | `autocomplete` `select` | autocomplete filters as you type; select is a button that opens the list (type a letter to jump). Prefer the native pk-select on a phone. |
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `placeholder` | `placeholder` | string | `""` |  | Hint shown while empty. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `free` | `free` | boolean | `false` |  | Autocomplete: keep typed text that is not an option, and use it as the value. |
| `filtering` | `filtering` | enum | `"client"` | `client` `off` | client also filters the options here; off leaves filtering to the host, which listens for pk-combo-query and replaces the options. |
| `open` | `open` | boolean | `false` |  | Whether the list is open. |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | option elements (value, disabled and text). |
| `empty` | The message shown when nothing matches. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-combo-select` | `{ value: string, label: string }` | An option was chosen. |
| `pk-combo-query` | `{ query: string }` | The user typed: the text so far, so a host can fetch more options (with filtering="off" it also replaces the client filter). |
| `pk-combo-toggle` | `{ open: bool }` | The list opened or closed by itself (typing, a click, a key, focus leaving); open already has the new value. Not raised for a change the host made. |

**Methods**

| Method | Description |
|---|---|
| `show()` | Opens the list. |
| `hide()` | Closes the list. |
| `focus()` | Focuses the control. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The bordered field. |
| `control` | The input (autocomplete mode). |
| `trigger` | The button (select mode). |
| `chevron` | The arrow (select mode). |
| `popup` | The list panel. |
| `empty` | The no-match message. |
| `option` | One option (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |

**Accessibility.** ARIA 1.2 combobox: role=combobox, aria-expanded, aria-controls and aria-activedescendant (focus stays on the input or button); Arrow keys open and move, Home and End jump (select mode), Enter picks, Escape closes, Tab closes, letters jump in select mode. The chosen option carries aria-selected and a check mark. Options are 44px tall on a phone; the list opens upward when there is no room below.

Example: Autocomplete

```html
<pk-combobox label="Variant" placeholder="Type to search"><option value="AC-1001">AC-1001 - Widget 1</option><option value="AC-1002">AC-1002 - Widget 2</option><option value="AC-1003">AC-1003 - Widget 3</option></pk-combobox>
```

Example: Custom select

```html
<pk-combobox mode="select" label="Channel" placeholder="Choose a channel" value="market"><option value="web">Web</option><option value="market">Market</option><option value="live">Live</option><option value="other" disabled>Poshmark (unavailable)</option></pk-combobox>
```

## `pk-dropzone`

**Dropzone** (Form controls). A drop target over a real file input: drag files in or click, with type, size and count checks, a list of accepted files with Remove, and a reason for each rejection. Files are submitted with the form.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `accept` | `accept` | string | `""` |  | Accepted types: extensions, MIME types or type/* (like the accept attribute). |
| `multiple` | `multiple` | boolean | `false` |  | Allows several files. |
| `max-size` | `maxSize` | string | `""` |  | Largest file: 500KB, 5MB, 1GB or bytes. |
| `max-files` | `maxFiles` | number | `0` |  | Most files (multiple); 0 means no limit. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | The zone title. |
| `hint` | A hint under the title (types and limits). |
| `input` | An existing file input (a Blazor InputFile) that keeps doing the reading: the zone then only draws the target, and dropped files are placed in that input. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `change` | `native Event` | The file list changed. |
| `pk-files` | `{ count: number, rejectedCount: number, files: File[], rejected: { file: File, reason: string }[] }` | Files were picked or dropped: those kept and those refused with a reason. |

**Methods**

| Method | Description |
|---|---|
| `clear()` | Removes every file. |
| `focus()` | Focuses the file input. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `zone` | The dashed drop target. |
| `control` | The real file input, invisible over the zone. |
| `title` | The zone title. |
| `hint` | The zone hint. |
| `list` | The list of files. |
| `item` | One file row (dynamic). |
| `item-name` | A file name (dynamic). |
| `item-note` | A file size or rejection reason (dynamic). |
| `item-remove` | A Remove button (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-border` | `var(--color-input-border)` | Dashed border colour. |

**Accessibility.** A real focusable file input covers the zone, so the keyboard opens the picker and the zone shows the focus ring; the list is a polite live region; rejections are text (with the reason), not just red; each Remove button names its file. The whole zone is the tap target on a phone.

Example: Drop zone

```html
<pk-dropzone label="Files to import" accept=".csv,text/csv" multiple max-size="5MB" max-files="3">Drop files here or click to choose<span slot="hint">CSV, up to 5 MB each, 3 files at most</span></pk-dropzone>
```

## `pk-input`

**Input** (Form controls). The single-line control: text, email, url, tel, search, password, number and the date and time types, with an optional clear button, password reveal, number stepper, money format, prefix and suffix, and a floating label.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `type` | `type` | enum | `"text"` | `text` `email` `url` `tel` `search` `password` `number` `date` `time` `datetime-local` `month` `week` | The kind of value. Date and time types open the platform picker. |
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `placeholder` | `placeholder` | string | `""` |  | Hint shown while empty (ignored by a floating label). |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `show-label` | `showLabel` | boolean | `false` |  | Shows label as visible text above the field (linked to it), for use without a pk-field. Ignored by a floating label. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `readonly` | `readonly` | boolean | `false` |  | The value can be read and selected but not edited. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `warning` | `warning` | boolean | `false` |  | Shows the warning state (a value the user may want to check). |
| `valid` | `valid` | boolean | `false` |  | Shows the valid state. |
| `min` | `min` | string | `""` |  | Minimum value (number and date types). |
| `max` | `max` | string | `""` |  | Maximum value (number and date types). |
| `step` | `step` | string | `""` |  | Step for number and date types; also the stepper increment. |
| `minlength` | `minlength` | string | `""` |  | Minimum text length. |
| `maxlength` | `maxlength` | string | `""` |  | Maximum text length. |
| `pattern` | `pattern` | string | `""` |  | A regular expression the whole value must match. |
| `autocomplete` | `autocomplete` | string | `""` |  | The autofill token, for example current-password, new-password or one-time-code. |
| `inputmode` | `inputmode` | string | `""` |  | The virtual keyboard to request (numeric, decimal, email, ...). |
| `size` | `size` | enum | `"md"` | `sm` `md` `lg` | Control height. |
| `clearable` | `clearable` | boolean | `false` |  | Shows a clear button while there is a value. |
| `reveal` | `reveal` | boolean | `false` |  | Password only: a show and hide button. |
| `stepper` | `stepper` | boolean | `false` |  | Number only: minus and plus buttons that respect min, max and step. |
| `format` | `format` | enum | `"none"` | `none` `money` | money formats the value as an amount (1,234.50) when the field is left and shows the plain number while editing; value stays the plain number. |
| `decimals` | `decimals` | number | `2` |  | Decimal places for the money format. |
| `currency` | `currency` | string | `"USD"` |  | ISO currency code whose symbol leads a money field. |
| `locale` | `locale` | string | `"en-US"` |  | Locale for money formatting. |
| `floating` | `floating` | boolean | `false` |  | Shows label inside the field until it has focus or a value, then floats it to the top edge. |
| `autohide` | `autohide` | number | `0` |  | Password reveal: seconds until the value is masked again; 0 keeps it revealed. |
| `debounce` | `debounce` | number | `0` |  | Search only: milliseconds to wait after typing before pk-search fires; 0 fires on every change. |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| `prefix` | A leading add-on: a currency sign, an icon, a unit. |
| `suffix` | A trailing add-on: a unit, a domain, a button. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-value-change` | `{ value: string }` | The user committed a new value (same moment as change; carries the value). |
| `pk-search` | `{ value: string }` | Search type: the debounced search text, or immediately on Enter and clear. |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Moves focus to the inner input. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The bordered field that holds the control and its add-ons. |
| `label` | The visible label shown by showLabel. |
| `control` | The inner input element. |
| `search-icon` | The magnifier of a search field. |
| `currency` | The currency symbol of a money field. |
| `floating-label` | The floating label. |
| `clear` | The clear button. |
| `reveal` | The password reveal button. |
| `step-down` | The stepper minus button. |
| `step-up` | The stepper plus button. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |

**Accessibility.** A native input lives in the shadow root, named by label (aria-label) and described by description (aria-description); invalid sets aria-invalid, and the state is never colour alone (the ring is thicker). Buttons are labelled and reveal reports aria-pressed. Money and stepper fields keep the number in value while showing formatted text. Text is 16px and the control 44px tall on a phone.

Example: Text, states and sizes

```html
<pk-input label="Product title" value="Widget 1"></pk-input>
<pk-input label="Read only" value="AC-1001" readonly></pk-input>
<pk-input label="Invalid" value="bad sku" invalid></pk-input>
<pk-input label="Small" size="sm"></pk-input>
```

Example: Add-ons, money and stepper

```html
<pk-input label="Price" format="money" value="1234.5"></pk-input>
<pk-input label="Quantity" type="number" stepper min="0" max="10" value="3"></pk-input>
<pk-input label="Domain" value="example"><span slot="prefix">https://</span><span slot="suffix">.com</span></pk-input>
```

Example: Search, password and floating label

```html
<pk-input type="search" label="Search products" placeholder="Search products" clearable debounce="250"></pk-input>
<pk-input type="password" label="Password" reveal autohide="15" autocomplete="current-password"></pk-input>
<pk-input label="Product title" floating></pk-input>
```

Example: Visible label

```html
<pk-input label="Email" show-label type="email"></pk-input>
```

Example: Date and time

```html
<pk-input type="date" label="Release date" value="2026-09-19"></pk-input>
<pk-input type="time" label="Ships by" value="14:30"></pk-input>
```

## `pk-otp-input`

**OTP input** (Form controls). A one-time code or PIN as a row of cells: typing advances, Backspace steps back, a pasted or autofilled code spreads across the cells, and completion is an event.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The code typed so far. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `length` | `length` | number | `6` |  | Number of cells. |
| `type` | `type` | enum | `"numeric"` | `numeric` `alnum` `any` | The alphabet: digits, letters and digits, or anything. |
| `separator-at` | `separatorAt` | number | `0` |  | Puts a dash after this many cells (0 for none). |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-otp-change` | `{ value: string }` | The code changed. |
| `pk-otp-complete` | `{ value: string }` | Every cell is filled. |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the first empty cell. |
| `clear()` | Empties the code. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `group` | The row of cells. |
| `cell` | One cell (dynamic). |
| `separator` | The dash (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |

**Accessibility.** A role=group named by label; every cell is named "Digit n of N"; the first has one-time-code autocomplete so the OS can fill an SMS code; arrow keys, Home and End move between cells; numeric cells open the numeric keypad. Cells are 44px wide on a phone.

Example: Six digits with a dash

```html
<pk-otp-input label="Verification code" length="6" separator-at="3"></pk-otp-input>
```

## `pk-radio-group`

**Radio group** (Form controls). One choice from a few options, as radios or as a segmented bar. The options are its own option children; the radios are native inputs, so arrow keys and grouping are the browser's.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `variant` | `variant` | enum | `"radio"` | `radio` `segmented` | radio draws a list of radios; segmented draws one bar of segments. |
| `direction` | `direction` | enum | `"vertical"` | `vertical` `horizontal` | Radio variant: stacked or in a wrapping row. |
| `fill` | `fill` | boolean | `false` |  | Segmented variant: spread the segments across the full width. |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | option elements (value, disabled and text). |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-value-change` | `{ value: string }` | The user changed the value (same moment as change; carries it). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the chosen radio, or the first enabled one. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `group` | The radiogroup container. |
| `option` | One option label (dynamic). |
| `option-label` | The option text (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-radio-border` | `var(--color-input-border)` | Border colour of a radio. |
| `--pk-segmented-active` | `var(--color-accent)` | Background of the chosen segment. |

**Accessibility.** role=radiogroup named by label; each option is a native radio in one tab stop with arrow-key movement; the segmented variant hides the radio visually but keeps it focusable and shows the focus ring on the segment. Options are 44px tall on a phone.

Example: Radios

```html
<pk-radio-group label="Condition" value="nm"><option value="nm">Near mint</option><option value="vf">Very fine</option><option value="g" disabled>Graded (unavailable)</option></pk-radio-group>
```

Example: Segmented

```html
<pk-radio-group variant="segmented" label="View" value="week"><option value="day">Day</option><option value="week">Week</option><option value="month">Month</option></pk-radio-group>
<pk-radio-group variant="segmented" fill label="Channel" value="all"><option value="all">All</option><option value="web">Web</option><option value="market">Market</option></pk-radio-group>
```

## `pk-range`

**Range** (Form controls). A slider, or with dual a minimum and maximum slider, drawn from tokens with a live value output.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `min` | `min` | number | `0` |  | Lowest value. |
| `max` | `max` | number | `100` |  | Highest value. |
| `step` | `step` | number | `1` |  | Increment. |
| `value` | `value` | number | `0` |  | Single mode: the value. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `value-low` | `valueLow` | number | `0` |  | Dual mode: the low value. |
| `value-high` | `valueHigh` | number | `100` |  | Dual mode: the high value. |
| `dual` | `dual` | boolean | `false` |  | Two thumbs: a low and a high value; the low thumb never passes the high one. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `output` | `output` | boolean | `false` |  | Shows the current value under the slider. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-range` | `{ value: number, low: number, high: number }` | The user committed a value (single) or a pair (dual). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the first thumb. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `wrap` | The slider area. |
| `track` | The track with its fill. |
| `control` | The single slider, or the low thumb. |
| `control-high` | The high thumb (dual). |
| `output` | The value text. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-range-thumb` | `1.25rem` | Thumb size. |
| `--pk-range-fill` | `var(--color-accent)` | Colour of the filled track and the thumb. |
| `--pk-range-track` | `a mix of muted and the input colour` | Colour of the empty track. |

**Accessibility.** Native range inputs: arrow keys, Home, End and Page keys; each thumb of a dual slider is named "<label> minimum" and "<label> maximum". The 44px phone hit area comes from the taller wrapper. A form receives one entry (single) or two entries under the same name (dual).

Example: Single and dual

```html
<pk-range label="Volume" value="40" output></pk-range>
<pk-range label="Price range" dual value-low="20" value-high="80" output></pk-range>
```

## `pk-rating`

**Rating** (Form controls). A whole-star rating input (radios underneath), and a compact read-only display form.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | number | `0` |  | The rating, 0 for none. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `max` | `max` | number | `5` |  | Number of stars. |
| `readonly` | `readonly` | boolean | `false` |  | The display form: not interactive, exposed as an image with a text alternative. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-rating-change` | `{ value: number }` | The user changed the value (same moment as change; carries it). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the chosen star. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `group` | The stars container. |
| `star` | One star (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-rating-color` | `var(--field-warn)` | Colour of a lit star. |

**Accessibility.** An input rating is a radio group: each star is named "3 stars", arrow keys change the value and focus is visible. The read-only form is role=img with "4 out of 5 stars". Stars are 44px on a phone.

Example: Input and display

```html
<pk-rating label="Your rating" value="3"></pk-rating>
<pk-rating readonly value="4"></pk-rating>
```

## `pk-select`

**Select** (Form controls). The browser select in the same skin as the text input. Its options are its own option and optgroup children; a phone shows its native picker.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `warning` | `warning` | boolean | `false` |  | Shows the warning state (a value the user may want to check). |
| `valid` | `valid` | boolean | `false` |  | Shows the valid state. |
| `multiple` | `multiple` | boolean | `false` |  | Allows several selections (value is comma-separated; the form submits one entry each). |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | option and optgroup elements. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-value-change` | `{ value: string }` | The user committed a new value (same moment as change; carries the value). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Moves focus to the select. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The bordered field. |
| `control` | The inner select. |
| `chevron` | The drop-down arrow. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |

**Accessibility.** A native select in the shadow root: keyboard, type-to-select and the platform picker are the browser's own. Named by label, described by description.

Example: Select and multiple

```html
<pk-select label="Status" value="Draft"><option>Active</option><option>Draft</option><option>Archived</option></pk-select>
<pk-select label="Formats" multiple value="JSON"><option>CSV</option><option>JSON</option><option>XML</option></pk-select>
```

## `pk-select-menu`

**Select menu** (Forms & inputs). A single-choice listbox popup that reads native option children, so it degrades to nothing worse than a list; form-associated, keyboard driven with typeahead.

It is form-associated (takes part in a `<form>` by its `name`).

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `value` | `value` | string | `""` |  | Selected option value. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `name` | `name` | string | `""` |  | Form field name. |
| `placeholder` | `placeholder` | string | `"Select…"` |  | Shown with no selection. |
| `open` | `open` | boolean | `false` |  | Whether the list is shown. |
| `disabled` | `disabled` | boolean | `false` |  | Prevents interaction |
| `required` | `required` | boolean | `false` |  | A value is required |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | option elements. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `pk-change` | `{ value: string, label: string, previous: string }` | The selection changed. |
| `pk-open` | `none` | The list opened. |
| `pk-close` | `{ reason: string }` | A close was asked for (Escape, outside press, choosing, the trigger). Cancelable: preventDefault keeps it open. |

**Methods**

| Method | Description |
|---|---|
| `show()` | Opens it |
| `hide()` | Closes it |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `trigger` | The button that opens the list |
| `value` | The selected value text |
| `caret` | The open/close arrow |
| `list` | The option list (rows are built inside it) |

**Accessibility.** The trigger is a combobox with aria-haspopup=listbox and aria-expanded; the list is role=listbox with option children and aria-activedescendant; arrows, Home, End, typeahead, Enter, Escape.

Example: Select menu

```html
<pk-select-menu name="status" value="draft"><option value="draft">Draft</option><option value="active">Active</option><option value="archived">Archived</option></pk-select-menu>
```

## `pk-switch`

**Switch** (Form controls). An on/off toggle that takes part in a form: it submits its value when on and resets with the form.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `checked` | `checked` | boolean | `false` |  | Whether the switch is on. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `"on"` |  | The value submitted while checked. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state (a red ring) and sets aria-invalid. |
| `size` | `size` | enum | `"md"` | `sm` `md` `lg` | Track size. |
| `label-position` | `labelPosition` | enum | `"end"` | `end` `start` `between` | end puts the label after the switch, start before it, between pushes them to opposite edges of the row. |

**Slots** (`slot="name"` on a child)

| Slot | Description |
|---|---|
| (default) | The label. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `change` | `{ checked }` | The user toggled the switch (a native-style event). |
| `pk-change` | `{ checked: boolean }` | The same toggle as a typed custom event; the Blazor wrapper listens to this one. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `control` | The inner button. |
| `track` | The track. |
| `thumb` | The thumb. |
| `label` | The label wrapper. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-switch-on` | `var(--color-accent)` | Track colour when on. |
| `--pk-switch-off` | `var(--color-border)` | Track colour when off. |

**Accessibility.** An inner button with role=switch and aria-checked, named by the label slot. Space and Enter toggle it. In a form it contributes name=value while checked, resets to its initial state with the form, and is disabled by a disabled fieldset. Use it only for settings that take effect at once; a value that needs Save is a checkbox.

Example: In a form

```html
<form>
  <pk-switch name="notify" checked>Email me</pk-switch>
  <pk-button type="submit">Save</pk-button>
</form>
```

Example: Sizes and label position

```html
<pk-switch size="sm">Small</pk-switch>
<pk-switch size="lg" checked>Large</pk-switch>
<pk-switch label-position="between">Email me a daily summary</pk-switch>
```

## `pk-tag-input`

**Tag input** (Form controls). Free-text tags typed into one control: Enter or a separator adds, paste splits, Backspace in an empty field removes the last, duplicates and a maximum are handled.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The tags, comma-joined. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `placeholder` | `placeholder` | string | `""` |  | Hint shown in the empty field. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `separators` | `separators` | string | `","` |  | Characters that end a tag (besides Enter). |
| `max` | `max` | number | `0` |  | Most tags allowed; 0 means no limit. |
| `allow-duplicates` | `allowDuplicates` | boolean | `false` |  | Allows the same tag twice (case-insensitive by default). |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-tags-change` | `{ value: string, tags: string[] }` | A tag was added or removed. |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Focuses the text field. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The bordered field holding tags and the text field. |
| `field` | The text input. |
| `status` | The visually hidden live region. |
| `tag` | One tag pill (dynamic). |
| `tag-label` | A tag's text (dynamic). |
| `tag-remove` | A tag's remove button (dynamic). |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |
| `--pk-tag-bg` | `var(--color-accent)` | Tag pill background. |

**Accessibility.** Each remove button has an aria-label ("Remove dc") and is out of the tab order (Backspace is the keyboard route); adds and removes are announced through a polite status element; a form receives one entry per tag.

Example: Tags

```html
<pk-tag-input label="Tags" value="dc,variant" max="8" placeholder="add a tag"></pk-tag-input>
```

## `pk-textarea`

**Textarea** (Form controls). A multi-line control that can grow with its content up to a cap.

It is form-associated (takes part in a `<form>` by its `name`) and delegates focus to its inner control.

**Props** (set as an attribute in kebab-case, or as a property in camelCase; a boolean is present or absent)

| Attribute | Property | Type | Default | Values | Description |
|---|---|---|---|---|---|
| `name` | `name` | string | `""` |  | The form field name. |
| `value` | `value` | string | `""` |  | The current value; what the form submits. A form reset sets it back to its initial value without raising the commit event, as a native control does: pk-form raises pk-reset after it, so a host that mirrors the value reads it again there. A browser state restore also sets it silently. |
| `placeholder` | `placeholder` | string | `""` |  | Hint shown while empty. |
| `label` | `label` | string | `""` |  | The accessible name (aria-label). pk-field fills it from its own label when this is empty. |
| `description` | `description` | string | `""` |  | Help and error text, exposed as aria-description; pk-field fills it. |
| `disabled` | `disabled` | boolean | `false` |  | Blocks interaction; also set by a disabled fieldset. |
| `readonly` | `readonly` | boolean | `false` |  | The value can be read but not edited. |
| `required` | `required` | boolean | `false` |  | The form cannot be submitted while the value is empty. |
| `invalid` | `invalid` | boolean | `false` |  | Shows the invalid state and sets aria-invalid; set by pk-field or pk-form (or by a Blazor EditContext). |
| `warning` | `warning` | boolean | `false` |  | Shows the warning state (a value the user may want to check). |
| `valid` | `valid` | boolean | `false` |  | Shows the valid state. |
| `rows` | `rows` | number | `3` |  | Visible rows of a fixed-height textarea. |
| `minlength` | `minlength` | string | `""` |  | Minimum text length. |
| `maxlength` | `maxlength` | string | `""` |  | Maximum text length. |
| `autogrow` | `autogrow` | boolean | `false` |  | Grows with the content instead of scrolling. |
| `max-height` | `maxHeight` | number | `0` |  | Auto-grow cap in pixels; 0 means no cap and the box scrolls beyond it otherwise. |

**Events** (`addEventListener`; `pk-*` events are CustomEvents whose `detail` is shown)

| Event | Detail | Description |
|---|---|---|
| `input` | `native Event` | The value changed (native, composed: bubbles out of the shadow root). Read event.target.value. |
| `change` | `native Event` | The user committed the value (re-dispatched from the inner control, composed). |
| `pk-value-change` | `{ value: string }` | The user committed a new value (same moment as change; carries the value). |

**Methods**

| Method | Description |
|---|---|
| `focus()` | Moves focus to the textarea. |
| `select()` | Selects the text. |

**CSS parts** (`::part(name)`)

| Part | Description |
|---|---|
| `box` | The bordered field. |
| `control` | The inner textarea. |

**CSS custom properties** (set on the element or a parent, from a stylesheet)

| Property | Default | Description |
|---|---|---|
| `--pk-control-bg` | `var(--color-input)` | Background of the field. |
| `--pk-control-border` | `var(--color-input-border)` | Border colour. |
| `--pk-control-radius` | `var(--radius-sm)` | Corner radius. |
| `--pk-textarea-max-height` | `none` | Cap on the auto-grow height (set from max-height). |

**Accessibility.** A native textarea in the shadow root, named by label and described by description; invalid sets aria-invalid. Auto-grow never traps scrolling: a capped box scrolls internally.

Example: Fixed and auto-grow

```html
<pk-textarea label="Notes" rows="3" value="Fixed height, resizable."></pk-textarea>
<pk-textarea label="Description" autogrow max-height="160" value="Grows as you type, up to 160px."></pk-textarea>
```
