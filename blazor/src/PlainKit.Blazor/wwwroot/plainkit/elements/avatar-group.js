// GENERATED by tools/build.mjs from elements/avatar-group/: do not edit.
import { PkElement, define } from '../js/element.js';
const behaviour = Base => class extends Base {
    connected() { this.watchSlot('', () => this.requestUpdate()); }
    updated() {
        const people = this.slotted();
        people.forEach((p, i) => { p.hidden = i >= this.max; });
        const more = Math.max(0, people.length - this.max);
        const tile = this.part('more');
        tile.hidden = more === 0;
        tile.textContent = `+${more}`;
        this.aria({ role: 'group', ariaLabel: this.label });
    }
};
export default define(class extends behaviour(PkElement) {
    static tag = "pk-avatar-group";
    static props = {"max":{"type":"number","default":4,"reflect":true},"label":{"type":"string","default":"","reflect":false}};
    static delegatesFocus = false;
    static formAssociated = false;
    static template = "<slot></slot><span part=\"more\"></span>";
    static css = ":host{display: inline-flex;align-items: center}::slotted(pk-avatar){box-shadow: 0 0 0 2px var(--color-panel)}::slotted(pk-avatar:not(:first-child)){margin-inline-start: calc(-1 * var(--pk-avatar-group-overlap,var(--space-2)))}[part=\"more\"]{display: inline-grid;place-items: center;min-inline-size: calc(var(--space-8) + var(--space-1));block-size: calc(var(--space-8) + var(--space-1));margin-inline-start: calc(-1 * var(--pk-avatar-group-overlap,var(--space-2)));padding-inline: var(--space-2);border-radius: var(--radius-pill);background: var(--color-surface-alt);box-shadow: 0 0 0 2px var(--color-panel);color: var(--color-muted);font-size: var(--text-meta);font-weight: 600}";
});
